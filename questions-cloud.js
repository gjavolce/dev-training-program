var TRACK_CLOUD = {
  "id": "cloud",
  "label": "Cloud & Observability",
  "icon": "cloud",
  "desc": "GCP, GKE, and the full observability stack: Cloud Monitoring, OpenTelemetry, Grafana, structured logging, and distributed tracing.",
  "levels": [
    {
      "num": 1, "name": "Foundations",
      "desc": "Reading what the platform is telling you and navigating GCP without getting lost.",
      "scenarios": [
        "Reading Grafana dashboards and distinguishing real alerts from noise",
        "Using kubectl to diagnose pod restarts (OOM, probe failures, crash loops)",
        "Using Cloud Logging to filter and find specific log entries",
        "Following runbook triage steps and escalating with useful context",
        "Reading Cloud SQL metrics dashboards (CPU, connections, storage)"
      ]
    },
    {
      "num": 2, "name": "Practitioner",
      "desc": "Instrumenting your service correctly and contributing to the team's observability setup.",
      "scenarios": [
        "Configuring GKE resource requests/limits and verifying service health in Cloud Monitoring",
        "Adding structured log statements with trace ID, tenant ID, and operation context",
        "Instrumenting custom metrics with Micrometer and building Grafana panels",
        "Using distributed tracing (Cloud Trace / Grafana Tempo) to follow requests across services",
        "Defining alert conditions with appropriate thresholds and runbook links"
      ]
    },
    {
      "num": 3, "name": "Advanced",
      "desc": "Diagnosing complex production issues across multiple services and owning the observability for a domain.",
      "scenarios": [
        "Correlating distributed traces with metrics to diagnose cross-service latency",
        "Investigating GKE node-level resource issues (throttling, pod scheduling)",
        "Tuning OpenTelemetry configuration (sampling, cardinality, noisy spans)",
        "Designing golden-signals dashboards for business-critical flows",
        "Reading and modifying Terraform modules for GKE and Cloud SQL"
      ]
    },
    {
      "num": 4, "name": "Specialist",
      "desc": "Owning the observability platform for the domain and ensuring the team can operate services in production with confidence.",
      "scenarios": [
        "Leading SLO/SLI definition and error budget management for critical services",
        "Analysing GCP cost breakdowns and proposing concrete optimisations",
        "Designing zero-downtime deployment sequencing with observability checkpoints",
        "Producing reusable observability guides (logging, metrics, tracing, alerts, dashboards)",
        "Planning and executing Cloud SQL maintenance with minimal disruption"
      ]
    },
    {
      "num": 5, "name": "Expert",
      "desc": "Setting observability and cloud standards for the organisation and mentoring teams through complex production scenarios.",
      "scenarios": [
        "Defining org-wide instrumentation standards and driving adoption",
        "Taking incident commander role for major cross-team incidents",
        "Leading evaluations of new observability tools against current stack",
        "Analysing GKE platform costs and modelling optimisation scenarios for leadership",
        "Defining runbook standards and running peer-review sessions"
      ]
    },
    {
      "num": 6, "name": "Authority",
      "desc": "Cloud platform strategy, reliability engineering at the organisational level, and technology direction.",
      "scenarios": [
        "Leading long-term GCP strategy (managed vs GKE vs serverless, multi-region)",
        "Designing and running DR drills with success criteria and gap analysis",
        "Owning the cloud cost narrative at the executive level",
        "Producing audit evidence for security event detection and response",
        "Leading platform capability rollouts that teams adopt because they're useful"
      ]
    }
  ],
  "questions": [
    {"level":1,"diff":1,"scenario":0,"q":"A Grafana panel shows a red alert on 'HTTP error rate'. The panel has been red for 2 weeks and no one has complained. What is the most likely explanation?","opts":["A real ongoing incident that no one noticed","The alert threshold is misconfigured — the metric is normal but the threshold is too low","The Grafana data source is broken and returning fake data","The panel is showing data from a decommissioned service","Grafana has a known bug with HTTP error rate panels"],"ans":1,"fb":"A persistent alert nobody acts on usually means the threshold is wrong, not a real incident. When an alert fires continuously with no user impact, suspect a misconfigured threshold first. Real incidents get noticed through user complaints or on-call pages."},
    {"level":1,"diff":1,"scenario":0,"q":"A Grafana panel shows a spike in latency p99 at 14:00, then returns to normal by 14:05. No alert fired. Should you investigate?","opts":["No — no alert fired, so it was within normal bounds","Yes — a 5-minute p99 spike may indicate a transient issue worth documenting even if auto-resolved","No — p99 spikes are always caused by garbage collection and are expected","Yes — you must open a P1 incident for any latency spike","No — Grafana only shows real problems when the panel is red"],"ans":1,"fb":"A transient spike that auto-resolved is still worth a brief investigation. It may have caused real user impact for those 5 minutes, or it may be a leading indicator of a larger issue. Check if it correlates with a deployment, a batch job, or a Kafka lag spike."},
    {"level":1,"diff":2,"scenario":0,"q":"You open Grafana during an on-call shift and see two panels: one red (error rate > 5%), one yellow (latency p95 slightly elevated). Which do you investigate first?","opts":["The yellow panel — elevated latency affects more users than errors","The red panel — error rate above 5% is a direct user-facing symptom and highest severity","Both simultaneously — they are likely unrelated","Neither — wait for an alert to page you before investigating","The oldest panel — investigate in chronological order"],"ans":1,"fb":"Error rate > 5% is a direct symptom: users are seeing failures. Elevated p95 latency may be minor or a precursor. Always triage the most severe user-facing signal first — errors over slowness."},
    {"level":1,"diff":3,"scenario":0,"q":"Grafana shows a panel with a constant flat line at exactly 0 for 'payment_success_total'. Is this good or bad?","opts":["Good — zero errors means the service is healthy","Bad — a flat line at 0 on a counter usually means the metric is not being emitted or the data source query is broken, not that there are zero successes","Neutral — counters at 0 are normal outside business hours","Bad — a payment counter at 0 means all payments are failing","Good — this confirms the payment service is idle"],"ans":1,"fb":"A counter stuck at exactly 0 for an active service is a red flag. Either the metric is not being scraped, the label filter in the query is wrong, or the service is not emitting the metric. A healthy payment service in production should always show some non-zero rate. Verify with a simpler query before trusting the panel."},
    {"level":1,"diff":1,"scenario":1,"q":"You run `kubectl get pods` and see a pod with STATUS `CrashLoopBackOff`. What does this mean?","opts":["The pod is starting up and will be ready shortly","The pod's container is repeatedly crashing and Kubernetes is backing off restart attempts","The pod was manually stopped by an operator","The pod is waiting for a ConfigMap to be updated","CrashLoopBackOff means the pod has too many replicas scheduled"],"ans":1,"fb":"CrashLoopBackOff means the container exits shortly after starting, and Kubernetes is increasing the delay between restart attempts. Check `kubectl logs <pod>` and `kubectl describe pod <pod>` to find the crash reason — common causes are OOM kills, missing config, or failed health checks."},
    {"level":1,"diff":1,"scenario":1,"q":"A pod shows RESTARTS=5 in `kubectl get pods`. What is the first command to understand why it restarted?","opts":["kubectl delete pod <pod> — force a clean restart","kubectl logs <pod> --previous — view logs from the previous (crashed) container instance","kubectl exec <pod> -- ps aux — check what processes are running","kubectl rollout undo deployment/<name> — revert to the previous version","kubectl top pod <pod> — check current CPU and memory usage"],"ans":1,"fb":"`kubectl logs <pod> --previous` shows the logs of the terminated container instance, which contains the crash reason. Without `--previous`, you see the current (running) container logs, which won't show why it crashed. Use `kubectl describe pod <pod>` to see the termination reason (OOMKilled, Error, etc.)."},
    {"level":1,"diff":2,"scenario":1,"q":"A pod shows `OOMKilled` as the termination reason. What does this indicate and what is the immediate action?","opts":["The pod crashed due to a code exception — redeploy with a bug fix","The container exceeded its memory limit and was killed by the kernel. Check memory usage trends and either fix a memory leak or increase the memory limit in the pod spec","OOMKilled means the node ran out of memory — drain and replace the node","OOMKilled is a Kafka consumer error — check consumer group lag","The pod was killed because it was unhealthy — check liveness probe configuration"],"ans":1,"fb":"OOMKilled (Out of Memory Killed) means the container hit its `resources.limits.memory` and the OOM killer terminated it. Check actual usage with `kubectl top pod` and Grafana JVM heap panels. If usage consistently approaches the limit, either increase the limit or investigate a memory leak with heap dump analysis."},
    {"level":1,"diff":2,"scenario":1,"q":"A pod's liveness probe is failing but the service appears to handle requests correctly. What could cause this?","opts":["A liveness probe failure always means the service is broken — the probe is correct","The probe endpoint may be slow to respond, causing timeouts. Or the probe path/port is misconfigured. A liveness failure causes pod restarts even if the service is functional — check probe config in the deployment spec","Liveness probes only affect readiness — the pod still receives traffic when liveness fails","The probe fails because the pod is under-resourced — add more CPU","Liveness probes are advisory — Kubernetes only logs failures, does not restart pods"],"ans":1,"fb":"A liveness probe failure triggers a pod restart even if the service is handling traffic. Common causes: the probe endpoint is slower than `timeoutSeconds`, the port is wrong, or the `/health/liveness` endpoint has a bug. Check `kubectl describe pod` for probe failure events and review the probe configuration in the deployment."},
    {"level":1,"diff":3,"scenario":1,"q":"Three pods in a deployment all show RESTARTS=0 but users report intermittent 502 errors. What kubectl investigation steps would you take?","opts":["Run `kubectl delete pod` on all pods to force a restart and see if errors stop","Check `kubectl get pods` for Ready status, run `kubectl describe pod` for each to check for probe failures or events, and check `kubectl get endpoints` to confirm all pods are registered in the service — intermittent 502s suggest a pod is in a broken state but passing liveness while failing readiness","Run `kubectl scale deployment/<name> --replicas=6` to add more pods and reduce load","502s are always caused by missing ConfigMaps — check `kubectl get configmap`","Run `kubectl rollout restart deployment/<name>` immediately without investigating"],"ans":1,"fb":"502s with RESTARTS=0 often mean a pod is registered but not healthy. Check: (1) `kubectl describe pod` for readiness probe failures or warning events, (2) `kubectl get endpoints` to confirm all pods are in the endpoint list — a pod failing readiness is removed from the service. Pods can be alive (liveness passing) but not ready (readiness failing), causing traffic to hit it and return errors in some load balancer configurations."},
    {"level":1,"diff":1,"scenario":2,"q":"In Cloud Logging, you want to find all ERROR-level logs from the payment-service in the last hour. Which filter would you use?","opts":["severity=ERROR AND timestamp>now()-1h","resource.type=\"k8s_container\" AND resource.labels.container_name=\"payment-service\" AND severity=ERROR","logName=\"payment-service\" AND level=ERROR","service=payment-service AND log_level>WARNING","container.name=payment-service severity>=ERROR"],"ans":1,"fb":"Cloud Logging uses structured filter syntax. `resource.type=\"k8s_container\"` scopes to GKE containers, `resource.labels.container_name` identifies the specific service, and `severity=ERROR` filters by log level. The time range is set via the UI time picker or `timestamp` filter. Incorrect syntax like `logName=payment-service` or `level=ERROR` won't match Cloud Logging's schema."},
    {"level":1,"diff":1,"scenario":2,"q":"A developer says 'I can't find my log line in Cloud Logging — I printed it with System.out.println'. What is the issue?","opts":["GKE does not capture stdout — use a file appender instead","Spring Boot routes stdout to Cloud Logging but only if the app is deployed with a specific label","stdout from containers is captured by Cloud Logging, but plain System.out.println produces an unstructured log with no severity field, making it hard to filter. Use a structured logger (SLF4J/Logback) so logs have severity, timestamp, and searchable fields","System.out.println output is delayed by 10 minutes before appearing in Cloud Logging","Cloud Logging only captures logs at ERROR level or above — use System.err.println instead"],"ans":2,"fb":"Container stdout is captured by Cloud Logging, so the log line is there — but System.out.println produces an unstructured string with no severity, no trace ID, no tenant ID. It's hard to filter and correlates with nothing. Structured logging with Logback/SLF4J emits JSON with severity, timestamp, and custom fields that Cloud Logging indexes and makes searchable."},
    {"level":1,"diff":2,"scenario":2,"q":"You need to find all logs for a specific request that returned a 500 error. The trace ID is `abc123`. How do you find all related log lines across services?","opts":["Search for `500` in Cloud Logging — this finds all logs related to that request","Filter by `labels.trace_id=\"abc123\"` or `jsonPayload.traceId=\"abc123\"` to find all log lines that include the trace ID, across any service","Search by time range and manually correlate log lines by reading through them","Use `kubectl logs` to dump all pod logs and grep locally","Trace IDs are only in Cloud Trace — Cloud Logging cannot filter by trace ID"],"ans":1,"fb":"If services log the trace ID as a structured field, filtering by it in Cloud Logging returns all correlated log lines from all services in a single query. This is why structured logging with trace ID propagation is essential — it enables request-scoped log correlation across service boundaries without manual time-based correlation."},
    {"level":1,"diff":3,"scenario":2,"q":"Cloud Logging shows the payment service emitting 10,000 log lines per minute at INFO level, causing excessive logging costs. What is the right approach?","opts":["Delete all INFO logs from Cloud Logging using a sink exclusion filter","Lower the log level to WARN or ERROR in production via the logging configuration, or use log-based metrics + sampling. Keep DEBUG for local development. Structured logs at WARN/ERROR with full context are more useful than verbose INFO logs in production","Disable Cloud Logging entirely for the payment service","Increase the Cloud Logging quota to accommodate the volume","INFO logs are free — logging costs only apply to ERROR and above"],"ans":1,"fb":"10,000 log lines/minute at INFO is excessive for production. Best practice: set production log level to WARN/ERROR, log INFO only for critical business events (payment initiated, payment confirmed), and use metrics for high-frequency measurements. Cloud Logging charges by ingestion volume — reducing log verbosity is the primary cost control lever. Log-based metrics can replace log lines for counting events."},
    {"level":1,"diff":1,"scenario":3,"q":"An alert fires at 03:00. The runbook says 'Step 1: check pod health'. You check and all pods are Running. What do you do next?","opts":["Close the alert — if pods are running, everything is fine","Follow the next step in the runbook — runbooks are sequential; pod health is step 1, not the only step","Page the on-call lead immediately since you don't know what to do","Restart all pods as a precaution","Delete the alert rule — it is generating false positives"],"ans":1,"fb":"Runbooks are designed as sequential diagnostic steps. Pod health being OK rules out one cause but the alert still fired for a reason. Follow the next runbook step — it will check a more specific symptom. Only escalate after exhausting the runbook steps or finding something the runbook doesn't cover."},
    {"level":1,"diff":2,"scenario":3,"q":"You've followed a runbook for 20 minutes and cannot resolve the issue. What information should you include when escalating to the senior engineer?","opts":["Just say 'the service is down' and let the senior engineer investigate from scratch","Include: which alert fired, what the runbook steps were and their outcomes, current metrics (error rate, latency, affected tenants), what you've already tried, and the relevant log snippets — this allows the senior to start from where you left off, not from scratch","Send a link to the Grafana dashboard and wait","Only send the error message from the logs","Include your personal assessment of what the root cause is, even if you are not certain"],"ans":1,"fb":"Effective escalation transfers your investigation context — the senior shouldn't repeat work you've done. Include: the alert, what the runbook showed, current observable symptoms, what you ruled out, and what you couldn't explain. A structured escalation like 'Alert X fired at 03:00. Pods healthy, Cloud SQL healthy, but error rate is 8% on payment POST endpoints, started at 02:47. Runbook steps 1-3 completed, step 4 inconclusive' saves 10 minutes versus 'service is broken'."},
    {"level":1,"diff":2,"scenario":3,"q":"A runbook step says 'check Cloud SQL connection count'. You find it at 195 out of 200 max connections. What do you do?","opts":["This is within limits — 195/200 is fine, continue to the next runbook step","Flag this as a likely contributing factor, note it in your escalation context, and continue to the next runbook step to see if it correlates with the alert symptom — 195/200 is close to the limit and may be causing connection timeout errors","Immediately increase Cloud SQL max_connections to 500","Restart all application pods to force connection pool drain","This is a database problem — escalate to the DBA team immediately without continuing"],"ans":1,"fb":"195/200 connections is a near-saturated pool — likely contributing to the issue but not a definitive root cause until correlated. Note it as evidence and check if the alert symptom (e.g. connection timeout errors) aligns with this. Runbook steps build a picture together. Escalate with this finding included."},
    {"level":1,"diff":3,"scenario":3,"q":"The runbook tells you to run `gcloud sql instances describe <instance>` but you don't have the gcloud CLI configured. What should you do?","opts":["Skip this runbook step and proceed to the next one","Use the GCP Console Cloud SQL page to check the instance status — the same information is available via UI. Note the gap and suggest adding a console-based alternative to the runbook afterward","Give up and escalate immediately","Install gcloud CLI on the production pod and run it from there","Restart the Cloud SQL instance from the GCP Console as a workaround"],"ans":1,"fb":"Runbooks should be accessible to all on-call engineers. When a tool isn't available, use an equivalent — Cloud SQL status is visible in the GCP Console. The important thing is completing the diagnostic step, not the specific tool. After the incident, suggest a runbook improvement to add console-based alternatives for common CLI steps."},
    {"level":1,"diff":1,"scenario":4,"q":"The Cloud SQL dashboard shows CPU at 85% for the last 30 minutes. What is the most appropriate first action?","opts":["Immediately upgrade the Cloud SQL instance to a larger tier","Check which queries are running with `pg_stat_activity` or look for a recent deployment or batch job that correlates with the CPU spike","Restart the Cloud SQL instance to clear the CPU usage","Disable all non-critical services to reduce database load","CPU at 85% is normal for production — no action needed"],"ans":1,"fb":"CPU spikes are often caused by a specific query or workload event. Before scaling, investigate root cause: check `pg_stat_activity` for long-running queries, correlate the spike with recent deployments or scheduled jobs, and check if a specific query plan changed. Scaling without understanding the cause may be wasteful and won't fix a query problem."},
    {"level":1,"diff":2,"scenario":4,"q":"The Cloud SQL dashboard shows active connections jumping from 20 to 190 in 5 minutes. The application just scaled from 2 to 8 pods. Is this expected?","opts":["No — a jump to 190 connections indicates a connection leak","Yes — if each pod has a HikariCP minimumIdle of about 24, scaling from 2 to 8 pods would add ~144 connections, explaining the jump","No — Cloud SQL should handle connection pooling automatically regardless of pod count","Yes — more pods always means more connections, and this is fine regardless of the number","No — HikariCP never opens more than 10 connections per pod"],"ans":1,"fb":"Connection count scales with pod count when using per-pod connection pools (HikariCP). If minimumIdle is set, each new pod immediately opens connections to fill the idle pool. 8 pods × ~24 minimumIdle ≈ 192 connections, consistent with the observed jump. Verify the pool configuration and ensure max connections across all pods stay safely below Cloud SQL's `max_connections` limit."},
    {"level":1,"diff":2,"scenario":4,"q":"Cloud SQL storage is at 78%. Auto-resize is disabled. What action do you take?","opts":["No action needed — 78% is fine until it reaches 100%","Create a ticket to proactively resize storage before it fills, and enable auto-resize as a safety net. Alert at 80% is the recommended threshold — waiting for 100% causes write failures","Enable auto-resize immediately via `gcloud sql instances patch`","Restart the database to reclaim storage space","Delete old tables to free up space immediately"],"ans":1,"fb":"78% with auto-resize disabled is close to the 80% alert threshold. Proactive action: resize storage now (storage can be increased but not decreased on Cloud SQL), investigate what's consuming space (table bloat, WAL, log tables), and enable auto-resize as a safety net. Cloud SQL becomes read-only at 100% storage — don't wait."},
    {"level":1,"diff":3,"scenario":4,"q":"The Cloud SQL dashboard shows CPU at 5% but active connections are 0. The application is deployed and pods are Running. What is wrong?","opts":["This is healthy — low CPU with no active connections means low traffic","Zero active connections with running pods is abnormal. The pods may be failing to connect (wrong credentials, network policy blocking, Auth Proxy misconfigured). Check pod logs for connection errors and verify Cloud SQL Auth Proxy sidecar status","CPU at 5% confirms the database is fine — the metric is lagging","Zero connections means HikariCP is using lazy initialization — this is expected at startup","Cloud SQL reports 0 active connections during maintenance windows"],"ans":1,"fb":"Running pods should always have some connections to the database (at minimum, HikariCP minimumIdle connections). Zero connections with running pods means connection establishment is failing. Check pod logs for JDBC errors, verify the Cloud SQL Auth Proxy sidecar is running (`kubectl describe pod`), confirm the service account has Cloud SQL Client IAM role, and check network policies allow pod-to-proxy traffic."},
    {"level":2,"diff":1,"scenario":0,"q":"A Spring Boot service has no resource requests set on its GKE deployment. What is the impact?","opts":["No impact — Kubernetes manages resource allocation automatically without requests","Without requests, the pod may be placed on an overloaded node, be the first killed during resource pressure (QoS class BestEffort), and HPA cannot calculate utilization percentages","Resource requests are optional for non-production services only","Without requests, the pod gets the highest priority on the node","Resource requests only affect billing — no operational impact"],"ans":1,"fb":"Resource requests tell the Kubernetes scheduler how much CPU/memory the pod needs. Without them: (1) pods get QoS class BestEffort — first evicted under node pressure, (2) the scheduler may pack too many pods on one node, (3) HPA cannot calculate utilization percentages. Always set requests based on observed usage from Grafana."},
    {"level":2,"diff":1,"scenario":0,"q":"In Cloud Monitoring, you want to verify a GKE service is up and responding. What is the simplest approach?","opts":["SSH into a pod and run curl every minute","Create a Cloud Monitoring uptime check targeting the service's external URL, using HTTP with the /health endpoint — Cloud Monitoring polls it and alerts if it stops responding","Deploy a separate GKE pod that curl-checks the service every 30 seconds","Use kubectl to check pod status periodically from a CronJob","Uptime checks are only available for Cloud SQL, not GKE services"],"ans":1,"fb":"Cloud Monitoring uptime checks send HTTP(S) requests from multiple Google locations and alert if the endpoint becomes unreachable or returns errors. Target the service's load balancer IP or domain with the /health/readiness path. This is simpler and more reliable than self-monitoring pods."},
    {"level":2,"diff":2,"scenario":0,"q":"A pod's CPU request is 100m but it consistently uses 800m during normal load. What are the consequences?","opts":["No consequences — requests are just hints and do not affect actual CPU usage","The pod uses more CPU than requested. HPA calculates utilization as actual/request — 800m/100m = 800%, which may trigger unnecessary scale-out. On a loaded node, the pod's CPU may also be throttled by its limit","CPU requests cap actual usage — the pod cannot use more than 100m","The pod will be evicted immediately for exceeding its CPU request","Cloud Monitoring will show a critical alert for CPU over-utilization"],"ans":1,"fb":"CPU requests define the scheduler's placement decision, not a hard cap. The pod can use more CPU (up to the limit or node capacity). The problem: (1) HPA calculates utilization as actual/request — 800m/100m = 800%, which may trigger unnecessary scale-out, (2) on a loaded node, the pod's CPU may be throttled. Fix: set requests based on p95 actual usage from Grafana metrics."},
    {"level":2,"diff":2,"scenario":0,"q":"You set CPU limit to 500m and CPU request to 500m. Under load, the pod's response time doubles. Grafana shows CPU usage hitting exactly 500m. What is happening?","opts":["The pod needs more replicas — this is a scale-out event","The pod is CPU-throttled. When a container hits its CPU limit, the Linux CFS throttles it, causing latency spikes. Increase the CPU limit or optimize the hot code path","500m CPU is always enough for a Spring Boot service — investigate the database","The response time doubling is caused by GC — not related to CPU limits","Cloud Monitoring is showing incorrect CPU data — check the data source"],"ans":1,"fb":"When CPU usage hits the limit, Kubernetes (via Linux CFS) throttles the container — it pauses execution until the next CPU quota period. This causes latency spikes. Evidence: CPU usage plateaus at exactly the limit value. Fix: increase the limit or profile the hot path with async-profiler to reduce CPU consumption. Check `container_cpu_cfs_throttled_seconds_total` in metrics for confirmation."},
    {"level":2,"diff":3,"scenario":0,"q":"A team proposes setting resource limits to 10x the requests (e.g., request=100m, limit=1000m) for flexibility. What is the risk?","opts":["No risk — a large gap between request and limit is the recommended GKE configuration","A large limit-to-request ratio enables noisy neighbour problems: one pod spikes and steals resources from all pods on the node. For memory, hitting the limit causes OOMKill. GKE best practice is to set limits close to requests and tune based on profiling","Limits must be equal to requests — GKE rejects deployments where they differ","The only risk is higher billing — no operational impact","A large gap helps with traffic spikes — it is the correct approach for bursty services"],"ans":1,"fb":"Wide request-to-limit ratios enable noisy neighbour scenarios. The scheduler placed pods based on requests (100m), but the pod can burst to 1000m — 10x more than expected. On a node with 16 such pods, one burst can consume the entire node's CPU. For memory, hitting a high limit causes OOMKill which is a hard crash. Best practice: set limits close to requests, use VPA recommendations for initial sizing, and tune with real production data."},
    {"level":2,"diff":1,"scenario":1,"q":"You need to add the current trace ID to every log statement in a Spring Boot service. What is the correct approach?","opts":["Manually include the trace ID in every log.info() call as a string parameter","Configure the MDC (Mapped Diagnostic Context) with the trace ID via a request filter. Logback then includes MDC fields in all log statements automatically without modifying each call","Add the trace ID to the application.properties file","Use a custom Logback appender that queries Cloud Trace for the trace ID","Trace IDs are automatically included in all Spring Boot logs — no configuration needed"],"ans":1,"fb":"MDC is the correct pattern. A servlet filter calls `MDC.put(\"traceId\", currentTraceId)` on request entry and `MDC.clear()` on exit. Logback's pattern layout includes `%X{traceId}` to emit the MDC value in every log line. With OpenTelemetry Java agent, the trace ID is automatically set in MDC via the `otel.instrumentation.logback-mdc.add-baggage` configuration."},
    {"level":2,"diff":2,"scenario":1,"q":"A log line shows: `INFO payment completed amount=5000`. An engineer wants to filter logs by tenant. What is missing and how do you fix it?","opts":["Nothing is missing — amount is sufficient context for filtering","The tenant ID is missing as a structured field. Add it: `log.info(\"payment completed\", kv(\"amount\", amount), kv(\"tenantId\", tenantId))`. Then filter in Cloud Logging with `jsonPayload.tenantId=\"tenant-abc\"`","Add the tenant ID as a log level suffix: `INFO:tenant-abc payment completed`","Tenant filtering is done in the application layer — Cloud Logging cannot filter by tenant","The fix is to add a comment in the code noting the tenant ID"],"ans":1,"fb":"Structured logging requires all searchable context as key-value fields in the JSON payload. A log line without tenantId cannot be filtered by tenant in Cloud Logging. Use structured logging (e.g. `log.atInfo().addKeyValue(\"tenantId\", tenantId).log(\"payment completed\")` in Spring Boot 3, or a library like `net.logstash.logback`). Cloud Logging then indexes `jsonPayload.tenantId` for filtering."},
    {"level":2,"diff":2,"scenario":1,"q":"Every log statement must include traceId, tenantId, and serviceName. Where is the best place to set these so they appear automatically?","opts":["Set them in each log.info() call manually across all classes","Set traceId and tenantId in MDC via a request-scoped filter, and serviceName in logback.xml as a static property — all log statements in the request scope then emit these fields without per-statement changes","Set them in a global static variable accessible by all loggers","Use a custom Logger wrapper class that all services must inherit from","Set them in Cloud Logging directly via the GCP Console"],"ans":1,"fb":"MDC is request-scoped thread-local storage for contextual logging fields. A filter sets `MDC.put(\"traceId\", ...)` and `MDC.put(\"tenantId\", ...)` on request entry and clears on exit. `serviceName` is static — set it in logback.xml as a `<property>` or via Spring's `logging.structured.json.include`. This approach requires zero changes to individual log statements."},
    {"level":2,"diff":3,"scenario":1,"q":"A service logs customer email addresses in DEBUG statements used for development. These logs are reaching Cloud Logging in production. What is the risk and fix?","opts":["No risk — Cloud Logging is a secure GCP service and PII in logs is acceptable","PII in logs violates GDPR/PCI-DSS requirements and increases breach impact. Fix: remove PII from log statements, use masked representations, and ensure production log level is WARN+ so DEBUG statements don't emit. Add a log audit step to the deployment checklist","The risk is only storage costs — PII does not create compliance issues in GCP","Fix by encrypting the Cloud Logging bucket — this satisfies all compliance requirements","PII in logs is acceptable if the service is not customer-facing"],"ans":1,"fb":"PII in logs creates compliance and breach risk. Under GDPR, logs are data storage — PII requires a legal basis, and a log breach is a reportable data breach. Under PCI-DSS, card data in logs is a finding. Fix: (1) remove PII from log statements — log IDs, not values, (2) set production log level to WARN to suppress DEBUG, (3) use Cloud Logging exclusion filters as a safety net, (4) implement log review in your security checklist."},
    {"level":2,"diff":1,"scenario":2,"q":"You want to count the number of payment requests per second in your Spring Boot service using Micrometer. Which Micrometer type do you use?","opts":["Gauge — it measures a current value like request count","Counter — it increments on each payment request. Use `registry.counter(\"payment.requests\", \"status\", \"success\").increment()`","Timer — timers are the only Micrometer type that track request counts","Distribution summary — for counting discrete events","Histogram — Micrometer histograms count events automatically"],"ans":1,"fb":"A Counter increments monotonically and is perfect for counting events (requests, payments, errors). Use `counter.increment()` on each request. In Grafana, use `rate(payment_requests_total[5m])` to get requests per second. A Gauge would be wrong — it measures current state (queue depth, active connections), not cumulative events."},
    {"level":2,"diff":1,"scenario":2,"q":"You've added a Micrometer timer to a payment endpoint but the metric doesn't appear in Grafana. What is the most likely cause?","opts":["Grafana cannot display timer metrics — use a counter instead","The Prometheus scrape endpoint (/actuator/prometheus) is not enabled or not configured in prometheus.yml. Verify `management.endpoints.web.exposure.include=prometheus` in application.properties","Micrometer metrics require a restart of the Grafana server","Timer metrics appear after 100 requests — insufficient traffic so far","Micrometer is disabled by default in Spring Boot 3"],"ans":1,"fb":"The most common cause for missing metrics: the Prometheus scrape endpoint isn't exposed or isn't being scraped. Check: (1) `management.endpoints.web.exposure.include=prometheus` in application.properties, (2) the Prometheus scrape config has the service's pods annotated with `prometheus.io/scrape: \"true\"`, (3) access `<pod-ip>:8080/actuator/prometheus` directly to verify the metric is emitted."},
    {"level":2,"diff":2,"scenario":2,"q":"You want a Grafana panel showing p99 response time for the payment service over the last 1 hour. Which PromQL expression is correct?","opts":["avg(payment_requests_duration_seconds)","histogram_quantile(0.99, sum(rate(payment_requests_duration_seconds_bucket[5m])) by (le))","max(payment_requests_duration_seconds_sum / payment_requests_duration_seconds_count)","payment_requests_p99 — Micrometer publishes pre-computed percentiles","count(payment_requests_duration_seconds > 1)"],"ans":1,"fb":"Micrometer's Timer emits `_bucket`, `_count`, and `_sum` metrics by default. `histogram_quantile(0.99, ...)` computes the 99th percentile from the histogram buckets. The `rate()` function calculates per-second rates from the counter-type bucket series. This is the standard PromQL pattern for p99/p95 latency from Micrometer Timer metrics."},
    {"level":2,"diff":2,"scenario":2,"q":"A Grafana panel shows 'No data' for a metric that was working yesterday. What are the first two things to check?","opts":["Redeploy the service — metric emission stops after 24 hours","Check whether the service pods are still emitting the metric (curl /actuator/prometheus), and check whether the Prometheus scrape target is still healthy — common causes are pod IP changes or scrape config issues","Delete the Grafana panel and recreate it","Check if the Grafana license expired","No data always means the service is down — escalate immediately"],"ans":1,"fb":"'No data' in Grafana can mean: (1) the metric is no longer being emitted (service issue, code change), (2) Prometheus stopped scraping it (pod IP changed, scrape target broken), (3) the metric name changed. Start with the raw source: `curl <pod-ip>/actuator/prometheus | grep <metric-name>`. Then check Prometheus targets for scrape errors."},
    {"level":2,"diff":3,"scenario":2,"q":"A team proposes using tenantId as a Micrometer tag on a counter. The platform has 10,000 tenants. What is the risk?","opts":["No risk — Micrometer tags are designed for high-cardinality values like tenant IDs","High cardinality labels cause a metric cardinality explosion. 10,000 tenant IDs × multiple metrics = potentially millions of unique time series in Prometheus, causing memory exhaustion and slow queries. Use log-based metrics in Cloud Logging for per-tenant analytics or aggregate at a higher granularity in Micrometer","Using tenantId tags is fine if you limit the counter to once per minute","High cardinality is only a problem with histograms, not counters","Use tenantId tags but set a low retention period to limit storage impact"],"ans":1,"fb":"Prometheus stores each unique label combination as a separate time series. 10,000 tenants × 3 metrics × 3 label values = tens of thousands of series per scrape interval. At scale, this exhausts Prometheus memory. Solutions: (1) log the per-tenant data as structured log lines and use Cloud Logging log-based metrics with tenant filter, (2) use BigQuery for per-tenant analytics, (3) keep Micrometer metrics at aggregate level (all tenants)."},
    {"level":2,"diff":1,"scenario":3,"q":"A request is slow and you want to trace it through all services. What does a distributed trace show that logs alone cannot?","opts":["Distributed traces show the exact SQL queries executed — logs do not","A trace shows the timing and sequence of calls across all services as a waterfall: which service called which, how long each segment took, and where the time was spent","Distributed traces show CPU usage per service — logs show memory usage","Traces show error messages — logs show timing information","Both traces and logs show the same information in different formats"],"ans":1,"fb":"A distributed trace reconstructs the full call graph as a waterfall: Service A called Service B (50ms), which called Service C (200ms), which queried PostgreSQL (180ms). This cross-service timing view is impossible to reconstruct manually from per-service logs. Traces and logs complement each other: traces show where time was spent, logs show what happened within each service."},
    {"level":2,"diff":2,"scenario":3,"q":"Cloud Trace shows a request trace for a payment flow. The total duration is 2 seconds but the sum of all spans is only 800ms. What does the gap indicate?","opts":["The trace data is corrupted — spans should always sum to the total duration","Uninstrumented time — there are gaps where code runs but no span is active. This could be serialization, connection wait time, middleware, or code paths without OTEL instrumentation","This is normal — traces always have a 1-2 second baseline overhead","The 1.2 second gap is network latency between services","Cloud Trace rounds span durations — this is a display artifact"],"ans":1,"fb":"In a well-instrumented trace, spans should cover nearly all the elapsed time. A large gap between total trace duration and sum of spans indicates uninstrumented code sections. Common causes: a library that isn't auto-instrumented, a thread handoff without context propagation, or Kafka consumer time where no span is active. Add manual spans to the gap sections to diagnose where the time is going."},
    {"level":2,"diff":2,"scenario":3,"q":"Service A calls Service B but Cloud Trace shows them as separate unconnected traces. What is the most likely cause?","opts":["Cloud Trace does not support multi-service tracing — each service has its own trace","Service A is not propagating the trace context header (W3C traceparent or B3) in its outgoing HTTP request. Check if the HTTP client is auto-instrumented by the OTEL agent or if context propagation is manually configured","Service B is not configured to accept trace context headers","Trace IDs are not compatible between Spring Boot 3 services","The OTEL collector is dropping spans from one of the services"],"ans":1,"fb":"Distributed trace correlation requires the caller to propagate the `traceparent` header to the callee. The OTEL Java agent auto-instruments common HTTP clients (RestTemplate, WebClient, OkHttp) to inject these headers. If using a custom HTTP client or a non-instrumented library, propagation must be done manually via `W3CTraceContextPropagator`. Check the outgoing request headers from Service A."},
    {"level":2,"diff":3,"scenario":3,"q":"A trace shows a database span taking 1.8 seconds. The SQL query is `SELECT * FROM payments WHERE tenant_id = ?`. How do you determine whether this is a query plan problem or a connection wait problem?","opts":["1.8 seconds is always a query problem — add an index on tenant_id immediately","Check EXPLAIN ANALYZE in PostgreSQL for query plan, and check HikariCP metrics: `hikaricp_connections_pending` and `hikaricp_connections_acquire_seconds`. If connection wait is high, the problem is pool saturation, not the query plan","Database spans always include connection wait time — there is no way to separate them","1.8 seconds for a filtered query means the database is overloaded — scale up Cloud SQL","Connection wait is always negligible — the issue must be the query"],"ans":1,"fb":"A database span includes time from connection acquisition through query execution. If HikariCP is saturated, most of the 1.8s could be waiting for a connection, not executing the query. Check: (1) HikariCP metrics `hikaricp_connections_pending` and `hikaricp_connections_acquire_seconds` in Grafana, (2) EXPLAIN ANALYZE for the actual query execution time. If connection wait is high, increase pool size or reduce concurrent load."},
    {"level":2,"diff":1,"scenario":4,"q":"A Cloud Monitoring alert is firing every 5 minutes and resolving immediately. Engineers are ignoring it. What is the most likely issue?","opts":["The service has a real intermittent failure that engineers should not ignore","The alert threshold is too sensitive — the metric briefly crosses the threshold each time. Set a longer evaluation window (e.g., 'sustained for 5 minutes') to avoid flapping alerts","Cloud Monitoring has a bug with recurring alerts — raise a GCP support ticket","The alert is correct — the service needs to be restarted every 5 minutes","Cloud Monitoring alert policies cannot be tuned — replace with a Grafana alert"],"ans":1,"fb":"A flapping alert that fires and immediately resolves is a sign of threshold misconfiguration. If engineers ignore it, it loses all value as a signal. Fix: add a duration condition ('condition must be true for at least 5 minutes') and ensure the threshold is based on a meaningful signal level. Alert policies should be tuned so they only fire when action is genuinely required."},
    {"level":2,"diff":2,"scenario":4,"q":"You are defining an alert for the payment API. The SLO is 99.9% availability. What alert condition best detects an SLO breach in progress?","opts":["Alert when any 5xx error occurs","Alert when error rate > 0.1% sustained for 5 minutes — this corresponds to the SLO boundary and gives a brief window to confirm the issue before paging","Alert when error rate > 50% — alert only on severe outages","Alert when response time > 1 second for any request","Alert when CPU > 80% on any payment service pod"],"ans":1,"fb":"An SLO of 99.9% means up to 0.1% errors are acceptable. The alert threshold should match: error rate > 0.1% sustained for 5 minutes means the SLO is being consumed. A 5-minute window prevents false positives from brief spikes. Alerting on any 5xx or on CPU couples the alert to implementation details rather than user impact."},
    {"level":2,"diff":2,"scenario":4,"q":"An alert's runbook link points to a Confluence page that no longer exists (404). What should you do when you notice this?","opts":["Ignore it — the alert will still fire and engineers can investigate without the runbook","Fix the runbook link in the alert policy immediately. A broken runbook link during an incident at 03:00 costs critical minutes. Alert hygiene includes keeping runbook links current","Disable the alert until the runbook is rewritten","Change the alert to include the runbook content inline as a description field","Broken links are expected — engineers should use Google to find the runbook"],"ans":1,"fb":"A broken runbook link during an incident is an operational hazard. Alert maintenance includes: verifying runbook links are valid, ensuring runbooks are up to date, and testing that on-call engineers can find and follow them. Fix immediately — update the alert policy's runbook URL and either restore the Confluence page or redirect to the new location."},
    {"level":2,"diff":3,"scenario":4,"q":"You are setting alert thresholds for a new service with no historical data. What is the best approach to set initial thresholds?","opts":["Set all thresholds to very low values — it's better to over-alert and tune later","Deploy without alerts for the first two weeks to gather baseline data, then set thresholds based on p95/p99 of the baseline. Set initial alerts conservatively (e.g., 5x the baseline error rate) and refine over time","Copy thresholds from an existing similar service without modification","Ask the product team what thresholds feel right","Set thresholds at 100% error rate — only alert on complete outages initially"],"ans":1,"fb":"Without baseline data, thresholds are guesses. The pattern: (1) deploy with loose thresholds or no alerts except for total outage, (2) observe the metric pattern for 1-2 weeks to understand normal variance, (3) set thresholds at 3-5x the observed normal level, (4) add duration conditions to prevent flapping. SLO-based alerting (error budget burn rate) is more reliable than absolute thresholds when you know your SLO."},
    {"level":3,"diff":1,"scenario":0,"q":"A trace shows Service A → Service B taking 800ms. Service B's own spans show only 50ms of work. Where is the remaining 750ms?","opts":["750ms is network latency between services in GKE — this is expected","The time is between spans: connection setup, request queuing, or time not covered by instrumentation. Check for a gap between Service A's outgoing span and Service B's incoming span in the trace waterfall","Service B has a memory leak causing the slowdown","The OTEL agent is adding 750ms of overhead to every call","Cloud Trace is incorrectly attributing time — the data is unreliable"],"ans":1,"fb":"When total span duration exceeds the sum of child spans, there is uninstrumented time. In a service-to-service call, the gap between the caller's outgoing span and the callee's incoming span may include: thread pool queue time, serialization, connection establishment, or GKE ingress processing. Add spans to the uninstrumented sections or check if the HTTP client span is capturing connection time correctly."},
    {"level":3,"diff":1,"scenario":0,"q":"Traces show high latency for a specific tenant but aggregate metrics for the service look normal. What does this tell you?","opts":["The trace data is unreliable — aggregate metrics are the ground truth","The aggregate metrics are masking a per-tenant issue. The specific tenant may have unusually large data volumes or a missing index. Trace-level investigation with per-tenant filtering is needed","High latency for one tenant always indicates a noisy neighbour pod issue","The tenant's client has network problems — not a service issue","This pattern means the tenant is making incorrect API calls"],"ans":1,"fb":"Aggregate metrics average across all tenants — a single slow tenant is diluted. Distributed traces allow filtering by tenantId (if the span includes it as an attribute) to isolate per-tenant performance. Common causes: the tenant has more data than others causing slower queries, a missing index for their query patterns, or a Kafka partition where they have disproportionate message volume."},
    {"level":3,"diff":2,"scenario":0,"q":"A Grafana p99 latency panel shows spikes every 15 minutes for exactly 2 minutes. A trace during a spike shows a database span jumping to 1.8 seconds. What is the most likely cause?","opts":["A deployment occurs every 15 minutes causing pod restarts","Autovacuum or a scheduled batch job is running every 15 minutes, causing table lock contention or high I/O on Cloud SQL. Correlate the spike timing with `pg_stat_activity` and Cloud SQL I/O metrics","A garbage collection pause is causing the 2-minute slowdown every 15 minutes","The Kafka consumer is rebalancing every 15 minutes","A Cloud Monitoring alert is throttling requests every 15 minutes"],"ans":1,"fb":"Regular periodic latency spikes often indicate a scheduled process. 15-minute intervals suggest: autovacuum running on a large table, a scheduled Spring job (@Scheduled), or a periodic batch query. The database span jump confirms the I/O is happening in PostgreSQL. Check `pg_stat_activity` and Cloud SQL I/O metrics (disk read/write bytes) during the spike window."},
    {"level":3,"diff":2,"scenario":0,"q":"Traces show a Kafka consumer processing each message in 5ms, but p99 end-to-end latency from produce to consume is 45 seconds. Where is the time being spent?","opts":["5ms per message is too slow — optimize the consumer logic first","The message sits in the Kafka partition for ~45 seconds before the consumer processes it. Check consumer group lag metrics and Kafka topic partition count — the consumer may be under-scaled relative to producer throughput","Kafka has a known 45-second overhead for each message — this is expected","The consumer is batching messages in 45-second windows","The 45-second gap is caused by Spring Boot serialization overhead"],"ans":1,"fb":"When trace spans show fast processing but end-to-end latency is high, the time is in the queue — the message waits in Kafka before being consumed. Monitor `kafka_consumer_group_lag` in Grafana: if lag grows, consumers can't keep up with producers. Solutions: increase consumer parallelism (more partitions + consumers), optimize consumer processing, or check for consumer rebalancing causing processing gaps."},
    {"level":3,"diff":3,"scenario":0,"q":"A service occasionally shows 3-second latency spikes. Traces during spikes show a 3-second gap before the first database span with no other spans in between. What is the most likely cause?","opts":["A 3-second gap means the network is congested — check GKE node bandwidth utilization","A gap before the first database span suggests HikariCP connection acquisition time — the pool is exhausted and the request waits 3 seconds for a connection. Confirm with `hikaricp_connections_pending` and `hikaricp_connections_acquire_seconds_max` in Grafana","The Spring Boot startup is slow — the first request always takes 3 seconds","A 3-second gap is caused by OTEL span sampling — every 100th trace has artificial delay","The Cloud SQL Auth Proxy is rate-limiting connections to 3 seconds per request"],"ans":1,"fb":"A gap before the first span in a request (especially before the first DB call) is classic HikariCP pool exhaustion — the request thread blocks waiting for a connection to be returned to the pool. Evidence: `hikaricp_connections_pending > 0` and `hikaricp_connections_acquire_seconds_max` approaching your observed 3-second gap. Fix: increase maximumPoolSize, reduce connection hold time, or reduce concurrent requests."},
    {"level":3,"diff":1,"scenario":1,"q":"A GKE node is showing `NotReady` status. What is the expected GKE behavior for pods on that node?","opts":["GKE terminates all pods immediately and they are lost permanently","GKE marks the node NotReady and starts evicting pods after the pod eviction timeout (default 5 minutes). Pods with a deployment are rescheduled to healthy nodes. Pods without a controller (bare pods) are not rescheduled","GKE automatically replaces the node and reschedules pods within 30 seconds","All pods continue running — only new pod scheduling is affected","GKE requires manual intervention to reschedule pods from a NotReady node"],"ans":1,"fb":"When a node goes NotReady, the node controller waits for the pod eviction timeout (default 5 minutes) before marking pods as Failed and rescheduling them. Deployment-managed pods get rescheduled to available nodes. The 5-minute delay means services lose those pod replicas during that window. GKE Node Auto Provisioning may create a new node if capacity is insufficient."},
    {"level":3,"diff":1,"scenario":1,"q":"You run `kubectl top node` and see a node at 95% memory utilization. Pods are being OOMKilled on that node. What is the cause?","opts":["The node's memory is undersized for the cluster — upgrade the node pool","Pods on the node collectively exceed the node's available memory. This happens when pod memory limits are set much higher than requests, allowing over-scheduling. When actual usage exceeds physical memory, the OOM killer evicts processes — usually pods that exceed their limits","OOMKill on a node means the node itself has a memory leak — replace the node","95% node memory means GKE will auto-scale — wait for the new node","Pod OOMKills are independent of node memory — this is a JVM heap configuration issue"],"ans":1,"fb":"Node-level OOM happens when the sum of actual memory usage by all pods exceeds physical memory. This often occurs when limits >> requests — the scheduler allows over-commitment based on requests, but actual usage can exceed physical capacity. Immediate action: cordon the node to stop new pod scheduling, check which pods are consuming the most memory, and fix resource configurations."},
    {"level":3,"diff":2,"scenario":1,"q":"kubectl describe node shows: `Conditions: MemoryPressure=True`. What actions does Kubernetes take?","opts":["Kubernetes immediately terminates all pods on the node","Kubernetes stops scheduling new pods to the node and may evict pods based on QoS class: BestEffort first, then Burstable, then Guaranteed","Memory pressure is advisory only — no automatic actions are taken","Kubernetes doubles the node's memory limit from the cloud provider","Memory pressure triggers an immediate node replacement in GKE Autopilot"],"ans":1,"fb":"When kubelet detects memory pressure (available memory below eviction threshold), it: (1) taints the node to prevent new pod scheduling, (2) evicts pods in QoS order — BestEffort (no requests/limits) first, then Burstable (requests != limits), then Guaranteed (requests == limits). This is why setting proper requests and limits affects eviction priority. Set requests = limits for critical services to get Guaranteed QoS."},
    {"level":3,"diff":2,"scenario":1,"q":"You suspect CPU throttling is causing latency on a GKE service. Which metric confirms this?","opts":["container_cpu_usage_seconds_total — high value confirms throttling","rate(container_cpu_cfs_throttled_seconds_total[5m]) — this shows the rate of CPU throttling. If consistently non-zero, CPU throttling is occurring","node_cpu_utilization — node-level CPU shows container throttling","container_cpu_system_seconds_total — high system CPU indicates throttling","kubectl top pod — CPU shown in millicores confirms throttling if above the limit"],"ans":1,"fb":"`container_cpu_cfs_throttled_seconds_total` measures time the container was throttled by the Linux CFS scheduler. A non-zero rate means the CPU limit is being hit and the container is being artificially slowed. `kubectl top pod` shows instantaneous usage, not throttling. Use `rate(container_cpu_cfs_throttled_periods_total[5m]) / rate(container_cpu_cfs_periods_total[5m])` for the throttling percentage."},
    {"level":3,"diff":3,"scenario":1,"q":"A GKE node has 16 cores. Total pod CPU requests are 15 cores. One pod has a CPU limit of 12 cores and uses 2 cores at rest but needs 8 cores under load. Will this pod be throttled under load?","opts":["Yes — 8 cores under load triggers CFS throttling","No — the pod uses 8 cores which is below its 12-core limit. CFS throttling only occurs if actual usage exceeds the CPU limit. At 8 cores, the pod is within its limit. However, if all pods burst simultaneously, the node may become overloaded since total limits can exceed physical capacity","Throttling occurs whenever total requests exceed physical cores","The pod is limited to 1 core because requests from other pods reserve capacity","CPU limits work differently from memory limits — the pod can never exceed 2 cores under load"],"ans":1,"fb":"CPU limits are enforced per container using CFS bandwidth control. A pod using 8 cores with a 12-core limit is not throttled — it's well within the limit. The risk: if other pods also burst, total CPU demand may exceed 16 physical cores. Unlike memory, CPU over-commitment doesn't cause crashes — it causes throttling across all pods sharing the node."},
    {"level":3,"diff":1,"scenario":2,"q":"OpenTelemetry is configured with `always_on` sampling in production. The service processes 10,000 requests/minute. What is the impact?","opts":["No impact — OpenTelemetry is designed to handle any trace volume","100% sampling generates 10,000 traces/minute, creating high overhead on the OTEL collector, high Cloud Trace ingestion costs, and per-request instrumentation overhead. Use probabilistic or rate-limited sampling for high-volume production services","always_on sampling only affects metrics, not traces","10,000 traces/minute is below the Cloud Trace free tier — no cost impact","always_on sampling compresses traces automatically — no storage or cost impact"],"ans":1,"fb":"`always_on` (100% sampling) is appropriate only for low-volume services or debugging. At 10,000 req/min: Cloud Trace charges per span ingested, the OTEL collector must process every trace, and each request has instrumentation overhead. Use `parentbased_traceidratio` sampler with a ratio (e.g. 0.1 = 10%) or a fixed rate limit (e.g. 100 traces/minute) to balance observability with cost."},
    {"level":3,"diff":2,"scenario":2,"q":"An OTEL collector is dropping spans under load. Increasing memory helps temporarily but recurs. What is the root cause and sustainable fix?","opts":["The collector needs to be replaced — OTEL collectors are not designed for high volume","The collector is receiving more spans than it can export to the backend. Increasing memory only shifts the bottleneck. Fix: reduce sampling rate at source, add more collector replicas, or use tail-based sampling to keep error/slow traces and drop successful ones","Spans are being dropped due to Cloud Trace API rate limits — file a quota increase request","Drop the OTEL collector and export directly from services to Cloud Trace","Span drops are expected — increase the collector memory limit to 8GB"],"ans":1,"fb":"Collector span drops indicate the pipeline can't keep up: more spans arrive than can be exported. Memory helps with bursts but not sustained load. Sustainable fixes: (1) reduce ingestion at source (lower sampling ratio), (2) scale collectors horizontally, (3) tail-based sampling keeps high-value traces (errors, slow) and drops routine successful ones — this is the most impactful approach for maintaining observability while controlling costs."},
    {"level":3,"diff":2,"scenario":2,"q":"A service is emitting a span attribute `http.request.body` containing the full JSON request body. What is the problem?","opts":["Request body attributes help debug issues — this is a good observability practice","Span attributes are stored in trace backends and may contain PII or sensitive data. Large attributes also increase trace ingestion costs. Remove sensitive attributes using an OTEL collector attribute processor: `actions: [{key: http.request.body, action: delete}]`","The problem is attribute name convention — use `request.body` not `http.request.body`","Full request bodies in spans help debugging — only remove in PCI-DSS environments","This is only a problem if the span is sampled — with low sampling it's acceptable"],"ans":1,"fb":"Span attributes in distributed trace backends are stored and potentially visible to anyone with trace access. Sensitive data (PII, payment details, auth tokens) in spans creates: (1) compliance risk (GDPR, PCI-DSS), (2) cost increase (larger spans = higher ingestion cost), (3) security risk if trace backend access is broad. Filter at the OTEL collector using attribute processors to remove or redact sensitive fields before export."},
    {"level":3,"diff":3,"scenario":2,"q":"Head-based sampling at 10% missed an important error trace for a rare payment failure. How do you ensure future error traces are always captured?","opts":["Switch to always_on sampling — there is no way to guarantee error traces with probabilistic sampling","Implement tail-based sampling: the OTEL collector buffers complete traces and makes sampling decisions after the trace is complete. Configure the tail sampler to always retain traces containing error spans (status_code=ERROR) while sampling successful traces at 10%","Increase sampling to 100% — the cost is acceptable if you're missing error traces","Add a custom log statement in all error handlers as a fallback — accept that traces will be lost","Use Cloud Trace's built-in error trace retention — it automatically captures all error traces"],"ans":1,"fb":"Head-based sampling decides at trace start — you can't know if a trace will contain an error. Tail-based sampling (via the OTEL Collector's `tailsampling` processor) buffers complete traces and decides after completion. Rules like `type: status_code, status_codes: [ERROR]` ensure all error traces are always kept, while routine successful traces are sampled at the desired rate. This is the correct pattern for maintaining error observability while controlling costs."},
    {"level":3,"diff":1,"scenario":3,"q":"What are the four 'golden signals' for monitoring a service?","opts":["CPU, memory, disk, network","Latency, traffic, errors, saturation — these are the core signals that define service health from the user's perspective","Response time, throughput, availability, correctness","p99 latency, p95 latency, p50 latency, error rate","Request rate, error count, pod count, CPU utilization"],"ans":1,"fb":"The four golden signals (from Google SRE Book): (1) Latency — how long requests take, (2) Traffic — how many requests, (3) Errors — how many requests fail, (4) Saturation — how full the service is (queue depth, CPU throttling, connection pool). These four metrics can diagnose most service health issues and form the basis of good dashboards and SLO definitions."},
    {"level":3,"diff":2,"scenario":3,"q":"You are designing a Grafana dashboard for the payment service. A product manager asks to add a panel showing 'total payments processed today'. Why is this a good addition?","opts":["It is not a good addition — dashboards should only contain technical metrics","Business metrics like total payments belong on a separate business dashboard, not the golden-signals dashboard","A payment count panel bridges technical and business context. During an incident, it immediately shows whether the degradation is affecting actual business transactions, helping prioritize severity and communicate impact to stakeholders","Total payments is a sum metric — it cannot be displayed in Grafana","Business metrics require separate permissions — they cannot be on the same dashboard as technical metrics"],"ans":2,"fb":"Golden-signals dashboards are most powerful when they include a business health panel alongside technical signals. During a 5% error rate incident, 'total payments processed today vs yesterday' immediately quantifies business impact. This guides prioritization (0 payments = P1, normal payment volume = lower priority) and provides stakeholder communication data."},
    {"level":3,"diff":2,"scenario":3,"q":"A golden-signals dashboard shows error rate, latency p99, and throughput. What is missing to complete the four golden signals?","opts":["A panel showing the number of pods running","A saturation panel — examples: HikariCP connection pool utilization, Kafka consumer lag, or CPU throttling ratio. Saturation predicts upcoming degradation before error rate rises","An uptime panel showing percentage availability since last deployment","A panel comparing current metrics to the previous week's baseline","A panel showing the number of active database connections"],"ans":1,"fb":"Saturation is the fourth golden signal — how close the service is to capacity. Before errors appear, saturation signals impending problems: connection pool at 95%, Kafka lag growing, CPU throttling rate rising. A good saturation panel for a payment service: HikariCP `hikaricp_connections_pending` or `hikaricp_connections_usage` percentage, and Kafka consumer lag. These predict degradation 5-10 minutes before error rate spikes."},
    {"level":3,"diff":3,"scenario":3,"q":"The payment service golden-signals dashboard looks healthy but tenant support is reporting failures. What dashboard panel would surface this discrepancy?","opts":["Add a p99.9 latency panel — the 99th percentile is masking failures at the tail","Add per-tenant error rate panels using tenant_id as a dashboard variable. Aggregate metrics hide per-tenant issues — a 99.5% success rate globally masks one tenant at 0% success","The support reports are wrong — dashboard metrics are the ground truth","Add a panel showing the total number of unique tenants making requests","The discrepancy means the instrumentation is broken — fix metrics before investigating"],"ans":1,"fb":"Aggregate dashboards are blind to individual customer impact. One tenant at 0% success is invisible in a platform-wide 99.8% success rate. The fix: add a Grafana variable for tenantId (or use a top-N panel showing the worst-performing tenants). During incidents, this immediately identifies who is affected and helps validate when resolution is complete."},
    {"level":3,"diff":1,"scenario":4,"q":"You need to increase the Cloud SQL instance tier by editing a Terraform module. Where do you make this change?","opts":["Directly in the GCP Console — Terraform modules cannot change Cloud SQL tier","In the `google_sql_database_instance` resource block's `settings.tier` attribute. Run `terraform plan` first to confirm only the tier change is shown before applying","Change the tier in Cloud SQL directly and then import the state into Terraform","Run `gcloud sql instances patch` to change the tier and update Terraform to match afterward","Terraform does not support Cloud SQL tier changes — use a database migration script"],"ans":1,"fb":"Terraform manages Cloud SQL via the `google_sql_database_instance` resource. The `settings.tier` attribute sets the instance machine type. Always run `terraform plan` before `terraform apply` to verify the plan only shows the intended change — a tier change may trigger a brief instance restart. Do not change Cloud SQL outside Terraform (console, gcloud) when IaC manages it, as this creates state drift."},
    {"level":3,"diff":2,"scenario":4,"q":"A teammate ran `terraform apply` in production directly, bypassing the PR review process. What is the organizational risk?","opts":["No risk — terraform apply is safe since it shows a plan first","Direct terraform apply bypasses peer review, meaning changes are not audited before applying. This risks unreviewed changes to production infrastructure, no rollback artifact, and CI/CD policy controls being bypassed. Correct process: all Terraform changes via PR with plan output reviewed, applied via CI/CD with approvals","The risk is only that git history will be incorrect — the infrastructure itself is fine","terraform apply in production is fine if done during business hours","The risk is that the Terraform state file becomes corrupted"],"ans":1,"fb":"Direct `terraform apply` to production is an anti-pattern in IaC workflows. The risks: no peer review catches mistakes before they hit production, changes aren't documented in PR history, and CI/CD policy controls are bypassed. Best practice: Terraform changes via PR → plan output posted as PR comment → approval → CI/CD pipeline applies. Protect production environments with Terraform Cloud workspaces or custom CI gates."},
    {"level":3,"diff":3,"scenario":4,"q":"You are reviewing a Terraform module change that adds Cloud SQL flag `cloudsql.enable_pgaudit=on`. What operational implications should you flag?","opts":["No implications — Cloud SQL flags are applied without any service impact","pgaudit enables PostgreSQL audit logging, generating very high log volume (potentially 10-100x normal), significantly increasing Cloud Logging costs. Also verify whether enabling this flag requires a Cloud SQL instance restart, and confirm whether pgaudit is required for compliance (PCI-DSS/SOX) or being added speculatively","pgaudit is a read-only flag change — apply without review","The only implication is more data available for debugging — no cost or operational impact","pgaudit requires a new Cloud SQL instance — the existing instance cannot be modified"],"ans":1,"fb":"pgaudit (PostgreSQL audit extension) generates a log line for every SQL statement. On a busy production database, this means millions of log lines per hour, dramatically increasing Cloud Logging ingestion costs. Review checklist: (1) Is this required for compliance (if yes, proceed with cost awareness), (2) what is the estimated log volume increase, (3) does enabling this flag require an instance restart, (4) is a log exclusion filter needed to prevent full audit logs reaching standard storage."},
    {"level":4,"diff":1,"scenario":0,"q":"What is the difference between an SLI and an SLO?","opts":["SLI is the target percentage; SLO is the measured value","SLI is the metric measuring a specific aspect of service quality (e.g. request success rate); SLO is the target value for that metric (e.g. success rate ≥ 99.9%)","SLI and SLO are interchangeable terms for service reliability targets","SLI is defined by the team; SLO is set by the customer","SLO is measured by Cloud Monitoring; SLI is defined in Grafana"],"ans":1,"fb":"SLI (Service Level Indicator) is the measurement — e.g., the ratio of successful requests to total requests. SLO (Service Level Objective) is the target — e.g., SLI ≥ 99.9% over a rolling 28-day window. The SLO defines the threshold at which the SLI is acceptable. An SLA (Service Level Agreement) is the external commitment with consequences if the SLO is missed."},
    {"level":4,"diff":2,"scenario":0,"q":"The payment service has a 99.9% availability SLO over 28 days. In the last 7 days, error rate was 0.5%. How much error budget has been consumed this week?","opts":["0.5% of the total error budget","The weekly error budget is 0.1% × 7/28 = 0.025%. Actual errors were 0.5% — consuming 20× the weekly budget. This week alone has used more than the entire 28-day budget","Error budget is calculated monthly — weekly data is not relevant","0.5% error rate means 99.5% availability, which is below the SLO — the budget is 100% consumed","The error budget is reset weekly — this week's consumption does not affect the 28-day window"],"ans":1,"fb":"28-day error budget = 0.1% × 28 days = total allowable errors. Weekly allocation = 0.1% × (7/28) = 0.025%. At 0.5% actual errors, the week consumed 0.5/0.025 = 20× the weekly allocation. This means the SLO is being severely violated and the 28-day budget is already exhausted. Action: stop non-critical feature deployments and focus on reliability improvements."},
    {"level":4,"diff":3,"scenario":0,"q":"The error budget for a payment service is 80% consumed with 12 days left in the 28-day window. The team wants to deploy a risky database migration. What is the correct decision framework?","opts":["Deploy immediately — 20% budget remains and that's enough headroom","Evaluate deployment risk against remaining budget: 20% budget over 12 days means ~48 minutes of allowable downtime. If the risky migration has a >10% chance of causing >30 minutes of downtime, the expected budget consumption exceeds what remains. Consider: delay to next window, reduce migration risk (blue-green, incremental), or accept the risk with a rollback plan and stakeholder sign-off","Never deploy when error budget is below 100% — wait for the window to reset","Error budget only applies to infrastructure changes — database migrations are exempt","Deploy at off-peak hours — time of day determines whether budget is consumed"],"ans":1,"fb":"Error budget decision framework: quantify deployment risk (probability × expected downtime) against remaining budget. With 80% consumed and 12 days left, you have ~20% of total budget remaining. A risky migration that has a meaningful chance of consuming more than the remaining budget requires: (1) risk reduction (incremental rollout, feature flag), (2) delay to next window, or (3) explicit stakeholder decision to accept the risk."},
    {"level":4,"diff":1,"scenario":1,"q":"A Cloud SQL instance is provisioned with 8 vCPUs but consistently uses only 10% CPU. What is the cost implication and recommended action?","opts":["CPU usage percentage does not affect Cloud SQL cost — only storage costs vary","Cloud SQL instances are billed by instance tier (vCPU count and memory), not by actual usage. Running an 8-vCPU instance at 10% wastes ~90% of the instance cost. Downsize to a 2-vCPU instance to reduce costs by ~75% — verify performance in staging first","Downsize during off-peak hours — Cloud SQL billing adjusts dynamically with usage","CPU underutilization is expected — Cloud SQL needs headroom for burst capacity","Cloud SQL offers committed use discounts that make low utilization cost-neutral"],"ans":1,"fb":"Cloud SQL is billed by instance tier (machine type), not by actual CPU/memory utilization. An 8-vCPU db-n1-standard-8 costs 4× more than a 2-vCPU db-n1-standard-2 regardless of whether you use 10% or 100% of CPU. Right-sizing is the highest-impact Cloud SQL cost optimization. Check p99 CPU usage over the last 30 days before downsizing — leave headroom for bursts and maintenance operations."},
    {"level":4,"diff":2,"scenario":1,"q":"GCP billing shows $8,000/month on Cloud Logging. The team logs at DEBUG level in production across 20 services. What concrete actions reduce this cost?","opts":["Contact GCP to negotiate a volume discount for Cloud Logging","Set production log level to WARN/ERROR (reducing ~80-90% of log volume), create Cloud Logging exclusion filters for high-volume health check logs, and use log sampling for DEBUG-level traces. Cloud Logging charges per GB ingested — reducing volume directly reduces cost","Move all services to a single logging pipeline to reduce per-service overhead","Cloud Logging costs are fixed — you cannot reduce them through configuration","Enable Cloud Logging compression — this reduces cost without reducing log volume"],"ans":1,"fb":"Cloud Logging charges $0.50/GB ingested beyond the free tier. DEBUG logs are the highest-volume category. Actions: (1) set production log level to WARN — this alone can reduce volume 80-90%, (2) add exclusion filters for health check access logs which fire every 10 seconds per pod, (3) route non-essential logs to a cheaper storage (GCS) instead of Cloud Logging. These three actions typically reduce Cloud Logging costs by 70-90%."},
    {"level":4,"diff":3,"scenario":1,"q":"GCP billing shows $45,000/month for GKE. A colleague suggests switching to Autopilot to reduce costs. When is this advice correct and when is it wrong?","opts":["GKE Autopilot always costs less than Standard mode — switch immediately","GKE Standard charges for nodes whether used or not; Autopilot charges per pod resource request while running. Autopilot is cheaper for bursty workloads with good resource requests. Autopilot is more expensive for consistently high-utilization workloads. Analyze node utilization: if average >60%, Standard may be cheaper; if <40%, Autopilot likely saves money","Autopilot eliminates all GKE costs — it is always the cheaper option","GKE Autopilot does not support Spring Boot services — Standard mode is required","Switch to Autopilot only if the team has fewer than 5 services"],"ans":1,"fb":"The cost comparison depends on workload patterns. GKE Standard: you pay for nodes 24/7 regardless of pod utilization. GKE Autopilot: you pay per-pod per-second based on resource requests — efficient for variable workloads, expensive if pods run 24/7 at high resource requests. To decide: calculate Standard cost (nodes × hours × price) vs Autopilot cost (sum of pod CPU/memory requests × hours × per-unit price). The break-even is around 60-70% node utilization in most regions."},
    {"level":4,"diff":1,"scenario":2,"q":"What is a rolling deployment and why does it prevent downtime?","opts":["A rolling deployment deploys to all pods simultaneously, then switches traffic","A rolling deployment replaces pods incrementally — new pods start and pass health checks before old pods are terminated. At each step, a mix of old and new versions handles traffic. If new pods fail health checks, the rollout pauses","Rolling deployment requires a feature flag to switch between old and new versions","A rolling deployment is the same as a blue-green deployment","Rolling deployments always cause 2-3 minutes of downtime during pod restarts"],"ans":1,"fb":"Rolling deployments maintain availability by incrementally replacing old pods with new ones. Kubernetes waits for the new pod to pass readiness probes before terminating an old pod (controlled by `maxSurge` and `maxUnavailable` settings). If a new pod fails to become ready, the rollout pauses — preventing a broken version from replacing all pods. This is the default Kubernetes deployment strategy and is zero-downtime when readiness probes are correctly configured."},
    {"level":4,"diff":2,"scenario":2,"q":"You deploy a new version of the payment service. Five minutes after deployment, error rate rises from 0.1% to 3%. What deployment observability checkpoint would have caught this earlier?","opts":["There is no way to detect a bad deployment in under 5 minutes","A canary or staged rollout with automated rollback: deploy to 10% of pods first, monitor error rate and latency for 5 minutes before proceeding. An automated rollback trigger on error rate > 0.5% would have caught this during the canary phase before full rollout","Deploy only during business hours when engineers are available to detect issues","Use kubectl rollout undo immediately — detection time is irrelevant","Set the deployment strategy to Recreate — this makes issues immediately visible"],"ans":1,"fb":"Canary deployments are the key observability checkpoint. Deploy to 10-20% of pods, hold for 5-10 minutes while monitoring error rate and latency. Automated rollback triggers on SLI degradation allow rollbacks before full rollout. In Kubernetes, implement this with Argo Rollouts or Flagger: configure the analysis template to check error rate and latency, and automatically pause or rollback if thresholds are crossed during the canary phase."},
    {"level":4,"diff":3,"scenario":2,"q":"The payment service has a Liquibase migration that renames a column. The old and new code use different column names. How do you deploy this without downtime?","opts":["Deploy code and migration simultaneously during a maintenance window with brief downtime","Use the expand-contract pattern: Phase 1 - add the new column and copy data, update code to write to both columns (backward compatible). Phase 2 - migrate all reads to the new column. Phase 3 - remove writes to old column. Phase 4 - drop the old column. Each phase is a separate deployment","Rename the column directly — Liquibase migrations are zero-downtime by default","Use a feature flag to switch between old and new column names at runtime","Deploy the migration with a rollback script — accept brief downtime as unavoidable for schema changes"],"ans":1,"fb":"Column rename with rolling deployment requires expand-contract: (1) add new column + copy data (both old and new code work), (2) update code to write both columns and read new column (deploy via rolling update — backward compatible), (3) stop writing old column in a subsequent deploy, (4) drop old column after all replicas are updated. Each phase is a separate PR/deployment. Directly renaming a column breaks the old code version still running during a rolling deployment."},
    {"level":4,"diff":2,"scenario":3,"q":"You are defining the standard OTEL Java agent configuration for all Spring Boot services on GKE. Which configuration properties are essential to include in the platform baseline?","opts":["Only otel.service.name — all other settings have sensible defaults","otel.service.name, otel.exporter.otlp.endpoint (pointing to the shared collector), otel.resource.attributes (including environment and version), and otel.traces.sampler with the agreed sampling configuration — these define identity, destination, context, and sampling policy","otel.traces.exporter=zipkin and otel.metrics.exporter=prometheus — these match the existing monitoring stack","Set otel.javaagent.enabled=false and instrument manually — auto-instrumentation creates too much overhead for production","otel.service.name and otel.traces.sampler=always_on — capture all traces in production"],"ans":1,"fb":"A platform baseline OTEL config must cover: (1) identity: otel.service.name (unique per service), (2) destination: otel.exporter.otlp.endpoint (the shared OTEL collector endpoint — do not let services export directly to backends), (3) context: otel.resource.attributes with environment, service.version, and team label, (4) sampling: otel.traces.sampler with the platform-agreed policy (not always_on in high-volume production). This ensures all services are discoverable in traces, export to the right place, and carry enough context for cross-service investigation.","context":{"Essential config":"service.name, exporter endpoint, resource attributes, sampler policy"}},
    {"level":4,"diff":2,"scenario":3,"q":"A team asks whether to use a pre-built Grafana dashboard JSON from Grafana.com versus building a custom one. What guidance should your observability guide give?","opts":["Always use community dashboards — they are battle-tested and require no maintenance","Always build custom dashboards — community dashboards are unreliable and poorly maintained","Use community dashboards as a starting point for infrastructure metrics (JVM, GKE node metrics) where metric names are standardized. Build custom dashboards for business flows and service-specific SLIs where metric names, labels, and business context are service-specific — the community dashboard will not know your service's business metrics","The choice does not matter — Grafana dashboards are interchangeable as long as the data source is configured","Use community dashboards for all services and suppress panels that are not applicable using dashboard permissions"],"ans":2,"fb":"Community dashboards work well for standardized metrics: JVM heap/GC (micrometer-jvm-extras uses standard names), Kubernetes node metrics (from node-exporter), and HikariCP metrics. These metric names are consistent across services. Business metrics — payment throughput, tenant-level error rates, Kafka consumer lag for specific topics — require custom dashboards because metric names, label values, and business context are service-specific. The observability guide should provide a template for the custom business dashboard with the standard structure (golden signals + service-specific panels).","context":{}},
    {"level":4,"diff":2,"scenario":3,"q":"You are writing the alerts section of the observability guide. A team asks whether to alert on CPU utilization. What is the correct guidance?","opts":["Yes — alert when CPU exceeds 80% for more than 5 minutes on any pod","No — CPU is not a user-facing signal. Alert on symptoms (error rate, latency) not causes. High CPU may or may not affect users. If CPU throttling is causing latency, the latency alert fires first. Add CPU as a dashboard metric for investigation, not as a primary alert","Alert on CPU only for database pods — application pods can tolerate high CPU without user impact","Alert when CPU exceeds 50% — this gives enough lead time before user impact","CPU alerts should fire at 95% with a 1-minute window to catch only the most severe cases"],"ans":1,"fb":"Alerting on CPU directly leads to alert fatigue — CPU spikes are common during GC, batch processing, or traffic bursts and often have no user impact. The SRE principle: alert on symptoms (what the user experiences) not causes (what the system is doing). Error rate and latency alerts are symptom-based — they fire when users are actually affected. CPU is a cause metric useful for investigation and dashboards. Exception: alert on CPU throttling specifically (container_cpu_cfs_throttled_seconds_total rate) if you know it correlates with latency in your service.","context":{}},
    {"level":4,"diff":3,"scenario":3,"q":"You are producing the Grafana dashboard standard for the platform. Teams must use dashboard-as-code (dashboard JSON stored in git). What operational problem does this solve and what tooling pattern enables it?","opts":["Dashboard-as-code prevents unauthorized dashboard edits — Grafana does not have role-based access control","Storing dashboard JSON in git enables version history, peer review of dashboard changes, automated deployment via CI/CD (e.g. Grafana provisioning or Terraform grafana_dashboard resource), and reproducible dashboard environments. It prevents dashboard drift where production Grafana differs from what teams expect and cannot be recreated after data loss","Dashboard-as-code is only necessary for dashboards shared across teams — individual team dashboards should be created in the UI","The main benefit is cost reduction — Grafana charges per dashboard when using managed Grafana (Grafana Cloud)","Dashboard-as-code enables Grafana to auto-generate dashboards from service definitions — no manual dashboard creation needed"],"ans":1,"fb":"Dashboard-as-code solves several operational problems: (1) auditability — who changed what and when, (2) reproducibility — dashboards can be recreated from git after accidental deletion or migration, (3) consistency — PR review ensures dashboards meet standards before production, (4) multi-environment — the same dashboard JSON parametrized by datasource variable deploys to staging and production. Implementation: store JSON in git, deploy via Grafana's provisioning (configmap mounted into Grafana pod), or use the Terraform grafana_dashboard resource in the IaC pipeline.","context":{}},
    {"level":4,"diff":3,"scenario":3,"q":"The observability guide needs to address how teams should instrument multi-step business transactions (e.g. a payment flow: validate → reserve → charge → notify). What is the recommended OpenTelemetry instrumentation pattern?","opts":["Emit one span per HTTP request — the service boundary spans are sufficient for tracing","Create a parent span for the overall transaction and child spans for each step (validate, reserve, charge, notify). Add business attributes to spans: tenant_id, payment_id, amount_cents, step_result. This allows the trace to show the full transaction flow, timing of each step, and which step failed — enabling both performance analysis and business debugging","Use log correlation instead of spans for business transactions — spans are for infrastructure, logs are for business events","Create metrics for each step (payment_validate_duration, payment_charge_duration) instead of spans — metrics are cheaper than traces at scale","Instrument only the slow steps — adding spans to all steps creates too much trace overhead"],"ans":1,"fb":"For multi-step business transactions, manual span creation with business context is the correct pattern. The OTEL Java API: `Span span = tracer.spanBuilder('payment.charge').startSpan(); span.setAttribute('payment.id', paymentId); span.setAttribute('tenant.id', tenantId)`. Child spans form a waterfall showing step timing. Business attributes (payment_id, amount, step outcome) make traces useful for both SRE debugging and business analysis. This is superior to metrics-only (which shows timing but not which specific payment failed) and logs-only (which cannot show the full flow waterfall).","context":{}},
    {"level":4,"diff":3,"scenario":3,"q":"An engineer asks why the observability guide recommends using recording rules in Prometheus for SLI calculations rather than computing them directly in Grafana dashboard queries. What is the correct reasoning?","opts":["Grafana cannot execute PromQL queries — it requires pre-computed metrics from recording rules","Recording rules pre-compute expensive PromQL expressions at the Prometheus server on a regular interval. SLI calculations (error budget burn rate, multi-window rate calculations) queried across 30-day windows are expensive — running them live in Grafana for every dashboard refresh adds query latency and Prometheus load. Recording rules also enable alerting on the same pre-computed values, ensuring alert and dashboard metrics are identical","Recording rules are required for SLI calculations — Prometheus does not support multi-window PromQL in ad-hoc queries","Recording rules automatically adjust SLO thresholds based on historical data — this is not possible with ad-hoc queries","Grafana's query caching handles this — recording rules and ad-hoc queries are equivalent in practice"],"ans":1,"fb":"Recording rules pre-compute expensive or frequently-used queries at a configured interval (e.g. every 30s) and store results as new metrics. Benefits for SLI/SLO: (1) performance — 30-day error budget calculations are expensive; pre-computing them prevents slow dashboard loads and Prometheus overload, (2) consistency — alerting rules and dashboard panels reference the same recording rule metric, eliminating discrepancies between what fires an alert and what the dashboard shows, (3) reusability — multiple dashboards and alerts reference one recording rule. This is why the Google SRE Workbook recommends recording rules for all SLO calculations.","context":{}},
    {"level":4,"diff":1,"scenario":4,"q":"A Cloud SQL maintenance window is configured for Sunday 02:00-06:00 UTC. What happens during a Cloud SQL maintenance event?","opts":["GCP applies patches to the Cloud SQL proxy running in your GKE cluster — the database itself is unaffected","GCP performs instance maintenance (OS patches, database minor version updates) which may cause a brief connection reset — typically 1-2 minutes of unavailability for non-HA instances, or a fast failover for HA instances","The Cloud SQL instance is completely unavailable for the entire 4-hour window","Maintenance only affects the standby instance — the primary remains fully available throughout","Cloud SQL maintenance is always zero-downtime — no connection interruption occurs"],"ans":1,"fb":"Cloud SQL maintenance involves OS patching and minor database version updates. For non-HA instances: a brief restart causes 1-2 minutes of unavailability — connections are dropped and must be retried. For HA instances: maintenance triggers a failover to the standby, causing a brief connection interruption (typically under 60 seconds) followed by automatic reconnection. Applications must handle transient connection failures gracefully via HikariCP connection validation and retry logic. The maintenance window defines when GCP is allowed to schedule this, not a guaranteed start time.","context":{"Maintenance window":"Sunday 02:00-06:00 UTC","HA instance downtime":"~60 seconds (failover)","Non-HA instance downtime":"1-2 minutes"}},
    {"level":4,"diff":1,"scenario":4,"q":"Your Cloud SQL instance has SSL certificates that expire in 30 days. What is the impact if the certificates are not rotated before expiry?","opts":["No impact — Cloud SQL automatically generates new certificates and updates all clients","The Cloud SQL Auth Proxy stops accepting connections — all database connections fail","Connections using SSL enforcement will fail once the server certificate expires. Applications using the Cloud SQL Auth Proxy are unaffected (proxy manages its own mTLS), but applications connecting directly with SSL mode=VERIFY_CA or VERIFY_FULL will fail","Only new connections fail — existing long-running connections remain valid until closed","SSL expiry triggers automatic Cloud SQL instance recreation — no manual action needed"],"ans":2,"fb":"Cloud SQL server SSL certificates have an expiry date. The Cloud SQL Auth Proxy manages its own certificate-based authentication independently — applications using the Auth Proxy are not affected by server SSL cert expiry. However, applications connecting directly to Cloud SQL with ssl_mode=VERIFY_CA or VERIFY_FULL will reject connections to a server presenting an expired certificate. The rotation procedure: use `gcloud sql ssl server-ca-certs create`, download new certificates, update client configurations, and verify connectivity before the old cert expires.","context":{"Certificate expiry":"30 days","Affected connections":"Direct SSL connections with VERIFY_CA/VERIFY_FULL","Unaffected":"Cloud SQL Auth Proxy connections"}},
    {"level":4,"diff":1,"scenario":4,"q":"After a Cloud SQL maintenance event, the application pods report HikariCP connection errors: 'Connection is not available, request timed out after 30000ms'. The Cloud SQL instance is healthy. What is the most likely cause?","opts":["The Cloud SQL instance requires a manual restart after maintenance — run `gcloud sql instances restart`","HikariCP's idle connections were dropped by Cloud SQL during the maintenance restart. HikariCP did not detect the stale connections and is attempting to use them. Enable `connection-test-query` or `keepalive-time` to validate connections before use","The maintenance event changed the Cloud SQL instance's IP address — update the JDBC URL","HikariCP pool size is too small for post-maintenance reconnection surge — increase maximumPoolSize","The Cloud SQL Auth Proxy needs to be restarted after every Cloud SQL maintenance event"],"ans":1,"fb":"During a Cloud SQL maintenance restart, the database server closes all existing connections. HikariCP pools idle connections — connections established before maintenance remain in the pool but are now dead (server-side closed). When the application tries to use a pooled connection, the attempt fails. HikariCP's connection validation settings prevent this: `connectionTestQuery=SELECT 1` validates connections before returning from the pool, `keepaliveTime=60000` proactively tests idle connections, and `idleTimeout` evicts connections that have been idle too long.","context":{"HikariCP error":"Connection is not available, request timed out after 30000ms","Cloud SQL status":"Healthy, restarted after maintenance","Root cause":"Stale pooled connections not detected by HikariCP"}},
    {"level":4,"diff":2,"scenario":4,"q":"You are planning a Cloud SQL minor version upgrade during a maintenance window. The production database has 150 active connections from 6 Spring Boot service deployments. What is the complete preparation checklist before the maintenance window?","opts":["Verify the new version is compatible and schedule the upgrade — Cloud SQL handles everything else","Verify application compatibility with the new version, confirm HikariCP is configured to retry connections (connectionTestQuery, initializationFailTimeout), test the upgrade in staging under load, prepare a rollback plan (create a pre-upgrade backup), notify stakeholders, monitor during the window with dashboards showing connection count and error rate","Drain all application connections before the upgrade by scaling deployments to 0 replicas, then upgrade, then scale back up","Only upgrade during business hours so the team is available — never do database maintenance at night","Run `gcloud sql instances patch` and monitor — Cloud SQL upgrades are always backward compatible and require no preparation"],"ans":1,"fb":"A Cloud SQL minor version upgrade requires full preparation: (1) compatibility testing in staging with the same application versions, (2) HikariCP reconnection validation (connectionTestQuery, keepaliveTime) to handle the brief connection reset, (3) pre-upgrade backup for rollback, (4) stakeholder notification (support teams, on-call), (5) dashboard preparation (connection count, error rate, HikariCP pool metrics), (6) rollback plan documentation. Scaling to 0 replicas is unnecessary if applications are configured for reconnection and the service has appropriate PDBs.","context":{"Active connections":"150","Services affected":"6 Spring Boot deployments"}},
    {"level":4,"diff":2,"scenario":4,"q":"A Cloud SQL HA instance failover occurs during peak hours due to a zone outage. What is the expected sequence of events for application connections, and what metric confirms the failover completed successfully?","opts":["Failover is transparent — no connections are dropped and applications experience no errors","The primary becomes unavailable; Cloud SQL promotes the standby within ~60 seconds; existing connections to the old primary are dropped and return connection errors; after promotion, new connections establish to the promoted instance at the same IP. Monitor `cloudsql.googleapis.com/database/postgresql/num_backends` returning to normal levels and application error rate returning to baseline","Failover requires manual intervention — run `gcloud sql instances failover` to complete the promotion","During HA failover, all writes are queued and replayed after the standby is promoted — no data loss and no errors","The Cloud SQL load balancer automatically routes connections to the standby during failover — no connections are dropped"],"ans":1,"fb":"Cloud SQL HA failover sequence: (1) GCP detects primary failure (~10-30s), (2) standby is promoted to primary (~30-60s total from failure), (3) the Cloud SQL IP address is remapped to the promoted instance, (4) existing connections to the old primary are dropped — applications see connection errors during this window. After promotion, applications reconnect (HikariCP eviction + reconnect). The Cloud SQL IP remains the same (it is a virtual IP), so no JDBC URL changes are needed. Monitor num_backends recovering and application error rate returning to baseline.","context":{"HA failover duration":"~60 seconds total","Connection behavior":"Existing connections dropped, new connections succeed after promotion"}},
    {"level":4,"diff":2,"scenario":4,"q":"You need to schedule Cloud SQL SSL certificate rotation across 6 production databases without causing downtime. What is the correct rotation sequence?","opts":["Delete old certificates and create new ones simultaneously — brief downtime is unavoidable","Phase 1: Create new server CA certs (gcloud sql ssl server-ca-certs create) — old certs still valid. Phase 2: Download new certs and update client-side certificate bundles in all services (deploy with both old and new certs in the bundle). Phase 3: Rotate the active cert (gcloud sql ssl server-ca-certs rotate) — server switches to new cert. Phase 4: Remove old cert from client bundles in a subsequent deployment","Update the JDBC connection string to disable SSL verification during rotation, rotate, then re-enable","Rotate all 6 databases simultaneously in a 2-hour maintenance window — parallel rotation is fastest","SSL rotation only requires a kubectl rollout restart of application pods — no Cloud SQL changes needed"],"ans":1,"fb":"Zero-downtime SSL rotation requires a two-phase approach: (1) create new certificates while old ones remain valid — both are active simultaneously, (2) update client certificate bundles to include the new cert (deploy updated ConfigMaps/Secrets), (3) rotate the active server cert (old cert is now the 'previous' cert, still accepted during transition), (4) remove old cert from client bundles. This ensures no moment where the server presents a cert the client doesn't trust, or the client presents a cert the server doesn't accept. Cloud SQL supports this overlap period.","context":{"Certificate overlap":"Both old and new certs valid during transition","Tool":"gcloud sql ssl server-ca-certs create && rotate"}},
    {"level":4,"diff":3,"scenario":4,"q":"A Cloud SQL instance has been running at 95% storage capacity for 3 days. Auto-resize is enabled. A developer asks whether this is a problem since auto-resize will handle it. What is the complete risk assessment?","opts":["No risk — auto-resize is designed exactly for this situation and will expand storage automatically","Three risks: (1) auto-resize is triggered by storage crossing a threshold — if a large write burst hits before resize completes, the instance can run out of space causing writes to fail mid-transaction; (2) auto-resize permanently increases the disk size, increasing monthly costs; (3) at 95% with auto-resize set to a fixed increment, rapid growth may trigger multiple resize cycles. Action: proactively resize to add headroom, investigate what is growing (tables, indexes, WAL, bloat), and set an alert at 80% to catch this earlier","The only risk is cost — auto-resize increases disk size and cannot be reversed. No operational risk exists","Auto-resize causes a brief Cloud SQL restart — at 95% capacity this is already scheduled and unavoidable","The database will become read-only at 100% storage — auto-resize prevents this automatically with no risk"],"ans":1,"fb":"Auto-resize mitigates the storage crisis but does not eliminate risk. Key risks: (1) timing gap — auto-resize triggers at a threshold but takes time; a sudden large write (batch import, log explosion) can exhaust space before resize completes, causing disk-full errors that abort transactions and corrupt partial writes; (2) cost — disk grows permanently (Cloud SQL storage cannot shrink); (3) root cause — 95% may indicate a runaway process (table bloat, WAL accumulation, missing vacuum) that will consume new space quickly. The alert at 80% is the correct prevention — 95% is already in the danger zone.","context":{"Current storage":"95% capacity","Auto-resize":"Enabled","Duration at 95%":"3 days"}},
    {"level":4,"diff":3,"scenario":4,"q":"During a planned Cloud SQL major version upgrade (PostgreSQL 13 → 15), the upgrade fails at 40% completion. The Cloud SQL instance is showing status 'MAINTENANCE'. What is the immediate action and recovery path?","opts":["Run `gcloud sql instances patch --database-version=POSTGRES_15` again to retry the upgrade","Wait for the MAINTENANCE status to resolve — Cloud SQL will either complete the upgrade or automatically roll back to the previous version. Do not attempt manual intervention during MAINTENANCE status. Monitor `gcloud sql instances describe` for status changes. If the instance stays in MAINTENANCE for more than 2 hours, contact GCP support with the instance name and operation ID from `gcloud sql operations list`","Delete the instance and restore from the pre-upgrade backup — this is the safest recovery path","Scale all application pods to 0 replicas to stop database connections, allowing the upgrade to complete","SSH into the Cloud SQL instance and run PostgreSQL upgrade scripts manually"],"ans":1,"fb":"Cloud SQL MAINTENANCE status means GCP is performing an operation — it is not accessible for management commands. During a failed upgrade, Cloud SQL will attempt to roll back automatically. Manual intervention (patching, deleting) during MAINTENANCE can cause data loss or undefined state. The correct action: monitor via `gcloud sql operations list --instance=INSTANCE_NAME` for the operation status, and contact GCP support if it does not resolve within a reasonable window. Always take a manual backup before major version upgrades (in addition to automated backups) precisely for this scenario.","context":{"Instance status":"MAINTENANCE","Upgrade":"PostgreSQL 13 → 15","Progress":"Failed at ~40%"}},
    {"level":4,"diff":3,"scenario":4,"q":"You discover that HikariCP is configured with `minimumIdle=50` and `maximumPoolSize=50` across 8 replicas of the payment service, connecting to a Cloud SQL instance with `max_connections=200`. After a recent scale-up to 12 replicas, the service starts throwing 'FATAL: remaining connection slots are reserved for non-replication superuser connections'. What is the root cause and the correct fix?","opts":["Increase Cloud SQL max_connections to 500 — the current limit is too low for the workload","The fixed pool size (minimumIdle=maximumPoolSize=50) means each replica holds 50 connections open always. At 12 replicas: 12 × 50 = 600 connections, far exceeding Cloud SQL's 200 max_connections. Fix: reduce maximumPoolSize per replica (e.g. floor(180 / 12) = 15), set minimumIdle lower than maximumPoolSize, or introduce a connection pooler (PgBouncer) as a proxy to multiplex application connections onto fewer database connections","Set maximumPoolSize=200 and minimumIdle=0 — this allows dynamic scaling up to the database limit","Enable Cloud SQL connection pooling in the GCP console — this handles connection limits automatically","The error means Cloud SQL is overloaded — scale up the Cloud SQL instance tier to increase connection capacity"],"ans":1,"fb":"Cloud SQL max_connections is a database-level hard limit. With minimumIdle=maximumPoolSize=50 (eager pool fill), each replica holds exactly 50 connections open — no idle eviction occurs. 12 replicas × 50 = 600, far exceeding 200. The error fires because Cloud SQL reserves some slots for superusers. Solutions: (1) reduce maximumPoolSize to floor((max_connections - reserved) / replicas) — leave headroom for replicas added by HPA, (2) PgBouncer in transaction mode multiplexes 600 application connections onto 50 database connections, (3) set minimumIdle < maximumPoolSize to allow pool shrinking when pods are idle. max_connections increasing on Cloud SQL requires an instance restart and is a less scalable long-term solution.","context":{"minimumIdle":"50","maximumPoolSize":"50","Replicas":"12","Total connections":"12 × 50 = 600","Cloud SQL max_connections":"200","Error":"FATAL: remaining connection slots are reserved for non-replication superuser connections"}}
  ]
};
