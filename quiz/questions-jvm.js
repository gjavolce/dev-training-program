var TRACK_JVM = {
  "id": "jvm",
  "label": "Memory Management",
  "icon": "jvm",
  "desc": "How memory works in Spring Boot services on GKE. Starts with dashboards, progresses through container memory budgets and Spring Boot internals, and reaches JVM diagnostic tools for deep investigation.",
  "levels": [
    {
      "num": 1, "name": "Foundations",
      "desc": "I know my app uses memory and I can see it. I can find the right dashboard, recognise symptoms, and escalate with useful information.",
      "scenarios": [
        "Reading Grafana memory panels and recognising healthy vs unhealthy patterns",
        "Understanding OOMKilled — what it means and how to spot it",
        "Recognising memory configuration in deployment manifests (-Xmx, resource limits)",
        "Understanding Spring Boot warm-up and why first requests are slow",
        "Having a basic mental model of heap, stack, GC, and OutOfMemoryError"
      ]
    },
    {
      "num": 2, "name": "Practitioner",
      "desc": "I understand the container memory budget. When a pod gets OOMKilled, I can figure out whether it's heap or container sizing, and configure a new service correctly.",
      "scenarios": [
        "Sizing JVM memory for containers: heap + Metaspace + threads + buffers must fit the limit",
        "Applying the 60-70% rule and understanding why -Xmx = container limit causes OOMKill",
        "Recognising non-heap memory growth when heap looks healthy but pod memory climbs",
        "Reading kubectl describe pod after OOMKill and correlating with JVM config",
        "Understanding UseContainerSupport and Kubernetes resource requests vs limits"
      ]
    },
    {
      "num": 3, "name": "Advanced",
      "desc": "I know where memory goes inside a Spring Boot app. I can reason about consumers and make informed configuration and code review decisions.",
      "scenarios": [
        "Reasoning about Spring Boot memory consumers: HikariCP, Kafka buffers, Tomcat threads, caches",
        "Reading Actuator memory metrics and correlating with behaviour",
        "Distinguishing OutOfMemoryError types: heap space, Metaspace, unable to create native thread",
        "Flagging memory risks in code reviews: unbounded collections, missing eviction, batch accumulation",
        "Estimating a service's memory footprint from its configuration"
      ]
    },
    {
      "num": 4, "name": "Specialist",
      "desc": "I can find root causes using JVM diagnostic tools. Dashboard observation isn't enough — I look inside the JVM to find leaks, GC pressure, and contention.",
      "scenarios": [
        "Analysing heap dumps with Eclipse MAT to find memory leaks and retained objects",
        "Reading GC logs to identify promotion rate, pause patterns, and old-gen growth",
        "Using JFR to profile allocation hotspots and correlate with GC pressure",
        "Interpreting thread dumps to diagnose contention and pool exhaustion",
        "Investigating Metaspace growth and class loader leaks"
      ]
    },
    {
      "num": 5, "name": "Expert",
      "desc": "I design for memory efficiency and set standards. Proactive decisions that prevent memory problems across services.",
      "scenarios": [
        "Evaluating G1 vs ZGC for production SLAs with JFR data",
        "Designing memory budgets across thread pools and connection pools",
        "Diagnosing cold start memory dimensions and designing readiness strategies",
        "Defining org-wide JVM configuration baselines",
        "Assessing Java version upgrade memory implications"
      ]
    },
    {
      "num": 6, "name": "Authority",
      "desc": "I own memory strategy for the engineering organisation. Platform observability, capacity planning, SLA budgets, and technology roadmap.",
      "scenarios": [
        "Designing fleet-wide memory reliability strategy and driving adoption",
        "Translating memory behaviour into SLA performance budgets",
        "Evaluating emerging JVM capabilities (virtual threads, CRaC, GraalVM AOT)",
        "Building fleet-wide memory observability and automated diagnostics",
        "Leading deep production investigations involving native memory or driver-level issues"
      ]
    }
  ],
  "questions": [
    {"level":1,"diff":1,"scenario":0,"q":"In a Grafana JVM dashboard, 'Used Heap' shows a sawtooth pattern — rises then drops sharply, over and over. What does this pattern indicate?","opts":["A memory leak — objects are not being collected","Normal healthy behaviour — heap rises as objects are allocated, then drops when GC runs","The service is crashing and restarting repeatedly","GC is misconfigured","The heap is too small"],"ans":1,"fb":"The sawtooth pattern is the signature of healthy garbage collection: objects are allocated (heap rises), GC runs and reclaims short-lived objects (heap drops sharply), and the cycle repeats. A memory leak would show a pattern where each trough is higher than the last — the baseline rises over time."},
    {"level":1,"diff":2,"scenario":0,"q":"In Grafana, 'Used Heap' climbs steadily over 6 hours without any drops. What does this pattern suggest?","opts":["Normal behaviour — the heap hasn't filled up yet so GC hasn't run","A memory leak — objects are being retained and GC cannot reclaim them","The service is idle with no traffic","GC is disabled","The dashboard is stale"],"ans":1,"fb":"Steady heap growth without drops means GC is running but cannot reclaim enough memory — objects are being retained by references the GC cannot clear. Normal behaviour produces a sawtooth pattern with periodic drops. GC runs automatically; it cannot be disabled in normal operation."},
    {"level":1,"diff":3,"scenario":0,"q":"A Grafana panel shows 'GC Pause Duration' spiking to 500ms every few minutes. The 'Used Heap' sawtooth looks normal. What is the immediate concern?","opts":["Nothing — GC pauses are normal","Application threads are frozen for 500ms during each pause, causing latency spikes for API callers","The service is about to crash","The heap is too large","The dashboard time range is too narrow"],"ans":1,"fb":"GC pauses are stop-the-world events — all application threads freeze while GC runs. A 500ms pause directly translates to a 500ms latency spike for any request in progress. G1GC's default target is 200ms — 500ms pauses indicate the heap may be too large or GC needs tuning."},

    {"level":1,"diff":1,"scenario":1,"q":"A GKE pod is restarting repeatedly with exit code 137. What happened?","opts":["The liveness probe failed","The JVM threw an unhandled exception","The container was OOM-killed by the Linux kernel — it exceeded its memory limit","The pod exceeded its CPU limit","A deployment rollback was triggered"],"ans":2,"fb":"Exit code 137 = killed by signal 9 (SIGKILL). The Linux OOM killer sends SIGKILL when a container's memory usage exceeds its cgroup limit. Liveness probe failures show different events in kubectl describe. CPU throttling slows the pod but does not kill it."},
    {"level":1,"diff":2,"scenario":1,"q":"A pod was OOM-killed (exit code 137). The JVM logs do NOT show OutOfMemoryError. How is this possible?","opts":["It's not possible — OOMKill always produces an OutOfMemoryError first","The kernel killed the container before the JVM detected it — OOMKill is external to the JVM","The logs were lost during the restart","The JVM suppressed the error","The OOM-kill was for a different container in the pod"],"ans":1,"fb":"OOMKill (exit 137) and OutOfMemoryError are different mechanisms. OutOfMemoryError is the JVM detecting it cannot allocate heap. OOMKill is the Linux kernel detecting the entire container process exceeds its memory limit. OOMKill can happen when non-heap memory pushes total memory past the limit — the JVM heap may be fine."},
    {"level":1,"diff":3,"scenario":1,"q":"A service restarts 3 times per day with exit code 137, always during peak traffic. Heap dashboards show the heap is only 60% full when restarts happen. What should you investigate?","opts":["The heap is too small — increase -Xmx","Non-heap memory (thread stacks, Kafka buffers, native memory) is growing under load and pushing total process memory past the container limit","The liveness probe is too aggressive during peak traffic","The service is CPU throttled, not memory constrained","Cloud SQL is rejecting connections during peak"],"ans":1,"fb":"Heap at 60% but container OOM-killed means the problem is outside the heap. Under peak traffic: more Tomcat threads (each ~1MB stack), more Kafka consumer buffers, JIT-compiled code — all grow. Total JVM memory = heap + Metaspace + thread stacks + direct buffers + native. If -Xmx is set too close to the container limit, there's no room for non-heap growth under load."},

    {"level":1,"diff":1,"scenario":2,"q":"In a Kubernetes deployment manifest, what does -Xmx512m control?","opts":["The container's total memory limit","The maximum heap memory the JVM can allocate","The amount of CPU allocated to the JVM","The number of threads the JVM can create","The maximum size of a single object"],"ans":1,"fb":"-Xmx512m sets the maximum Java heap size to 512 megabytes. The heap is where Java objects live. It does NOT control total JVM memory (which also includes Metaspace, thread stacks, and native memory) and it is NOT the container limit (set via resources.limits.memory)."},
    {"level":1,"diff":2,"scenario":2,"q":"A deployment manifest shows resources.limits.memory: 1Gi and the JVM flag -Xmx900m. Is this a good configuration?","opts":["Yes — the heap should use as much of the limit as possible","No — this leaves only ~124MB for Metaspace, thread stacks, direct byte buffers, and native memory, which is likely not enough","No — -Xmx should always equal the memory limit exactly","Yes — the 100MB headroom is standard","It depends on the CPU allocation"],"ans":1,"fb":"Total JVM memory = heap (900MB) + Metaspace (~80-150MB) + thread stacks (threads x ~1MB) + direct byte buffers + JIT code cache + native allocations. With a 1Gi limit and 900MB heap, only ~124MB remains. Under load this is almost certainly insufficient. Safe rule: -Xmx should be 60-70% of the container limit."},
    {"level":1,"diff":3,"scenario":2,"q":"You see -XX:+UseContainerSupport in a JVM configuration. What does this flag do?","opts":["Enables Spring Boot to detect it's running in Docker","Tells the JVM to read memory and CPU limits from the container's cgroup instead of the host machine","Enables Kubernetes health probes for the JVM","Optimises GC for containerised environments","Enables JFR recording in containers"],"ans":1,"fb":"Without container support, the JVM reads the host machine's total memory (e.g., 32GB on a GKE node) and may set its heap accordingly — far exceeding the container's actual limit. With UseContainerSupport, the JVM reads cgroup limits. This flag is on by default since Java 8u191 and Java 11+."},

    {"level":1,"diff":1,"scenario":3,"q":"A Spring Boot service takes 15 seconds to start. During those first 15 seconds, API response times are 2-3x higher than normal. Why?","opts":["The database is slow during startup","The JVM's JIT compiler hasn't yet optimised hot code paths — it runs in interpreted mode initially","The service is downloading dependencies at startup","Kubernetes is throttling CPU during startup","The readiness probe is misconfigured"],"ans":1,"fb":"The JVM initially runs bytecode through an interpreter. The JIT compiler identifies hot methods and compiles them to native machine code over time. Until hot paths are compiled, they run significantly slower. Spring context initialisation, connection pool creation, and Kafka rebalance also add to startup time."},
    {"level":1,"diff":2,"scenario":3,"q":"A freshly deployed pod passes its readiness probe but the first 50 API requests have 3x higher latency. What is happening?","opts":["The pod has a memory leak that only appears under initial load","The JVM is still warming up — JIT compilation, connection pool creation, and cache population haven't completed yet","The Kubernetes scheduler placed it on an overloaded node","The container image is being downloaded during these requests","The database needs to warm its cache for this pod"],"ans":1,"fb":"Readiness probes check basic health (e.g., /actuator/health returns UP), but the JVM isn't fully warm yet. JIT hasn't compiled hot paths, HikariCP may create connections lazily, Spring caches are empty, and Kafka consumers may still be rebalancing."},

    {"level":1,"diff":1,"scenario":4,"q":"What is 'garbage collection' in the JVM?","opts":["A background process that deletes unused files from the container's filesystem","The JVM's automatic process of identifying and reclaiming memory occupied by objects that are no longer referenced","A manual cleanup developers must trigger by calling System.gc()","A process that removes unused Docker images from the node","A Kubernetes feature that evicts idle pods"],"ans":1,"fb":"Garbage collection (GC) is the JVM's automatic memory management. It identifies objects in heap memory that are no longer reachable from any active reference chain and reclaims that memory for new allocations. Developers do not need to call it manually."},
    {"level":1,"diff":2,"scenario":4,"q":"A log shows java.lang.OutOfMemoryError: Java heap space. What does this mean?","opts":["The Linux kernel killed the container","The JVM could not allocate a new object because the heap is full and GC could not free enough space","The JVM ran out of thread stack space","The container's disk is full","Metaspace is exhausted"],"ans":1,"fb":"'Java heap space' means the garbage collector ran but could not free enough heap memory for a new allocation. This is different from OOMKill (exit 137). Metaspace exhaustion produces 'OutOfMemoryError: Metaspace'. Thread issues produce 'unable to create native thread'."},
    {"level":1,"diff":3,"scenario":4,"q":"What is the difference between OutOfMemoryError: Java heap space and the pod being OOM-killed (exit code 137)?","opts":["They are the same thing","OutOfMemoryError is the JVM detecting it can't allocate heap; OOMKill is the Linux kernel killing the container for exceeding its memory limit — they can happen independently","OutOfMemoryError always causes an OOMKill","OOMKill always produces an OutOfMemoryError first","OutOfMemoryError is for CPU; OOMKill is for memory"],"ans":1,"fb":"These are separate mechanisms at different layers. OutOfMemoryError: the JVM's internal check. OOMKill: the Linux kernel's enforcement. You can have OOMKill without OutOfMemoryError (non-heap exceeds limit while heap is fine). You can have OutOfMemoryError without OOMKill (heap full but total memory under limit)."},

    {"level":2,"diff":1,"scenario":0,"q":"A Spring Boot service runs in a container with 1Gi memory limit. The JVM uses heap, Metaspace, thread stacks, direct byte buffers, and native memory. What must be true?","opts":["Only the heap needs to fit within 1Gi","The sum of all JVM memory regions must fit within 1Gi, not just the heap","Metaspace and thread stacks are outside the container limit","Kubernetes automatically adjusts the limit for non-heap memory","The JVM manages the container limit automatically"],"ans":1,"fb":"The container memory limit (enforced by Linux cgroups) applies to the entire process. Total = heap + Metaspace + thread stacks (each ~1MB) + direct byte buffers + JIT code cache + native allocations. If this total exceeds the container limit, the kernel sends SIGKILL (exit 137)."},
    {"level":2,"diff":2,"scenario":0,"q":"You are sizing a new service. It has 200 Tomcat threads, 10 HikariCP connections, 3 Kafka consumers (50MB fetch buffer each). Estimated Metaspace is 120MB. What is the minimum non-heap memory needed?","opts":["About 120MB (just Metaspace)","About 200MB (Metaspace + some buffers)","About 520MB (200 threads x 1MB stacks + 150MB Kafka buffers + 120MB Metaspace + overhead)","About 50MB (non-heap is always small)","Impossible to estimate without a heap dump"],"ans":2,"fb":"Thread stacks: 200 x ~1MB = 200MB. Kafka buffers: 3 x 50MB = 150MB (direct byte buffers). Metaspace: 120MB. Plus JIT code cache (~50MB), native overhead. Total non-heap ~520MB+. If container limit is 1.5Gi, that leaves ~1GB for heap."},
    {"level":2,"diff":3,"scenario":0,"q":"A service has -Xmx=600m in a 1Gi container. After adding a new Kafka consumer group (5 threads, 50MB fetch.max.bytes each), the pod starts getting OOM-killed. Heap dashboards look normal. Why?","opts":["The heap is too small for the new consumer's messages","The 5 new Kafka consumers added ~250MB of direct byte buffer memory, pushing total JVM memory past the 1Gi container limit","Kafka is incompatible with the current JVM version","The new consumer group is causing GC pressure","The liveness probe is failing due to Kafka connectivity"],"ans":1,"fb":"Kafka consumer buffers (fetch.max.bytes) are allocated as direct byte buffers — outside the heap. 5 consumers x 50MB = 250MB off-heap. Before: 600MB heap + ~300MB non-heap = ~900MB. After: +250MB = ~1.15Gi > 1Gi → OOMKill."},

    {"level":2,"diff":1,"scenario":1,"q":"Why should -Xmx be set to 60-70% of the container memory limit, not 100%?","opts":["The JVM doesn't use all the heap anyway","The remaining 30-40% is needed for non-heap memory: Metaspace, thread stacks, direct byte buffers, JIT code cache, and native memory","Kubernetes reserves 30% for system processes","The GC needs 30% headroom to function","It's just a convention with no technical basis"],"ans":1,"fb":"The JVM uses significantly more memory than just the heap. Metaspace (~80-200MB), thread stacks (each ~1MB), direct byte buffers (NIO, Kafka, Netty), JIT compiled code cache (~50-240MB), and native allocations all live outside the heap but inside the container's memory limit."},
    {"level":2,"diff":2,"scenario":1,"q":"A team sets -Xmx equal to resources.limits.memory (both 2Gi). The service OOM-kills on every deployment but works at steady state. What explains this?","opts":["Deployments use more memory than steady state","During deployment, the JVM starts with heap + Metaspace for class loading + JIT compilation. Since -Xmx=2Gi and limit=2Gi, there is zero room for non-heap — any Metaspace or native allocation pushes past the limit","The deployment manifest is incorrect","Kubernetes doubles memory usage during rolling updates","The readiness probe causes extra memory allocation"],"ans":1,"fb":"At startup, the JVM allocates heap (reserves 2Gi), loads classes into Metaspace (100-200MB), creates thread stacks, and JIT-compiles code. Since 2Gi heap + any non-heap > 2Gi limit, the kernel kills the process before it finishes starting."},
    {"level":2,"diff":3,"scenario":1,"q":"Two identically configured services (-Xmx=512m, limit=1Gi) but one is OOM-killed weekly while the other never is. What is the most likely difference?","opts":["Different JVM versions","One service has more active threads, Kafka consumers, or larger caches — its non-heap memory footprint is higher, exhausting the headroom between heap and container limit","The OOM-killed service has a heap memory leak","They must be on different GKE node types","Cloud SQL is configured differently"],"ans":1,"fb":"With identical -Xmx and container limits, the difference must be in non-heap consumption. Service A might have 50 threads (50MB stacks) + 5 Kafka consumers (250MB). Service B might have 200 threads (200MB) + 10 Kafka consumers (500MB). Same config, very different non-heap footprints."},

    {"level":2,"diff":1,"scenario":2,"q":"Grafana shows JVM heap at 55% utilisation, but container memory usage is at 92%. What is consuming the remaining memory?","opts":["The heap metric is wrong","Non-heap JVM memory: Metaspace, thread stacks, direct byte buffers, JIT code cache, and native allocations","Kubernetes overhead","Other containers in the pod (e.g., sidecar)","Disk cache"],"ans":1,"fb":"JVM heap metrics only show heap. Container memory shows everything the process uses. The gap is non-heap: Metaspace, thread stacks, direct byte buffers, JIT-compiled code, and native allocations."},
    {"level":2,"diff":2,"scenario":2,"q":"A service's container memory usage grows slowly over days while heap usage stays stable. What are the most likely non-heap culprits?","opts":["Heap fragmentation","Metaspace growth (dynamic class loading) or direct byte buffer accumulation (connection/buffer leaks in NIO or Kafka)","GC pressure causing memory fragmentation","Kafka consumer lag","Increasing request payload sizes"],"ans":1,"fb":"Stable heap + growing container memory means something outside the heap is growing. Top candidates: Metaspace (dynamic class generation), direct byte buffers (Kafka/Netty not releasing buffers), or thread count growing (leaked thread pools)."},
    {"level":2,"diff":3,"scenario":2,"q":"After adding a new @Cacheable method using Spring's default ConcurrentMapCacheManager, heap usage grows linearly over weeks. What is the root cause?","opts":["The cache eviction policy is too aggressive","Spring's default ConcurrentMapCacheManager has no size limit or eviction — entries accumulate indefinitely","GC is not running frequently enough","The cache key is too large","The @Cacheable annotation is on the wrong method"],"ans":1,"fb":"Spring's default ConcurrentMapCacheManager uses a plain ConcurrentHashMap with no maximum size, no TTL, and no eviction. Every unique cache key adds an entry permanently. Fix: switch to Caffeine cache with maximumSize and expireAfterWrite."},

    {"level":2,"diff":1,"scenario":3,"q":"After an OOMKill, you run kubectl describe pod. What fields tell you the container exceeded its memory limit?","opts":["The pod's CPU usage section","'Last State: Terminated', 'Reason: OOMKilled', 'Exit Code: 137', and the container's memory limit in the resources section","The pod's event log only","The node's disk usage","The container's image version"],"ans":1,"fb":"kubectl describe pod shows 'Last State: Terminated, Reason: OOMKilled, Exit Code: 137'. The resources section shows limits.memory — the ceiling that was exceeded. Next step: correlate with JVM heap dashboards to determine heap vs non-heap cause."},
    {"level":2,"diff":2,"scenario":3,"q":"A pod shows OOMKilled in kubectl describe, but the JVM's last log line before the kill was a normal request completion — no OutOfMemoryError. What does this tell you?","opts":["The logs are incomplete","The JVM heap was fine — the OOMKill was caused by non-heap memory pushing total process memory past the container limit","The OOMKill was for a different container in the pod","The JVM suppressed the error","The container was killed for CPU, not memory"],"ans":1,"fb":"If the JVM didn't throw OutOfMemoryError, the heap had room. The OOMKill was triggered by total process memory (heap + non-heap) exceeding the container limit. The kernel kills instantly — the JVM has no chance to log."},

    {"level":2,"diff":1,"scenario":4,"q":"What does UseContainerSupport do for a JVM on GKE?","opts":["Enables health probes","Makes the JVM read memory and CPU limits from the container's cgroup instead of the host node's total resources","Optimises GC for containers","Enables JFR in containers","Allows the JVM to use swap memory"],"ans":1,"fb":"Without container support, a JVM on a 32GB GKE node would see 32GB and auto-size its heap to 8GB — exceeding a 1Gi container limit and causing OOMKill. UseContainerSupport makes the JVM read cgroup limits. Default since Java 8u191+."},
    {"level":2,"diff":2,"scenario":4,"q":"What is the difference between resources.requests.memory and resources.limits.memory for a JVM pod?","opts":["They are the same thing","Requests is the scheduling guarantee (Kubernetes ensures this much on the node); limits is the kill boundary (the kernel kills the process if it exceeds this)","Requests controls heap; limits controls non-heap","Requests is for startup; limits is for steady state","Limits is advisory; requests is enforced"],"ans":1,"fb":"Requests: used by the scheduler to place pods. Limits: enforced by the kernel — exceeding it causes SIGKILL. For JVM workloads, set requests close to limits (JVM reserves heap at startup), and set limits to accommodate heap + non-heap."},
    {"level":2,"diff":3,"scenario":4,"q":"A JVM pod has requests.memory=512Mi and limits.memory=1Gi. It consistently uses 800Mi. What is the risk?","opts":["No risk — it's within limits","The pod may be scheduled on a node without 800Mi available — the scheduler only guarantees 512Mi, and the pod could be evicted under node memory pressure","The pod will be OOM-killed at 512Mi","The JVM will crash at 512Mi","Kubernetes will autoscale the pod"],"ans":1,"fb":"The scheduler places based on requests (512Mi). If the node is overcommitted, Kubernetes may evict Burstable pods (requests != limits). For JVM workloads with predictable memory, setting requests = limits (Guaranteed QoS) prevents eviction."},

    {"level":3,"diff":1,"scenario":0,"q":"A Spring Boot service is using more memory than expected. Which components should you check first?","opts":["Only the heap size","HikariCP connection pool, Kafka consumer buffers, Tomcat thread pool, and Spring caches — the major memory consumers in a typical Spring Boot service","Only the number of REST endpoints","The JVM version","The container's CPU allocation"],"ans":1,"fb":"In a typical Spring Boot service: HikariCP holds DB connections (each ~1MB), Kafka consumers allocate fetch buffers (fetch.max.bytes per consumer, off-heap), Tomcat threads each have a ~1MB stack, and Spring caches can grow unboundedly."},
    {"level":3,"diff":2,"scenario":0,"q":"A service has spring.kafka.consumer.properties.fetch.max.bytes=52428800 (50MB) and spring.kafka.listener.concurrency=3. How much off-heap memory do the Kafka consumers use?","opts":["50MB total","150MB","300MB — Kafka double-buffers internally, so 3 x 50MB x 2","Zero — Kafka buffers are on the heap","It depends on partition count"],"ans":2,"fb":"Each Kafka consumer thread allocates direct byte buffers for fetching. With double-buffering (one being processed, next being filled): 3 x 50MB x 2 = 300MB of direct byte buffers — entirely outside the JVM heap."},
    {"level":3,"diff":3,"scenario":0,"q":"Three identical pods run the same service, but pod A uses 400MB, pod B uses 600MB, and pod C uses 900MB. Same configuration. What is the most likely cause?","opts":["Different JVM versions on different nodes","Different Kafka partition assignments — the pod with more partitions has more consumer buffers and cached data","Random GC timing","Different container limits","Network latency"],"ans":1,"fb":"Kafka partitions are assigned unevenly across consumers. A pod assigned more partitions uses more fetch buffer memory and may cache more per-partition state. Configuration is identical but runtime state diverges."},

    {"level":3,"diff":1,"scenario":1,"q":"Which Actuator metric tells you current heap usage?","opts":["/actuator/health","jvm.memory.used (with area=heap tag)","hikaricp.connections.active","process.cpu.usage","jvm.threads.live"],"ans":1,"fb":"The Micrometer metric jvm.memory.used with tag area=heap reports current heap usage. Query at /actuator/metrics/jvm.memory.used?tag=area:heap. Related: jvm.memory.max shows -Xmx, jvm.memory.committed shows reserved OS memory."},
    {"level":3,"diff":2,"scenario":1,"q":"jvm.memory.used{area=heap} is stable but jvm.buffer.memory.used is growing. What does this indicate?","opts":["A heap memory leak","Direct byte buffers (off-heap) are accumulating — commonly from NIO, Kafka, or Netty not releasing buffers","GC is not running","Actuator metrics are wrong","Metaspace is growing"],"ans":1,"fb":"jvm.buffer.memory.used tracks direct byte buffers — memory outside the heap via ByteBuffer.allocateDirect(). Growth while heap is stable indicates off-heap accumulation. This memory counts toward the container limit but not -Xmx."},
    {"level":3,"diff":3,"scenario":1,"q":"After a load test, hikaricp.connections.active stays at 10 (pool max) and never decreases. hikaricp.connections.pending is 45. What does this tell you?","opts":["The database is healthy","All 10 connections are checked out and not being returned (possible leak), while 45 threads are blocked waiting","The pool is too large","HikariCP is misconfigured","The load test is still running"],"ans":1,"fb":"Active=10 (pool max) with pending=45 means the pool is exhausted. 10 connections are held and 45 threads are queued. If this persists after load stops, it's a connection leak. Check with leakDetectionThreshold."},

    {"level":3,"diff":1,"scenario":2,"q":"A log shows OutOfMemoryError: Metaspace. What ran out?","opts":["The JVM heap","The area where class definitions and metadata are stored — too many classes loaded","The container's total memory","Thread stack space","Direct byte buffers"],"ans":1,"fb":"Metaspace stores class metadata. OutOfMemoryError: Metaspace means more classes were loaded than the configured (or default) Metaspace can hold. Common causes: dynamic class generation (CGLIB, reflection, SpEL)."},
    {"level":3,"diff":2,"scenario":2,"q":"A service throws OutOfMemoryError: unable to create native thread. Heap is 40% full. What is the cause?","opts":["The heap is too small","The OS cannot allocate memory for a new thread stack — either too many threads exist or remaining memory is insufficient for another ~1MB stack","GC is not collecting threads","The thread pool is misconfigured","Kafka is creating too many partitions"],"ans":1,"fb":"Each thread needs a stack (~1MB by default). 'Unable to create native thread' means the OS can't allocate more stack memory. With a 1Gi container, 600MB heap, 150MB Metaspace, and 200 existing threads (200MB), only ~50MB remains."},
    {"level":3,"diff":3,"scenario":2,"q":"OutOfMemoryError: Java heap space occurs during a batch job processing 500K records in a single @Transactional method. The same job worked for 100K records. What is the cause?","opts":["The heap is globally too small","Hibernate's first-level cache holds all 500K entities in memory for the duration of the transaction","The database query is too slow","Kafka is consuming too much memory","The GC algorithm is wrong for batch workloads"],"ans":1,"fb":"Hibernate's first-level (session) cache retains every entity loaded or persisted within a transaction. With 500K entities in a single @Transactional scope, all stay in heap until commit. Fix: flush() and clear() every 1000 records."},

    {"level":3,"diff":1,"scenario":3,"q":"When reviewing a PR, which of these patterns is a memory risk?","opts":["Using a List<String> as a local variable","Adding entries to a ConcurrentHashMap field based on request data with no size limit","Using Spring's @Autowired for dependency injection","Creating a new ObjectMapper in a utility method","Using Stream.of() for a small collection"],"ans":1,"fb":"A ConcurrentHashMap field that grows based on request data with no size limit is a classic memory leak. Every unique key adds an entry that is never removed. Fix: use a bounded cache (Caffeine with maximumSize) or add eviction."},
    {"level":3,"diff":2,"scenario":3,"q":"A PR adds @Cacheable('partnerRates') using Spring's default cache. The method returns exchange rates per partner per currency pair. What memory concern should you raise?","opts":["No concern — caching is good","Spring's default ConcurrentMapCacheManager has no size limit. Many partner x currency combinations will grow the cache indefinitely","The concern is only cache staleness, not memory","@Cacheable always uses off-heap","The concern is thread safety"],"ans":1,"fb":"With default Spring caching, every unique key becomes a permanent entry. 500 partners x 50 currencies = 25,000 entries. Recommend: configure Caffeine with maximumSize and expireAfterWrite."},
    {"level":3,"diff":3,"scenario":3,"q":"A PR introduces a new @Async thread pool with corePoolSize=50 and maxPoolSize=200. The service runs in a 1Gi container with -Xmx=600m. What memory concern should you raise?","opts":["No concern — thread pools are standard","200 threads at max x ~1MB stack each = 200MB. Combined with existing threads, heap, and Metaspace, total may exceed 1Gi under load","The concern is CPU, not memory","@Async pools don't create real threads","The concern is only pool exhaustion"],"ans":1,"fb":"Each thread consumes ~1MB stack memory outside the heap. Budget: 600MB heap + 150MB Metaspace + existing 100 threads (100MB) + new pool 200 threads (200MB) = ~1.05GB — exceeds 1Gi. Fix: reduce maxPoolSize, reduce -Xmx, or increase container limit."},

    {"level":3,"diff":1,"scenario":4,"q":"You need to estimate whether a new service fits within 1Gi. It has: -Xmx=500m, ~150 Tomcat threads, 5 Kafka consumers (30MB fetch buffer each), estimated Metaspace 100MB. Does it fit?","opts":["Yes — 500MB heap + 100MB Metaspace = 600MB","No — 500MB heap + 150 stacks (150MB) + 5 Kafka buffers x 30MB x 2 (300MB) + 100MB Metaspace + ~50MB overhead = ~1.1GB","Yes — non-heap is negligible","Impossible to estimate without running the service","Yes — Kubernetes allocates extra automatically"],"ans":1,"fb":"Memory budget: Heap 500MB + Stacks 150MB + Kafka buffers 300MB + Metaspace 100MB + overhead 50MB = ~1.1GB > 1Gi. Options: reduce -Xmx, reduce fetch.max.bytes, reduce Tomcat threads, or increase limit to 1.5Gi."},
    {"level":3,"diff":2,"scenario":4,"q":"A service currently uses 800MB total in a 1Gi container. A new feature adds 3 @Cacheable methods. How do you assess whether the service still fits?","opts":["Caches are negligible","Estimate cache sizes: number of unique keys x size per entry x 3 caches, then add to 800MB. If total exceeds 1Gi, increase limit or bound the caches","Just deploy and see","Add a heap dump to check","Increase -Xmx by the cache size"],"ans":1,"fb":"Cache memory is heap memory. Estimate: if each cache holds 10K entries at 500 bytes = ~5MB per cache, 15MB total — fine. But 100K entries at 50KB each = 500MB+ — not fine. Always calculate worst-case and configure bounded caches."},

    {"level":4,"diff":1,"scenario":0,"q":"Which tool takes a heap dump from a running JVM without restarting it?","opts":["jstack","jcmd <pid> GC.heap_dump <file>","jstat -gcutil","async-profiler","kubectl logs"],"ans":1,"fb":"jcmd <pid> GC.heap_dump <file> triggers a heap dump on a live JVM. It briefly pauses all threads. jstack produces thread dumps. jstat shows GC stats. async-profiler is for CPU/allocation sampling."},
    {"level":4,"diff":2,"scenario":0,"q":"In Eclipse MAT, a ConcurrentHashMap shows 'Retained Heap' of 800MB in a 1GB heap. What does 'retained heap' mean?","opts":["The map's own memory usage","The total memory that would be freed if this map were garbage collected — including everything it exclusively references","The maximum the map can grow to","Memory allocated but unused by the map","Memory used by keys only"],"ans":1,"fb":"Retained heap = memory freed if this object and all objects it exclusively retains were GC'd. 800MB retained means the map references an object graph totalling 800MB. The map's 'shallow heap' (structure itself) might be a few MB. Retained heap is the key metric for finding leaks in MAT."},
    {"level":4,"diff":3,"scenario":0,"q":"A heap dump shows 600MB of byte[] retained by a single LinkedHashMap with access-order enabled but no eviction. The service is multi-tenant. What is the diagnosis?","opts":["Normal serialisation buffers","An unbounded LRU-style cache accumulating entries per tenant without eviction","A Kafka consumer buffer","HikariCP connection state","A logging buffer"],"ans":1,"fb":"LinkedHashMap with access-order is an LRU cache, but without removeEldestEntry() returning true, entries accumulate forever. In multi-tenant service, each tenant's data adds permanently. Fix: switch to Caffeine with maximumSize and expireAfterWrite."},

    {"level":4,"diff":1,"scenario":1,"q":"In GC logs, a collection event shows 'before' and 'after' heap sizes nearly equal. What does this indicate?","opts":["GC ran efficiently","GC ran but reclaimed almost no memory — most objects survived, suggesting a potential leak or undersized heap","The heap is correctly sized","GC didn't run","The log format is wrong"],"ans":1,"fb":"If before ~= after, very few objects were collected — most are still reachable. Repeated pattern = strong leak signal or severely undersized heap. Healthy GC shows significant drops: before >> after."},
    {"level":4,"diff":2,"scenario":1,"q":"GC logs show frequent minor GCs with high allocation rate, but old-gen stays stable. Is this a problem?","opts":["Yes — frequent GC is always bad","No — high allocation of short-lived objects is normal under load. Stable old-gen means objects are collected in young gen. It's throughput, not a leak.","Yes — it's a memory leak","No — but only below 1GB/s","Yes — eden is too small"],"ans":1,"fb":"High allocation rate with stable old-gen = healthy high-throughput pattern. Short-lived objects created and collected in young gen. If old-gen were rising, that would indicate promotion and a leak signal."},
    {"level":4,"diff":3,"scenario":1,"q":"After a leak fix, GC logs show old-gen growth has slowed but not stopped. Over 24 hours, old-gen rises by 50MB. Is the fix complete?","opts":["Yes — slow growth is acceptable","No — a complete fix would produce stable old-gen baseline with sawtooth. Ongoing growth means retained objects still accumulating. Further profiling needed.","Yes — 50MB/day is normal","No — switch GC algorithms","Wait a week"],"ans":1,"fb":"A properly fixed service shows stable old-gen — rises and falls with GC around a baseline. 50MB/day x 30 days = 1.5GB — eventual crisis. Take two heap dumps 4 hours apart, compare in MAT."},

    {"level":4,"diff":1,"scenario":2,"q":"What does JFR offer for memory investigation that Grafana metrics do not?","opts":["JFR shows the same metrics in different format","JFR profiles allocation hotspots — which methods create the most objects — giving code-level detail","JFR fixes memory leaks","JFR only works on Oracle JDK","JFR replaces heap dumps"],"ans":1,"fb":"Grafana shows aggregates (total heap, GC pauses). JFR profiles at method level: which methods allocate the most, allocation rate per call site, object sizes, promotion behaviour. This traces GC pressure to specific code. Safe for production (<1% overhead)."},
    {"level":4,"diff":2,"scenario":2,"q":"A JFR recording shows the top allocator is byte[] from ch.qos.logback.classic.Logger at 300MB/s. What does this mean?","opts":["Logback has a memory leak","The service generates enormous log output, and string formatting allocates byte arrays at massive rate, increasing GC pressure","JFR miscounting","Logback needs upgrading","The bytes are from Kafka"],"ans":1,"fb":"Log message formatting creates temporary byte[] and String objects. At DEBUG level with high request volume, this produces hundreds of MB/s of garbage. Fix: set production logging to INFO, use parameterised logging (log.info('msg {}', var))."},
    {"level":4,"diff":3,"scenario":2,"q":"JFR shows TierEvaluationContext objects being promoted to old gen despite being short-lived. What explains this?","opts":["Objects are too large for young gen","The tier calculation spans multiple minor GC cycles — objects survive collections during processing and get promoted","Objects are static singletons","JFR misreporting","Old gen is undersized"],"ans":1,"fb":"Promotion happens when objects survive enough young gen collections. If calculation takes longer than minor GC interval, objects created at the start survive and get promoted. Fix: make calculations faster or increase young gen size."},

    {"level":4,"diff":1,"scenario":3,"q":"A thread dump shows 150 threads in TIMED_WAITING inside HikariPool.getConnection. What does this indicate?","opts":["Database is performing normally","The connection pool is exhausted — 150 threads are queued waiting for a connection","There is a deadlock","Too many threads","Kafka consumers are blocked"],"ans":1,"fb":"TIMED_WAITING inside HikariPool.getConnection means threads are waiting for a connection from the pool. 150 waiting = pool fully checked out. Cascades into HTTP timeouts. Root cause: leaked connections, slow queries, or undersized pool."},
    {"level":4,"diff":2,"scenario":3,"q":"A thread dump shows deadlock: Thread A holds lock X, waits for Y. Thread B holds lock Y, waits for X. Both in @Transactional methods. Most likely cause?","opts":["JVM bug","Two transactions acquiring database row locks in different orders","Spring proxy mechanism","Kafka consumer threads interfering","HikariCP internal deadlock"],"ans":1,"fb":"Classic lock-ordering deadlock at the database level. Thread A locks row X then needs Y; Thread B locked Y then needs X. Fix: consistent lock ordering (always lower ID first) or optimistic locking (@Version)."},
    {"level":4,"diff":3,"scenario":3,"q":"A thread dump shows 200 threads — 180 in RUNNABLE inside java.util.HashMap.put. What could cause this?","opts":["Normal HashMap usage","A non-thread-safe HashMap used concurrently — multiple threads cause infinite loop in bucket chains","Map is too large","GC interfering","HashCode is slow"],"ans":1,"fb":"HashMap is not thread-safe. Concurrent modification can form cycles in bucket chains. Threads get stuck in infinite loops inside put(), appearing RUNNABLE but burning CPU forever. Fix: use ConcurrentHashMap or synchronise access."},

    {"level":4,"diff":1,"scenario":4,"q":"A service's Metaspace usage grows slowly over weeks, long after startup. What is the most likely cause?","opts":["Normal class loading during every request","Dynamic class generation — CGLIB proxies, SpEL compilation, reflection-based serialisation creating new classes at runtime","JVM loading more classpath classes over time","Metaspace fragmentation","GC not running on Metaspace"],"ans":1,"fb":"After startup, classpath is fully loaded and Metaspace should plateau. Continued growth means runtime class creation. Common: CGLIB proxy regeneration, SpEL per unique expression, Groovy scripts creating classloaders."},
    {"level":4,"diff":2,"scenario":4,"q":"Metaspace grows 5MB/day. At this rate, the container will eventually be OOM-killed. What is the two-part fix?","opts":["Just increase container limit","Set -XX:MaxMetaspaceSize as safety net (e.g., 256m) AND investigate what generates classes at runtime","Just reduce -Xmx","Switch to ZGC","Restart the pod daily"],"ans":1,"fb":"Part 1: MaxMetaspaceSize converts silent OOMKill into visible OutOfMemoryError: Metaspace. Part 2: Use JFR class loading events or -verbose:class to find the source. Common fixes: disable DevTools in prod, cache compiled SpEL, switch serialisation libraries."},

    {"level":5,"diff":1,"scenario":0,"q":"A latency-sensitive service needs P99 under 30ms. G1GC produces occasional 100ms pauses. You evaluate ZGC. What is the key trade-off?","opts":["ZGC is slower in all cases","ZGC eliminates stop-the-world pauses (sub-ms) but uses more CPU and requires more memory (~2x RSS). P99 improves but CPU and memory cost increases.","ZGC only works on heaps above 16GB","ZGC is experimental","ZGC requires a different JVM"],"ans":1,"fb":"ZGC performs marking and compaction concurrently. Trade-offs: ~15-20% more CPU, coloured pointers increase RSS ~2x heap mapping. For P99 SLOs, ZGC is often right if the pod has enough CPU."},
    {"level":5,"diff":2,"scenario":0,"q":"After switching to ZGC, P99 drops from 100ms to 3ms. But the pod is OOM-killed because ZGC's RSS is 2x G1's. What is the fix?","opts":["Switch back to G1","Increase container memory limit to accommodate ZGC's higher RSS. The P99 improvement justifies the cost.","Reduce -Xmx by half","Switch to Shenandoah","Set ZGC SoftMaxHeapSize to half"],"ans":1,"fb":"ZGC's coloured pointers map heap multiple times, increasing RSS. Increase container limit accordingly. Reducing -Xmx defeats the purpose. SoftMaxHeapSize controls GC aggressiveness, not RSS."},

    {"level":5,"diff":1,"scenario":3,"q":"Multiple teams configure JVMs differently. One team has no GC logging, no HeapDumpOnOutOfMemoryError, and -Xmx at 95% of container limit. What is the risk?","opts":["No risk — each team knows best","No diagnostic data when memory issues occur, and -Xmx at 95% leaves almost no room for non-heap, making OOMKill likely under any load increase","Only slightly higher memory usage","Only a risk for the platform team","GC logging has too much overhead"],"ans":1,"fb":"Without GC logging, no visibility into allocation patterns. Without HeapDumpOnOutOfMemoryError, OOM crashes produce no evidence. With -Xmx at 95%, guaranteed OOMKill under load. Modern GC logging has negligible overhead."},
    {"level":5,"diff":2,"scenario":3,"q":"What should a mandatory JVM baseline include for all Spring Boot services?","opts":["Only -Xmx","UseContainerSupport, HeapDumpOnOutOfMemoryError with HeapDumpPath, GC logging (-Xlog:gc*), MaxMetaspaceSize as safety net, and -Xmx at 60-70% of container limit","Mandate ZGC for all services","Only Grafana dashboards","Weekly heap dumps from all services"],"ans":1,"fb":"A baseline ensures: container awareness, post-mortem capability, observability (GC logging), safety net for non-heap growth, and correct heap sizing. Delivered via shared Helm chart or ConfigMap template with per-service overrides."},

    {"level":5,"diff":1,"scenario":4,"q":"Your team evaluates upgrading from Java 17 to 21. What memory-related changes should you assess?","opts":["No memory changes between 17 and 21","ZGC generational mode (default in 21), virtual thread memory model, and potential GC ergonomics changes","Only virtual thread stack size","Only SecurityManager removal","Only the new GC performance"],"ans":1,"fb":"Java 21: generational ZGC (more efficient, default), virtual threads with different memory model (stacks on heap, not OS stacks), GC ergonomics changes. Test: existing -Xmx still works, virtual threads don't create heap pressure, libraries compatible with new threading."},

    {"level":6,"diff":1,"scenario":0,"q":"You're designing a fleet-wide memory reliability strategy. What should it include?","opts":["Just standardised -Xmx values","Standard JVM config, mandatory HeapDumpOnOutOfMemoryError, GC anomaly alerting, automated JFR on threshold breach, and fleet-wide per-service memory dashboards","Only Grafana dashboards","Only incident runbooks","Only automated restarts on high memory"],"ans":1,"fb":"Complete strategy: prevention (standard config), detection (anomaly alerting, growth trends), diagnosis (auto heap dump, triggered JFR), visibility (per-service dashboards, not averages). Shifts from reactive to proactive."},
    {"level":6,"diff":2,"scenario":0,"q":"Your fleet dashboard shows average JVM heap at 45% across 20 services. A stakeholder says 'memory is fine.' Why might this be misleading?","opts":["Averages are always accurate","The average hides outliers — one service at 95% with a growing leak is invisible when averaged with 19 at 40%. Show per-service MAX or P95 instead.","45% is indeed healthy","The dashboard measures wrong","Averages are fine with alerts"],"ans":1,"fb":"Fleet averages are dangerous. 19 healthy at 40% + 1 leaking at 95% = average 43%. Fix: per-service panels, MAX() as top indicator, alert on per-service P95 memory."},

    {"level":6,"diff":1,"scenario":1,"q":"How does GC behaviour translate into SLA latency commitments?","opts":["GC has no SLA impact","GC pauses directly add to P99 latency — a 100ms pause means any in-flight request experiences 100ms+ additional latency. Memory configuration must be validated against SLA targets.","Only heap size matters","SLAs should exclude GC","GC impact is too small"],"ans":1,"fb":"GC pauses are stop-the-world. If SLA promises P99 < 50ms but the service has 100ms pauses every 30s, ~3% of requests violate purely from GC. GC algorithm, heap sizing, and allocation rate optimisation are SLA engineering."},

    {"level":6,"diff":1,"scenario":2,"q":"When evaluating virtual threads (Java 21) for your Spring Boot services, what is the key memory implication?","opts":["Virtual threads use same memory as platform threads","Virtual threads have much smaller stacks (heap-allocated), enabling millions of tasks — but ThreadLocal-heavy libraries may leak memory per virtual thread","Virtual threads eliminate connection pools","Virtual threads reduce heap usage","Virtual threads are only for batch"],"ans":1,"fb":"Platform threads: ~1MB OS stack each. Virtual threads: tiny heap-allocated continuations. But: ThreadLocal per virtual thread can cause heap growth (millions of virtual threads x ThreadLocal copy). HikariCP, Spring Security use ThreadLocal extensively. Evaluation must include ThreadLocal audit."}
  ,
    {"level":1,"diff":1,"scenario":0,"q":"In Grafana, the 'Used Heap' panel shows the heap dropping to near zero right after a new deployment. What is the most likely explanation?","opts":["A memory leak was just fixed","The dashboard panel is broken","The service is idle","GC tuning is now more aggressive","The pod restarted as part of the deploy, so the new JVM started fresh"],"ans":4,"fb":"After a deploy, pods are recreated. A new JVM starts with an almost-empty heap and grows as work is processed. This is expected baseline behaviour, not a leak fix or GC tuning effect."},
    {"level":1,"diff":1,"scenario":0,"q":"Which Grafana panel is the best starting point if you want to know whether a Spring Boot pod is healthy from a memory standpoint?","opts":["Cloud SQL connections","Network traffic panel","Pod restart count only","Used Heap and GC pause panels for that pod","CPU usage panel"],"ans":3,"fb":"Used Heap shows whether memory is climbing or oscillating, and GC pauses show whether collection is keeping up. Together they give the most direct view of JVM memory health. The other panels are useful but not memory-specific."},
    {"level":1,"diff":2,"scenario":0,"q":"A Grafana 'Used Heap' panel shows a sawtooth that slowly steps higher each cycle: each trough is a bit higher than the last over 12 hours. What is this pattern called and what does it suggest?","opts":["JIT warm-up","Cache pre-loading","Healthy GC behaviour","A slow memory leak — the baseline of retained objects is growing","Container OOM imminent due to CPU throttling"],"ans":3,"fb":"A pure sawtooth at a flat baseline is healthy. When the baseline rises trough-over-trough, GC is running but cannot reclaim everything — long-lived references are accumulating. This is the classic shape of a slow leak."},
    {"level":1,"diff":2,"scenario":0,"q":"Two pods of the same service are shown side by side in Grafana. Pod A heap oscillates between 30-60%. Pod B heap sits flat at 85% with no oscillation. What is the most useful first observation?","opts":["Pod B is not garbage-collecting effectively or is under memory pressure — the lack of oscillation is suspicious","Pod B has more traffic","Pod B is healthier because the line is flatter","Pod A is restarting more often","They are equivalent"],"ans":0,"fb":"Healthy heaps oscillate as objects are allocated and reclaimed. A flat high line means GC cannot bring usage down — either retained objects are dominating or the heap is too small for the working set. It deserves investigation."},
    {"level":1,"diff":3,"scenario":0,"q":"You are looking at a Grafana panel for 'JVM heap committed' (not used). Committed sits at 1Gi and never moves, while 'Used' oscillates from 200MB to 800MB. A teammate worries the JVM is hoarding memory. What is the correct interpretation?","opts":["Committed heap is the memory the JVM has reserved from the OS — it normally grows to -Xmx and does not shrink. This is expected.","Used should equal committed for healthy services","The JVM is leaking 1Gi","GC is broken because committed is constant","The container limit is too high"],"ans":3,"fb":"Committed memory is the OS-backed heap the JVM has claimed. By default the JVM does not give it back even when used drops. Only Used heap reflects live allocations. Hoarding committed memory up to -Xmx is normal."},
    {"level":1,"diff":3,"scenario":0,"q":"In a Grafana panel labelled 'GC throughput', a value drops from 99% to 85%. What does this mean?","opts":["85% of the heap is full","GC ran 85 times in the interval","85% of objects survived GC","The application is running 85% of the time and GC is taking 15% of CPU time","GC paused for 85ms"],"ans":3,"fb":"GC throughput is the percentage of wall-clock time the application spent doing real work versus garbage collecting. Dropping from 99% to 85% means GC overhead grew significantly — usually a sign of pressure or oversized survivor traffic."},
    {"level":1,"diff":1,"scenario":1,"q":"Where should you first look to confirm that a pod was OOM-killed rather than crashed for another reason?","opts":["The application's error logs only","The Kafka consumer lag panel","The Cloud SQL logs","The Grafana CPU panel","kubectl describe pod — look for the Last State 'Terminated' with Reason 'OOMKilled' and exit code 137"],"ans":4,"fb":"kubectl describe pod (or the Kubernetes UI equivalent) is authoritative. The Reason field will say OOMKilled and the exit code will be 137 when the kernel killed the container for exceeding its memory limit."},
    {"level":1,"diff":1,"scenario":1,"q":"A pod's status reads 'CrashLoopBackOff' and the previous container exited with code 137. Which statement is correct?","opts":["Code 137 means a graceful shutdown","The Kubernetes scheduler intentionally evicted the pod","The pod's image is corrupted","The application threw an unhandled NullPointerException","The container was OOM-killed and Kubernetes is restarting it with backoff"],"ans":4,"fb":"Exit 137 = SIGKILL by the kernel OOM killer. CrashLoopBackOff is Kubernetes' response: restart with increasing delay. This pattern almost always points to a memory budget problem, not application logic."},
    {"level":1,"diff":2,"scenario":1,"q":"A teammate says 'the JVM throws OutOfMemoryError when the pod is OOMKilled'. Why is this incorrect?","opts":["OOMKill is the Linux kernel killing the container externally — the JVM does not get a chance to detect or log anything","Spring Boot intercepts OOMKill","Exit code 137 always corresponds to OutOfMemoryError","The JVM logs OOMKill events to /var/log","OutOfMemoryError is a CPU error"],"ans":0,"fb":"OOMKill is enforced by the Linux kernel via cgroup limits. When triggered, the process receives SIGKILL and terminates immediately with no chance to flush logs or throw a Java exception. OutOfMemoryError is an entirely separate, JVM-internal mechanism."},
    {"level":1,"diff":2,"scenario":1,"q":"After an OOMKill event, what minimum information should you collect before escalating to a senior engineer?","opts":["Only the heap dump","Pod name, namespace, time of kill, exit code, container limits, -Xmx setting, and a screenshot of the heap and container memory panels around the event","Pod name only","Only the application logs","Just the exit code"],"ans":1,"fb":"A senior engineer needs context to triage quickly. The pod identity, the timing, the limits and JVM flags, and a graph of memory leading up to the kill let them tell at a glance whether it's a sizing problem, a leak, or a load spike."},
    {"level":1,"diff":3,"scenario":1,"q":"A pod is OOMKilled. The heap usage at the time of death was at only 40% of -Xmx. Which conclusion is most reasonable?","opts":["The pod was not really killed","The heap reading was stale and heap was actually full","The OOMKill was a false positive","The Linux kernel was wrong","Non-heap memory (Metaspace, thread stacks, direct buffers, native) plus heap exceeded the container limit"],"ans":4,"fb":"A pod can be OOMKilled while the heap is healthy — the container limit covers the entire process. If non-heap regions grew (more threads, more direct buffers, more loaded classes), total RSS can exceed the limit even when heap is fine."},
    {"level":1,"diff":1,"scenario":2,"q":"Which Kubernetes manifest field sets the maximum memory the container is allowed to use?","opts":["env.JAVA_OPTS","resources.requests.memory","resources.limits.memory","containers[0].image","spec.replicas"],"ans":2,"fb":"resources.limits.memory is the cgroup hard cap. requests.memory is what the scheduler reserves and uses for placement, but it does not cap usage. JAVA_OPTS contains JVM flags but does not control container limits."},
    {"level":1,"diff":1,"scenario":2,"q":"What does the JVM flag -XX:MaxRAMPercentage=70 do?","opts":["Caps GC overhead at 70%","Reserves 70% of memory for the OS","Sets the JIT cache size to 70% of RAM","Sets the survivor ratio","Tells the JVM to size -Xmx as 70% of the available container memory"],"ans":4,"fb":"MaxRAMPercentage is the modern, container-aware way to set heap size. With UseContainerSupport on, the JVM reads the cgroup limit and computes -Xmx as that percentage. It is the standard alternative to hard-coding -Xmx."},
    {"level":1,"diff":2,"scenario":2,"q":"A deployment uses '-XX:MaxRAMPercentage=80' with no explicit -Xmx, and resources.limits.memory: 2Gi. Approximately how much heap will the JVM allocate at most, and is that safe?","opts":["About 1.6Gi — leaves only ~400MB for non-heap, which may or may not be enough depending on threads, buffers, and Metaspace","About 2Gi — equal to the limit, perfectly safe","Cannot be predicted","About 200MB — too small","About 800MB — wasted memory"],"ans":0,"fb":"80% of 2Gi is 1.6Gi for the heap, leaving roughly 400MB for everything else (Metaspace, thread stacks, direct buffers, JIT code cache, native). Whether that is safe depends on the service profile — for a busy service it can be tight."},
    {"level":1,"diff":2,"scenario":2,"q":"A deployment manifest contains '-Xms2g -Xmx2g'. What does setting -Xms equal to -Xmx achieve?","opts":["Saves CPU","Forces the JVM to commit the full heap immediately at startup, avoiding gradual growth and resize pauses","Disables GC","Doubles the heap","Makes the heap shrink dynamically"],"ans":1,"fb":"Setting -Xms equal to -Xmx fixes the heap size from start. The OS is asked for the full amount up front, eliminating heap-growth resize work and producing more predictable behaviour, especially in containers where bursty growth can collide with limits."},
    {"level":1,"diff":3,"scenario":2,"q":"You see only '-Xmx512m' in a deployment with no -Xms, no MaxRAMPercentage, and no UseContainerSupport-related flag. The container limit is 1Gi and the JVM is OpenJDK 17. What is true?","opts":["-Xmx is overridden by the container limit","The pod cannot start","The JVM will ignore the container limit","Java 17 cannot read cgroups","UseContainerSupport is on by default in Java 17, so the JVM is container-aware regardless"],"ans":4,"fb":"Since Java 8u191 and especially in Java 11+, -XX:+UseContainerSupport is the default. The JVM reads the cgroup limit automatically. An explicit -Xmx still wins over computed defaults if set."},
    {"level":1,"diff":1,"scenario":3,"q":"What is JIT compilation?","opts":["Kubernetes injecting bytecode at deploy time","The JVM converting hot bytecode methods into optimised native machine code while the app runs","The JVM compiling source files to bytecode at runtime","Liquibase executing migrations","Spring Boot generating proxies"],"ans":1,"fb":"Just-In-Time compilation watches which methods run frequently ('hot') and compiles them from bytecode to native code, with optimisations. This is why a JVM gets faster after running for a while — and why fresh pods are slower at first."},
    {"level":1,"diff":1,"scenario":3,"q":"Why does a freshly started Spring Boot pod respond slower to its first few requests than an established pod?","opts":["The container has slow disk","The pod has a memory leak","Spring Boot is broken","The JVM is in interpreted mode for hot paths until JIT compiles them, caches are empty, and connection pools may not be fully established","Kafka is throttling"],"ans":3,"fb":"Cold start latency is the combined effect of JIT not yet compiling hot methods, application caches being empty, and pools (HikariCP, Kafka client) not yet warmed. After a few thousand requests the JVM stabilises and latency settles."},
    {"level":1,"diff":2,"scenario":3,"q":"A team adds a more aggressive readiness probe that hits /actuator/health every 2 seconds during the first minute. They expect this to make pods 'ready faster'. Why does this not actually warm up the JVM?","opts":["Spring ignores probes","Readiness probes run on a different JVM","Actuator endpoints don't exercise business code paths, so JIT and caches don't get warmed for the methods that matter","Probes only check disk","Probes are blocked by the firewall"],"ans":2,"fb":"Readiness probes only verify a pod is ready to receive traffic. JIT compiles the methods that actually run, and your real endpoints are different from /actuator/health. A meaningful warm-up needs the real handlers to run."},
    {"level":1,"diff":2,"scenario":3,"q":"A pod's first response after startup takes 4 seconds, while subsequent ones take 80ms. Which warm-up factor is most likely contributing?","opts":["JIT compilation alone","DNS resolution","Class loading and Spring bean initialisation triggered lazily on the first request","Cloud SQL is slow","Kafka rebalancing"],"ans":2,"fb":"Lazy initialisation is a common cold-path cost. Many Spring components only fully initialise when first accessed (DataSource connection, message converters, security filters). The first request pays this one-time cost; later requests do not."},
    {"level":1,"diff":3,"scenario":3,"q":"A team wants to reduce cold start latency on GKE without changing the application. Which approach is most likely to help?","opts":["Disable JIT","Set CPU limit lower","Reduce -Xmx","Use a startup probe to give the JVM time to warm before liveness/readiness checks become active, and consider class-data sharing","Increase the readiness probe initial delay so probes don't run too early"],"ans":3,"fb":"Startup probes were designed for slow-starting applications. Combined with class-data sharing or AppCDS, they let the JVM finish initialising without being killed by aggressive liveness probes — both classic JVM cold start helpers."},
    {"level":1,"diff":1,"scenario":4,"q":"In simple terms, what is the JVM heap?","opts":["The disk used by the container","The thread stack","The CPU cache","Metaspace","The region of memory where Java objects are allocated and managed by the garbage collector"],"ans":4,"fb":"The heap is the memory area where every Java object instance lives. Garbage collection only operates on the heap. Other memory areas (stack, Metaspace, native) exist for different purposes."},
    {"level":1,"diff":1,"scenario":4,"q":"What happens when a Java method is called?","opts":["A new heap region is created","Metaspace expands","A stack frame is pushed onto the calling thread's stack — local variables and the return address live there","GC runs","A new pod is started"],"ans":2,"fb":"Each Java thread has its own stack. A method call pushes a frame onto that stack containing local variables and bookkeeping. When the method returns the frame is popped. Recursion that grows too deep leads to StackOverflowError."},
    {"level":1,"diff":2,"scenario":4,"q":"A teammate says 'a StackOverflowError happens when the heap is full'. Why is this wrong?","opts":["StackOverflowError means the disk is full","StackOverflowError is a CPU error","StackOverflowError only happens at compile time","StackOverflowError is unrelated to heap — it occurs when a thread's stack runs out of space, usually from excessive recursion","Heap and stack are the same thing"],"ans":3,"fb":"Stack memory is per-thread, separate from the heap. StackOverflowError is the JVM telling you a thread used more frames than fit in its stack — almost always unbounded recursion. The heap can be 99% empty at the same time."},
    {"level":1,"diff":2,"scenario":4,"q":"Which of these objects lives on the heap?","opts":["A new ArrayList<String> created by a service method","A method return address","A primitive int local variable in a method","A loop counter","A stack frame"],"ans":0,"fb":"Object instances created via 'new' are heap-allocated. Primitive locals and stack frames live on the thread stack. Distinguishing where things live is the foundation of reasoning about GC and OOM."},
    {"level":1,"diff":3,"scenario":4,"q":"A senior engineer says 'most of our garbage is short-lived, so generational GC works well'. What does generational GC mean in practice?","opts":["GC runs once per generation of the codebase","All objects are equal","Generations refer to JVM versions","The JVM groups objects by age — most allocations die young in the young generation, so GC focuses there with cheap, frequent collections","The heap is split into pods"],"ans":3,"fb":"Generational GC is built on the empirical observation that most objects die young. The JVM allocates new objects in a young generation, collects it cheaply and often, and only promotes long-lived survivors to the old generation, where collection is more expensive."},
    {"level":1,"diff":1,"scenario":4,"q":"What is Metaspace in the JVM?","opts":["A native (non-heap) memory region storing class metadata, method bytecode, and runtime structures","Disk-backed swap","A faster heap","Spring Boot's bean cache","A Kubernetes feature"],"ans":0,"fb":"Metaspace stores class definitions, method information, and runtime metadata. It lives in native memory, not the heap, and grows as classes are loaded. Apps that load many classes dynamically can exhaust Metaspace independently of heap."},
    {"level":1,"diff":2,"scenario":0,"q":"On a Grafana panel, 'GC pause time per minute' has been climbing slowly over a week from 200ms/min to 1.2s/min. The 'Used Heap' baseline is also climbing. What does this combination suggest?","opts":["Too much CPU","The panel is broken","A slow heap leak: more retained objects mean GC has more to scan and runs longer per minute","Better GC tuning","A healthy increase in traffic"],"ans":2,"fb":"As retained live data grows, GC has more to mark and copy. Both pause time and frequency tend to climb together. Combined with a rising heap baseline, this is the canonical leak pattern."},
    {"level":1,"diff":3,"scenario":1,"q":"You receive an alert: 'Pod restart count > 0 for service X'. The first thing you check is exit code, which is 0, and Kubernetes Reason 'Completed'. Is this an OOMKill?","opts":["Yes — exit 0 sometimes means OOM","No — exit code 0 with Reason Completed means the container exited normally, which is unusual but not OOM","Yes — all restarts are OOMKills","Yes if heap was full","Cannot tell"],"ans":1,"fb":"OOMKill is exit 137 with Reason OOMKilled. Exit 0 with Completed means the main process returned normally, which for a long-running service usually means the entrypoint script finished — a configuration bug, not memory."},
    {"level":1,"diff":3,"scenario":2,"q":"A service has resources.requests.memory: 512Mi and resources.limits.memory: 2Gi. Which is true about how the JVM, with -XX:MaxRAMPercentage=75, sizes the heap?","opts":["It uses 100% of requests","It is unaffected by container settings","It uses 75% of node memory","It uses 75% of requests = 384MB","It uses 75% of limits = 1.5Gi"],"ans":4,"fb":"With UseContainerSupport, the JVM reads the cgroup limit (limits.memory) for sizing decisions, not the request. With 2Gi limit and 75% percentage, -Xmx becomes ~1.5Gi. requests.memory affects scheduling but not the JVM."},
    {"level":1,"diff":2,"scenario":0,"q":"Which memory observation in Grafana is most likely a normal cold-start artefact, not a problem?","opts":["Pause times exceeding 2 seconds","Repeated OOMKill events","Steady upward heap drift over 24 hours","A one-hour flat-line at 95%","The first 60 seconds after a deploy show a brief spike in heap committed and GC activity that then settles"],"ans":4,"fb":"During startup, the JVM allocates initial structures, loads classes, and JIT-compiles hot paths — all of which produce a brief activity spike that settles quickly. Persistent drift, sustained high usage, OOMKills, and multi-second pauses are not normal."},
    {"level":1,"diff":2,"scenario":4,"q":"You see 'OutOfMemoryError: GC overhead limit exceeded'. What does this specific message mean?","opts":["The GC threads crashed","Metaspace is full","The JVM is spending more than ~98% of CPU time in GC while reclaiming less than 2% of heap — effectively the application is making no progress","The same as Java heap space","Native memory is exhausted"],"ans":2,"fb":"This message indicates a heap that is technically not full, but where GC churns continuously trying to free space. The JVM gives up rather than letting the app crawl forever. It usually points to a leak or grossly undersized heap."},
    {"level":2,"diff":1,"scenario":0,"q":"Which of these is a non-heap JVM memory region you must include when sizing a container?","opts":["Container image layers","Metaspace","Kubernetes API memory","Pod IP table","Cloud SQL storage"],"ans":1,"fb":"Metaspace is part of the JVM process memory but lives outside the heap. Together with thread stacks, direct byte buffers, JIT code cache, and native allocations, it must fit alongside -Xmx within the container limit."},
    {"level":2,"diff":1,"scenario":0,"q":"A service uses 100 Tomcat threads. Approximately how much memory do thread stacks alone consume?","opts":["About 1GB","About 10MB","About 10GB","Negligible — stacks are heap-allocated","About 100MB (each stack is about 1MB by default)"],"ans":4,"fb":"Default thread stack size on most JVMs is around 1MB (-Xss). 100 threads → roughly 100MB just in stack space, separate from the heap. Services with very high thread counts can spend hundreds of MB on stacks alone."},
    {"level":2,"diff":2,"scenario":0,"q":"A new microservice will run with a 2Gi container limit, expects ~150 Tomcat threads, ~20 HikariCP connections, ~100MB Metaspace, and ~150MB direct byte buffers. What is a reasonable -Xmx?","opts":["2Gi","About 1.4Gi — leaves headroom for ~150MB stacks + 100MB Metaspace + 150MB buffers + JIT code cache and native overhead","256MB","800MB","1.6Gi"],"ans":1,"fb":"Add up non-heap: 150MB (stacks) + 100MB (Metaspace) + 150MB (direct buffers) + ~80MB (JIT code cache) + native overhead ≈ 500–600MB. Heap should fit in the remaining ~1.4Gi. Setting -Xmx=2Gi would leave no room and OOMKill on first load."},
    {"level":2,"diff":2,"scenario":0,"q":"A team complains their service OOMKills after they doubled max connections in HikariCP from 10 to 20 — but heap utilisation is unchanged. Why?","opts":["The connections are not being closed","CPU limit is too low","HikariCP allocates on the heap","The probes are wrong","More connections means more direct buffers and per-connection state, plus driver native allocations — non-heap memory grew without affecting heap"],"ans":4,"fb":"Each open JDBC connection holds driver-side state, network buffers, and possibly OS file descriptors. Doubling connections grows non-heap memory without changing heap utilisation. If headroom was tight, this is enough to push past the container limit."},
    {"level":2,"diff":3,"scenario":0,"q":"Two services share a 1.5Gi container limit. Service A is single-threaded batch (8 threads). Service B is reactive with hundreds of small worker threads (300). They have identical -Xmx. Which is more likely to OOMKill, all else equal?","opts":["Service A","Both equally","Neither","Whichever has more CPU","Service B — its thread count alone consumes ~300MB of stack space, eating into the same headroom"],"ans":4,"fb":"Thread stack memory scales linearly with thread count. 300 threads × ~1MB = ~300MB consumed before any heap is touched. With identical -Xmx, Service B has far less headroom for non-heap and is more likely to be killed."},
    {"level":2,"diff":1,"scenario":1,"q":"What is the practical reason for the '-Xmx ≈ 60–70% of container limit' rule of thumb?","opts":["It is a Kubernetes requirement","GC requires 30% headroom","Java runs faster at 70%","CPU limits are tied to memory","Non-heap memory (Metaspace, thread stacks, direct buffers, JIT cache, native) typically needs 30–40% of the budget"],"ans":4,"fb":"The non-heap budget needs real space. A 60–70% heap leaves 30–40% for the rest of the JVM process. It is a starting heuristic, not an absolute — services with many threads may need more headroom, lean services may safely use a bit more."},
    {"level":2,"diff":2,"scenario":1,"q":"A service is configured with -Xmx=1Gi in a 1Gi container. It starts and runs for a few seconds before being OOMKilled every restart. What is happening?","opts":["The heap is leaking","The pod is mis-scheduled","The JVM commits the heap and immediately needs Metaspace and thread stacks on top — total exceeds 1Gi instantly","Kubernetes is broken","CPU is starved"],"ans":2,"fb":"With -Xmx equal to the container limit, the JVM has no room for any non-heap memory. As soon as Metaspace fills with classes and threads start, total RSS passes the limit and the kernel kills the process."},
    {"level":2,"diff":2,"scenario":1,"q":"A production service runs fine with -Xmx=1.4Gi in a 2Gi container most days. During a load spike, it OOMKills. Which adjustment most directly addresses the failure mode?","opts":["Add more replicas only","Disable GC","Increase -Xmx to 1.8Gi","Increase CPU","Lower -Xmx to 1.2Gi to give more headroom for non-heap growth under load"],"ans":4,"fb":"Under spike load, more threads, larger buffers, and busier JIT cache push non-heap up. Lowering -Xmx to leave more headroom is counter-intuitive but often correct. Adding replicas helps with throughput but doesn't fix per-pod sizing."},
    {"level":2,"diff":3,"scenario":1,"q":"A team resists lowering -Xmx because 'we need the heap for caches'. The service still OOMKills under load. What is a safer first move?","opts":["Disable caches entirely","Profile non-heap growth under load and either lower -Xmx or raise the container limit so total budget covers heap + observed non-heap","Restart pods more frequently","Set -Xmx higher than the limit","Switch to a different language"],"ans":1,"fb":"You cannot have a heap larger than the container minus non-heap. Either reduce -Xmx (and accept smaller cache), raise the container limit, or shrink non-heap (fewer threads, smaller buffers). Profiling makes the decision data-driven."},
    {"level":2,"diff":1,"scenario":2,"q":"A pod's container memory metric shows 1.6Gi used out of 2Gi limit. The JVM heap metric shows 600MB used. What accounts for the gap?","opts":["Kernel modules","Disk usage","Other pods","The heap is wrong","Non-heap JVM memory: Metaspace, thread stacks, direct buffers, JIT code cache, and native allocations"],"ans":4,"fb":"Container memory captures the entire process RSS. Heap is only one slice. The 1Gi gap is everything else the JVM uses. This pattern is common and normal up to a point — concerning only if the gap keeps growing."},
    {"level":2,"diff":2,"scenario":2,"q":"Container memory grows from 1.2Gi to 1.8Gi over several days while heap stays at 600MB. Which non-heap region is the most likely culprit, and how would you confirm?","opts":["Container image layers","DNS cache","Metaspace or direct buffers — read NMT (Native Memory Tracking) or Actuator memory.committed by area","Stack — count threads via JFR or jstack","CPU usage"],"ans":2,"fb":"With heap stable, the growth is non-heap. Metaspace (class loader leak) and direct buffers (NIO/Kafka leaks) are the usual causes. Native Memory Tracking or Spring Boot's /actuator/metrics jvm.memory.committed by area pinpoints which region is growing."},
    {"level":2,"diff":3,"scenario":2,"q":"A service exposes Actuator metrics. jvm.memory.used{area='nonheap', id='Metaspace'} grows by 5MB per day with no deployments. Which root cause is most plausible?","opts":["A class loader leak — possibly dynamic class generation (proxies, scripting, hot reload) without releasing old class loaders","Heap fragmentation","CPU leak","Network leak","Disk leak"],"ans":0,"fb":"Steady Metaspace growth without deploys means classes are accumulating. Common causes: dynamic proxy generation, scripting engines that recompile, broken hot-reload, or multiple application contexts created and never closed."},
    {"level":2,"diff":1,"scenario":3,"q":"After an OOMKill, you run 'kubectl describe pod' and see Last State Terminated, Reason OOMKilled, Exit Code 137. Which next file or signal should you correlate this against?","opts":["Kubernetes Service definition","Kafka topic configuration","Container memory and JVM heap Grafana panels around the kill timestamp","Container image manifest","Cloud SQL slow query log"],"ans":2,"fb":"With a confirmed OOMKill, the next question is whether heap or non-heap caused it. Correlating the exact kill time with the memory panels tells you whether heap was full or whether the gap (non-heap) ballooned."},
    {"level":2,"diff":2,"scenario":3,"q":"kubectl describe pod shows Reason OOMKilled with Exit Code 137 and Restart Count 18 in 24 hours. Heap is healthy throughout. What is the most likely class of problem?","opts":["CPU starvation","DNS","A heap leak","Either a non-heap leak or a chronic mis-sizing of -Xmx vs container limit","A bad image"],"ans":3,"fb":"Frequent OOMKills with healthy heap point to non-heap. Either the budget is wrong by design (config) or something is leaking outside the heap (Metaspace, direct buffers, threads). Both lead to the same investigation: profile non-heap."},
    {"level":2,"diff":2,"scenario":3,"q":"You see Reason: OOMKilled but the JVM also logged 'java.lang.OutOfMemoryError: Java heap space' just before. What does this indicate?","opts":["CPU throttling","Two unrelated errors","Both happened: the heap filled (JVM error) and the container limit was reached (kernel kill) in close sequence — most likely the heap was indeed the cause","An OOMKill always produces this error","A bug in Kubernetes"],"ans":2,"fb":"Sometimes the JVM detects heap exhaustion just before the kernel kills the container. When you see both signals, the cause is almost certainly heap — and the heap exhaustion likely drove total RSS over the cgroup limit."},
    {"level":2,"diff":3,"scenario":3,"q":"After an OOMKill, the JVM did not write a heap dump. The team set '-XX:+HeapDumpOnOutOfMemoryError'. Why might no heap dump have been produced?","opts":["The flag is ignored","Kubernetes deletes dumps","The dump directory is wrong","HeapDumpOnOutOfMemoryError only fires on Java OutOfMemoryError, not on kernel OOMKill, which kills the JVM with no chance to write","Java cannot write files"],"ans":3,"fb":"OOMKill is external to the JVM and arrives as SIGKILL, which cannot be caught. The JVM has no chance to write a heap dump. To capture state, you must trigger heap dumps before the kernel intervenes — typically on OutOfMemoryError or via on-demand jcmd."},
    {"level":2,"diff":1,"scenario":4,"q":"What does -XX:+UseContainerSupport do, and is it on by default in Java 17?","opts":["Enables container probes","Enables Liquibase","Compiles AOT code","Tells the JVM to read cgroup memory and CPU limits when computing defaults; on by default since Java 8u191/Java 11","Pulls images from a registry"],"ans":3,"fb":"UseContainerSupport makes the JVM container-aware. Without it, the JVM would see the host node's memory and over-size itself. It is on by default in modern Java; don't turn it off."},
    {"level":2,"diff":1,"scenario":4,"q":"What is the difference between resources.requests.memory and resources.limits.memory in Kubernetes?","opts":["They are the same","requests is for scheduling; limits is the cgroup hard cap above which the kernel will OOMKill","Both are advisory","Neither affects memory","limits is for scheduling, requests is the cap"],"ans":1,"fb":"requests.memory is what the scheduler reserves to place the pod. limits.memory is what the kernel enforces — exceed it and the container is killed. Setting requests = limits gives the most predictable behaviour but reduces flexibility for the scheduler."},
    {"level":2,"diff":2,"scenario":4,"q":"A pod has requests.memory=512Mi and limits.memory=2Gi. The JVM sets -Xmx based on the limit. Why might setting requests well below the limit be risky?","opts":["The pod can be scheduled on a node that doesn't actually have 2Gi free, leading to node-level memory pressure and potential pod evictions","The JVM ignores requests","It always causes OOM","Kubernetes doubles the request","Java cannot read requests"],"ans":0,"fb":"Requests influence scheduling. If you set requests low but the pod actually uses near the limit, you can over-commit a node. Under pressure, the kubelet may evict pods. Aligning requests closer to actual usage avoids surprising evictions."},
    {"level":2,"diff":2,"scenario":4,"q":"Setting requests.memory equal to limits.memory results in the pod being assigned which Kubernetes QoS class?","opts":["BestEffort","Hot","Cheap","Burstable","Guaranteed"],"ans":4,"fb":"requests == limits for both CPU and memory yields Guaranteed QoS. Guaranteed pods are last to be evicted under node pressure. For latency-sensitive services this is often desirable."},
    {"level":2,"diff":3,"scenario":4,"q":"A team uses requests.memory=1Gi, limits.memory=2Gi for predictability but encounters frequent evictions on busy nodes. What is the trade-off they should consider?","opts":["Switch to Guaranteed by setting requests = limits = 2Gi, accepting higher reservation cost in exchange for stability","Move to a different cluster","Disable QoS","Lower the limit","Increase replicas only"],"ans":0,"fb":"Burstable QoS pods are more vulnerable to eviction when nodes are under memory pressure. If stability matters more than scheduling efficiency, raising requests to match limits buys Guaranteed QoS at the cost of reserving more capacity."},
    {"level":2,"diff":1,"scenario":0,"q":"Roughly how much memory does Metaspace consume for a typical Spring Boot 3 service?","opts":["Negligible","Always 1GB","A few MB","Equal to heap","Roughly 80–200MB once classes are loaded"],"ans":4,"fb":"Spring Boot loads thousands of classes from itself, dependencies, and generated proxies. Metaspace typically settles between 80 and 200MB, depending on dependencies. It is a non-trivial slice of any sizing budget."},
    {"level":2,"diff":2,"scenario":0,"q":"A new service uses Netty for outbound HTTP calls. After enabling it, container memory grew by 200MB but heap stayed flat. Where did the memory go?","opts":["Disk","Metaspace","Direct byte buffers — Netty uses off-heap pooled buffers for I/O","Cloud SQL","Stack"],"ans":2,"fb":"Netty allocates pooled direct ByteBuffers for I/O. They live in native memory, not the heap, so heap metrics are unaffected. The buffer pool can be sized via -Dio.netty.maxDirectMemory and observed via NMT or specific Netty metrics."},
    {"level":2,"diff":3,"scenario":0,"q":"You suspect a service is leaking direct byte buffers. Which JVM flag enables tracking allocations from native memory regions?","opts":["-XX:+PrintGCDetails","-XX:+UseG1GC","-XX:NativeMemoryTracking=summary or =detail","-Xss","-XX:+PrintHeapDump"],"ans":2,"fb":"NativeMemoryTracking instruments JVM allocations from native memory. With it on you can use 'jcmd <pid> VM.native_memory summary' to see memory by category and detect growth in Internal, Direct Buffers, or Class regions."},
    {"level":2,"diff":2,"scenario":1,"q":"Which configuration is safest for a Spring Boot service in a 4Gi container with ~200 threads, ~150MB Metaspace expected, 200MB direct buffers, and JIT cache around 100MB?","opts":["-Xmx=1Gi","-Xmx=3Gi (leaves ~1Gi headroom for stacks/Metaspace/buffers/JIT/native)","-Xmx=512m","-Xmx=3.8Gi","-Xmx=4Gi"],"ans":1,"fb":"Non-heap budget: 200MB stacks + 150MB Metaspace + 200MB buffers + 100MB JIT + ~150MB native ≈ 800MB–1Gi. -Xmx=3Gi leaves the headroom safely. -Xmx=3.8Gi may work briefly but leaves no margin for spikes."},
    {"level":2,"diff":3,"scenario":2,"q":"A service shows committed Metaspace growing to 600MB and OOMing with 'OutOfMemoryError: Metaspace'. The team did not set -XX:MaxMetaspaceSize. What changed?","opts":["MaxMetaspaceSize is unlimited by default, so this is something else (a class loader leak driving uncontrolled growth)","Heap is full","Disk is full","CPU is throttled","Metaspace is always 600MB"],"ans":0,"fb":"Without MaxMetaspaceSize, Metaspace can grow until native memory is exhausted. Hitting OOM Metaspace usually means classes are accumulating — a class loader leak — not just normal growth. Setting MaxMetaspaceSize forces failure earlier and easier to diagnose."},
    {"level":2,"diff":1,"scenario":3,"q":"You run kubectl describe pod and see 'Last State: Terminated, Exit Code: 137'. Which one number do you mention to a senior on-call as the unambiguous OOMKill signal?","opts":["Exit code 137 with Reason OOMKilled","Restart count","The pod name","Pod IP","Image SHA"],"ans":0,"fb":"137 (= 128 + 9, SIGKILL) plus Reason: OOMKilled is unambiguous evidence of an OOMKill. Sharing this lets the on-call skip ambiguous troubleshooting and go straight to memory analysis."},
    {"level":3,"diff":1,"scenario":0,"q":"In a Spring Boot service, which of the following is the largest fixed-size off-heap memory consumer per active database connection in HikariCP?","opts":["The Liquibase change log","The Java HikariDataSource bean","Spring's bean cache","The JDBC driver's network and prepared statement state","Thread stacks"],"ans":3,"fb":"Each open JDBC connection holds driver-side state: TLS buffers, network buffers, and prepared statement caches. This adds up quickly with large pool sizes and is invisible to heap metrics."},
    {"level":3,"diff":1,"scenario":0,"q":"Which Spring Boot setting most directly controls the number of concurrent request-handler threads in a default Tomcat-based service?","opts":["spring.datasource.hikari.maximum-pool-size","management.server.port","server.tomcat.max-connections","server.tomcat.threads.max","spring.task.execution.pool.max-size"],"ans":3,"fb":"server.tomcat.threads.max sets the maximum worker thread count for Tomcat. Each thread holds a stack (~1MB) and contributes to concurrency limits. max-connections is the accept-queue cap, not the thread pool."},
    {"level":3,"diff":2,"scenario":0,"q":"A team's service has Tomcat threads.max=400, hikari pool=100, kafka.max.poll.records=500, two consumers with 64MB fetch.max.bytes. Roughly which non-heap region is dominant?","opts":["Stacks — 400 threads × ~1MB ≈ 400MB","JIT code cache","Metaspace","HikariCP heap usage","Kafka direct buffers — about 128MB total"],"ans":0,"fb":"At 400 threads, stacks alone are ~400MB — usually the largest non-heap consumer in such a profile. Kafka buffers add roughly 128MB. Right-sizing Tomcat threads is often the highest-leverage change for memory."},
    {"level":3,"diff":2,"scenario":0,"q":"A service uses Caffeine cache with maximumSize=1_000_000 entries, each averaging 2KB. What is the steady-state heap impact of the cache when full?","opts":["Cannot estimate","~2GB","~2KB","~2MB","Negligible"],"ans":1,"fb":"1,000,000 × 2KB = 2GB. Caches with large maximumSize values are easy to underestimate. Always do back-of-envelope sizing before deploying caches; Caffeine respects maximumSize but cannot prevent you from setting an unaffordable one."},
    {"level":3,"diff":3,"scenario":0,"q":"A Spring Boot service uses an in-process Caffeine cache and a HikariCP pool of 50. Average heap usage is fine, but Old Gen grows steadily. Which consumer is most consistent with this pattern?","opts":["HikariCP","Caffeine cache entries that survive enough GCs to be promoted to Old Gen","Direct buffers","JIT code cache","Tomcat threads"],"ans":1,"fb":"Long-lived cache entries are typical Old Gen residents. As they accumulate (or eviction is loose), Old Gen grows. HikariCP allocations are mostly off-heap. Tomcat threads do not directly fill Old Gen. The cache is the natural suspect."},
    {"level":3,"diff":1,"scenario":1,"q":"Spring Boot Actuator exposes /actuator/metrics/jvm.memory.used. Which tag distinguishes heap from non-heap?","opts":["kind","area (heap, nonheap)","scope","class","level"],"ans":1,"fb":"The 'area' tag separates heap from nonheap. Combined with 'id' (e.g., G1 Eden, Metaspace, Compressed Class Space), it tells you exactly where memory is being used inside the JVM."},
    {"level":3,"diff":2,"scenario":1,"q":"You query jvm.memory.used{area='nonheap', id='Compressed Class Space'} and see steady growth over 14 days. What does this region store and what is the implication?","opts":["Direct buffers","Old Gen overflow","Thread stacks","Class metadata pointers; growth points to dynamically loaded classes — possibly a class loader leak","JIT cache, no concern"],"ans":3,"fb":"Compressed Class Space holds compressed class pointers. Growth indicates classes are being loaded faster than they are unloaded. Combined with growing Metaspace, this is a strong indicator of a class loader leak."},
    {"level":3,"diff":3,"scenario":1,"q":"In Actuator metrics, jvm.gc.pause has a tag 'cause'. You see most pauses tagged 'G1 Evacuation Pause' and a few 'G1 Humongous Allocation'. Why does the second cause matter?","opts":["It is unrelated to memory","Humongous allocations (>50% of region size) bypass normal allocation paths and can fragment the heap, often hinting at a code path allocating very large arrays","It indicates network issues","It is purely informational","It means GC is broken"],"ans":1,"fb":"G1 treats objects larger than half a region as 'humongous'. They are allocated in dedicated regions and are inefficient. Frequent humongous causes usually point to a specific code path allocating large arrays — worth investigating."},
    {"level":3,"diff":1,"scenario":2,"q":"Which OutOfMemoryError message is produced when the JVM cannot allocate a new native thread?","opts":["Java heap space","unable to create native thread","Metaspace","GC overhead limit exceeded","Direct buffer memory"],"ans":1,"fb":"'unable to create native thread' means the OS refused thread creation. Common causes: per-process thread/PID limits, kernel.threads-max, or memory exhaustion preventing stack allocation. It is a non-heap problem despite the OOM prefix."},
    {"level":3,"diff":2,"scenario":2,"q":"Which of these OOMs is most likely caused by a code-level memory leak rather than a sizing problem?","opts":["Direct buffer memory","Compressed class space","Metaspace","unable to create native thread","Java heap space, with the heap baseline trending up over weeks"],"ans":4,"fb":"A heap whose baseline keeps rising for weeks is the textbook memory leak signature. The other forms can be code leaks too, but they more often reflect configuration limits being too low or dynamic class loading."},
    {"level":3,"diff":3,"scenario":2,"q":"A service is hitting 'OutOfMemoryError: Direct buffer memory'. Which JVM option directly limits this region?","opts":["-XX:ReservedCodeCacheSize","-XX:MaxMetaspaceSize","-Xss","-XX:MaxDirectMemorySize","-Xmx"],"ans":3,"fb":"Direct buffer memory is bounded by -XX:MaxDirectMemorySize (default ≈ -Xmx if unset). Setting it explicitly both protects against runaway allocation and makes the limit visible to ops."},
    {"level":3,"diff":1,"scenario":3,"q":"Which of these patterns in a PR is the clearest memory red flag during code review?","opts":["A field of type Map<String, Object> with no eviction or maximum size, used to cache results indefinitely","A new test","A new private method","A new logging statement","A new bean"],"ans":0,"fb":"Unbounded caches are one of the most common production memory leaks. Reviewers should flag any long-lived collection that grows with input but lacks a cap, eviction, or TTL."},
    {"level":3,"diff":2,"scenario":3,"q":"In code review you see: ThreadLocal<HeavyContext> CTX = new ThreadLocal<>(); used inside a Tomcat request thread, with no remove() in a finally block. What is the risk?","opts":["No risk","Compilation error","Thread safety only","Slow start","HeavyContext is retained on the thread until the thread dies; thread pools recycle threads, so the value lingers across unrelated requests and can leak memory"],"ans":4,"fb":"ThreadLocals on long-lived pool threads must be cleared after use, otherwise the value lives until the thread dies. This is a frequent leak source in pooled servers and is invisible to typical monitoring."},
    {"level":3,"diff":2,"scenario":3,"q":"A code change introduces a method that accumulates results into a List inside a loop reading millions of database rows, then returns the list. Why is this a memory risk worth flagging?","opts":["Returning a list is bad style","Lists are slow","Lists cannot hold many items","Database access is forbidden","The full result set is held in heap simultaneously; it should stream or paginate"],"ans":4,"fb":"Materialising large result sets into in-memory collections is a common cause of OOM. Streaming with cursors, batching, or pagination keeps memory bounded regardless of result size."},
    {"level":3,"diff":3,"scenario":3,"q":"A PR adds @Async to a method that is called from a tight loop processing webhook events. What memory risk should you raise?","opts":["@Async is forbidden","If the executor uses an unbounded queue (the default), tasks accumulate faster than they are processed and the queue grows without limit, eventually filling heap","Spring will fail to start","Logs will be missing","CPU usage will rise"],"ans":1,"fb":"Spring's default async executor is a SimpleAsyncTaskExecutor or a ThreadPoolTaskExecutor with an unbounded queue. Under back-pressure, queued tasks accumulate in heap. Always configure a bounded queue and a rejection policy."},
    {"level":3,"diff":1,"scenario":4,"q":"A Spring Boot service has -Xmx=1Gi, max Tomcat threads=200, hikari pool=20, Metaspace ~150MB. Estimate total RSS at steady state.","opts":["About 1.5Gi","Cannot estimate","About 1Gi","About 4Gi","About 1.6–1.8Gi (1Gi heap + ~200MB stacks + ~150MB Metaspace + ~100MB JIT + native overhead)"],"ans":4,"fb":"Adding the slices: 1Gi (heap) + 200MB (stacks) + 150MB (Metaspace) + ~100MB (JIT code cache) + native overhead → ~1.6–1.8Gi RSS. This is the realistic container budget the service requires."},
    {"level":3,"diff":2,"scenario":4,"q":"A service plans to run in a 1.5Gi container with 250 threads and 200MB Caffeine cache. What is the maximum sensible -Xmx?","opts":["~1.5Gi","~1.2Gi","256MB","About 950MB — leaves ~250MB for stacks plus ~150MB Metaspace plus JIT and native; the cache also lives inside the heap and counts against -Xmx","Equal to RAM"],"ans":3,"fb":"250 thread stacks ≈ 250MB; Metaspace ≈ 150MB; JIT ≈ 80MB; native ≈ 100MB → ~580MB non-heap. 1.5Gi - 580MB ≈ 950MB. Note the 200MB cache is heap-allocated and lives inside that 950MB, not on top."},
    {"level":3,"diff":3,"scenario":4,"q":"A team wants to halve memory cost by moving from 2Gi to 1Gi containers. The JVM has -Xmx=1.4Gi today and Metaspace ~120MB. What is the most concrete blocker to overcome before the cut?","opts":["The JVM cannot run in 1Gi","-Xmx must be reduced to fit (e.g., 600MB or less), and the team must verify the working set, GC behaviour, and cache budgets still meet SLA at the smaller heap","The deployment pipeline cannot deploy 1Gi pods","CPU must be doubled","Java itself is too large"],"ans":1,"fb":"The cut forces -Xmx down. The real question is whether the application can serve traffic with the smaller working set without GC thrashing. That requires load testing and possibly redesigning caches — the technical debt the cut surfaces."},
    {"level":3,"diff":1,"scenario":0,"q":"Which Spring Boot subsystem is the most common silent off-heap consumer when scaling out HTTP server threads?","opts":["Liquibase","Bean container","Spring AOP","Tomcat thread stacks (each ~1MB) plus per-thread NIO buffers","JIT code cache"],"ans":3,"fb":"Each Tomcat worker thread has ~1MB of stack and may hold per-thread NIO buffers. Doubling thread count doubles this slice. Reviewing thread pool sizes is one of the highest-impact memory levers."},
    {"level":3,"diff":2,"scenario":1,"q":"You expose Actuator metrics in Prometheus. Which combination best detects an early class loader leak?","opts":["CPU + memory","GC pauses only","Just heap used","Disk + network","jvm.memory.used{area='nonheap',id='Metaspace'} trend together with jvm.classes.loaded.classes"],"ans":4,"fb":"A class loader leak shows up as both Metaspace growth and a steadily climbing class count. Tracking them together makes the leak obvious before it ends in OOM Metaspace."},
    {"level":3,"diff":3,"scenario":2,"q":"Why does 'OutOfMemoryError: unable to create native thread' often appear suddenly with no warning, even when both heap and Metaspace are healthy?","opts":["It is random","It is caused by GC","It is a Spring bug","Thread creation depends on native memory and OS limits (pids, threads-max, RLIMIT_NPROC). Crossing those limits is binary and unrelated to JVM heap metrics.","It only happens at startup"],"ans":3,"fb":"Thread creation lives outside the JVM heap budget. Limits are kernel- and process-level. Crossing them is sudden and not visible in any heap-focused dashboard. Mitigation is bounding pool sizes and right-sizing the OS."},
    {"level":3,"diff":3,"scenario":3,"q":"A reviewer sees a new endpoint that loads an entire CSV upload into memory via Files.readAllBytes() and parses it with String.split. What memory risk applies?","opts":["For large files, both the byte[] and the resulting array of strings live on the heap simultaneously, multiplying the input size in memory","String.split is slow","No risk","Encoding issue","Compilation error"],"ans":0,"fb":"Reading the whole file plus splitting can produce 3–4x the file size in heap due to UTF-16 strings, char[] copies, and intermediate arrays. Streaming line-by-line keeps memory bounded regardless of file size."},
    {"level":3,"diff":1,"scenario":4,"q":"For a small Spring Boot service with -Xmx=256m, ~50 threads, expected Metaspace ~80MB, which container memory limit is the smallest realistic choice?","opts":["About 512Mi (256MB heap + 50MB stacks + 80MB Metaspace + ~50MB JIT and native)","256Mi","2Gi","1Gi","384Mi"],"ans":0,"fb":"Sum the slices: 256 + 50 + 80 + 50 ≈ 436MB. 512Mi gives a small but workable safety margin. 384Mi is too tight; 256Mi is impossible because it equals -Xmx alone."},
    {"level":3,"diff":2,"scenario":4,"q":"A service is moving from 500 RPS to 2000 RPS. Which non-heap region is most likely to require a dedicated re-sizing decision?","opts":["JIT code cache","Thread stacks, since handling more concurrency typically grows the worker pool","Compressed class space","Metaspace","Survivor space"],"ans":1,"fb":"More concurrency means more in-flight requests, which typically translates to more worker threads. Stack memory scales linearly with thread count and frequently dominates the non-heap budget at high concurrency."},
    {"level":3,"diff":3,"scenario":0,"q":"A service with 800 Tomcat threads is migrated to virtual threads (Java 21). Heap usage grows by 30%. Why?","opts":["The test environment is broken","Per-task continuations are heap-allocated; thousands of in-flight requests can move what was previously stack memory into the heap","CPU went up","GC tuning is wrong","Virtual threads use the same memory as platform threads"],"ans":1,"fb":"Virtual threads dramatically reduce stack memory because each is a small heap-allocated continuation. Total memory for the same workload usually drops, but the breakdown shifts: less off-heap stack, more heap. Heap budgets must be re-tuned."},
    {"level":3,"diff":2,"scenario":3,"q":"A reviewer flags a PR that adds a synchronized static Map<String, byte[]> as a 'simple cache'. Beyond unbounded growth, what other concern should be raised?","opts":["Inheritance break","Compilation issue","Synchronized is too fast","Static collections live for the entire JVM lifetime and are pinned in Old Gen, retaining their entries through every GC cycle","Maps are not Serializable"],"ans":3,"fb":"Static collections cannot be released by GC because the class is reachable for the JVM's lifetime. Combined with no eviction, this guarantees unbounded growth and Old Gen retention. Both are flagged."},
    {"level":3,"diff":1,"scenario":1,"q":"Which Actuator endpoint is the most useful single place to inspect JVM memory areas in a running Spring Boot service?","opts":["/actuator/health","/actuator/metrics, querying jvm.memory.used and jvm.memory.max with their tags","/actuator/beans","/actuator/info","/actuator/env"],"ans":1,"fb":"jvm.memory.used and jvm.memory.max under /actuator/metrics expose every JVM memory pool with tags. Querying them gives a precise picture: heap vs non-heap, by pool name, in real time."},
    {"level":4,"diff":1,"scenario":0,"q":"You take a heap dump from a leaking service and open it in Eclipse MAT. Which view is the natural starting point?","opts":["Dominator Tree","Class loaders","Histogram","Threads view","OQL"],"ans":0,"fb":"The Dominator Tree shows objects that exclusively retain large amounts of memory. Sorted by retained heap, it usually surfaces the leaking root within minutes. The histogram is a useful complement when the dominator does not point obviously."},
    {"level":4,"diff":2,"scenario":0,"q":"In Eclipse MAT, you find a single ConcurrentHashMap retaining 600MB on the dominator tree. Inspecting its key/value class shows User → SessionContext. Which next analysis confirms a leak vs an oversized cache?","opts":["Use jstack","Open the JVM logs","Use 'Path to GC Roots — exclude weak/soft references' to see what is keeping the map alive and check the entry count vs expected user count","Restart the pod","Look at retained size only"],"ans":2,"fb":"Path to GC Roots reveals whether the map is held by a static or a long-lived bean (a leak signature) versus a Cache abstraction with a configured size. If entries vastly exceed the expected user count, it is a leak."},
    {"level":4,"diff":3,"scenario":0,"q":"A heap dump's dominator tree shows 70% retained by class 'org.springframework.cglib.proxy.Enhancer'. Heap dumps from a healthy pod do not show this. What hypothesis fits best?","opts":["CGLib is generating proxies repeatedly without releasing — class loader leak via dynamic proxying","CPU is starved","Spring is broken","Heap is too small","CGLib is allocating off-heap"],"ans":0,"fb":"CGLib generates new classes for proxies. If application code creates new application contexts or scopes that generate fresh proxies without cleaning up the old ones, those generated classes accumulate via class loaders. This is a classic class loader leak path."},
    {"level":4,"diff":1,"scenario":1,"q":"A G1GC log line reads '[GC pause (G1 Evacuation Pause) (young), 0.0420 seconds]'. What does this mean?","opts":["A failure","A safepoint poll","A full GC of 42ms","A concurrent mark","A young-generation evacuation pause taking 42ms"],"ans":4,"fb":"G1 evacuates live objects from young-gen regions in stop-the-world pauses. 42ms is typical for a healthy young pause. Watching pause length and frequency tells you whether allocation rate and heap size are well matched."},
    {"level":4,"diff":2,"scenario":1,"q":"GC logs show steadily growing 'Heap before GC' values across many cycles, even after young collections. Which metric should you compute next?","opts":["Promotion rate (bytes promoted to Old Gen per second)","JIT compilation count","CPU usage","Network bandwidth","Disk I/O"],"ans":0,"fb":"Steady heap growth despite frequent young collections means objects are surviving long enough to be promoted. Promotion rate quantifies how fast Old Gen fills, which lets you predict full collections and identify leaks vs working-set growth."},
    {"level":4,"diff":3,"scenario":1,"q":"GC logs show many 'to-space exhausted' events. What does this indicate and what is the typical fix?","opts":["G1 ran out of free regions to evacuate live objects into; common fixes are increasing heap, increasing G1ReservePercent, or reducing allocation rate","Corrupt logs","Network issue","Disk full","JIT problem"],"ans":0,"fb":"To-space exhaustion forces G1 into more expensive collections (sometimes Full GC). Solutions include enlarging the heap, increasing the reserve, or addressing allocation pressure. It is a strong sign the heap is near capacity for the workload."},
    {"level":4,"diff":1,"scenario":2,"q":"You need to capture a JFR recording on a running production JVM. Which command works without restart?","opts":["kill -9","Edit the deployment","Restart the pod","Reduce -Xmx","jcmd <pid> JFR.start name=trace duration=120s filename=trace.jfr"],"ans":4,"fb":"jcmd JFR.start triggers a recording in-process without requiring a JVM restart. Modern JDKs include JFR by default. A 60–120s recording is usually enough to characterise allocation hotspots and GC behaviour."},
    {"level":4,"diff":2,"scenario":2,"q":"In JFR, the 'Allocation in new TLAB' event for a 60s recording lists one stack frame (a JSON serializer) as 60% of allocations. What action is most appropriate?","opts":["Reduce thread count","Investigate that serializer: maybe it's allocating large temporary buffers or recreating objects unnecessarily","Ignore — JFR is unreliable","Disable JFR","Increase -Xmx"],"ans":1,"fb":"JFR's allocation events directly identify where the JVM creates pressure on GC. Concentrated allocation in one site is the easiest optimisation lever. Pre-allocating buffers, reusing instances, or switching to streaming often makes a measurable difference."},
    {"level":4,"diff":3,"scenario":2,"q":"A JFR recording shows allocation rate of 1.2 GB/s. The heap is only 4 GB. What does this combination tell you about GC pressure?","opts":["Very high allocation churn — young generation will fill rapidly and force frequent collections, raising overhead and pause frequency","No pressure","Low pressure","Disk bound","CPU bound"],"ans":0,"fb":"High allocation rate relative to heap size produces frequent young collections. Even if individual pauses are short, the cumulative GC overhead grows. Either reduce allocation churn or grow the heap so collections happen less often."},
    {"level":4,"diff":1,"scenario":3,"q":"You take a thread dump with jstack. Hundreds of threads are in state BLOCKED waiting on the same monitor. What does this indicate?","opts":["Memory leak","CPU starvation","Network slowness","Lock contention on a single object — likely a bottleneck","Healthy concurrency"],"ans":3,"fb":"Many threads BLOCKED on the same monitor is the classic signature of contention. The next step is to identify which object is being locked and whether the critical section can be reduced or replaced with a lock-free structure."},
    {"level":4,"diff":2,"scenario":3,"q":"A thread dump shows all 100 HikariCP-managed worker threads in WAITING on getConnection. What conclusion is most likely?","opts":["Connection pool exhaustion — connections are not being released or the pool is too small for the workload","Deadlock","Network bandwidth","CPU bound","JVM bug"],"ans":0,"fb":"Threads waiting in getConnection means no connection is available. Causes: a leak (connections not closed), long-running queries holding connections, or pool size too small. Investigation starts with leak detection and slow query analysis."},
    {"level":4,"diff":3,"scenario":3,"q":"Hundreds of threads sit in BLOCKED on a synchronized method of an internal logging utility class. What is the likely architectural issue?","opts":["DNS is slow","A poorly chosen lock granularity in the logger creates a global serialization point under load","Heap is full","Logging is fast","CPU is high"],"ans":1,"fb":"Logging is invoked by every request. A coarse-grained lock around log writes serialises the whole service. Modern loggers use lock-free or per-appender concurrency. Migrating or fixing the lock granularity removes the bottleneck."},
    {"level":4,"diff":1,"scenario":4,"q":"What is the difference between Metaspace and Compressed Class Space in modern HotSpot JVMs?","opts":["They are the same","Compressed Class Space is for heap","Compressed Class Space is for direct buffers","They are unrelated","Compressed Class Space stores compressed class pointers and is a sub-region within Metaspace by default, with its own size limit (-XX:CompressedClassSpaceSize)"],"ans":4,"fb":"Compressed Class Space uses smaller pointers when heap is under 32GB. It is a slice of Metaspace with its own size cap. Both can be observed in /actuator/metrics, and both can leak independently."},
    {"level":4,"diff":2,"scenario":4,"q":"A service shows MaxMetaspaceSize defaulting to unlimited and Metaspace climbing day over day. What is the diagnostic next step before assuming a class loader leak?","opts":["Check whether classes are being loaded that should be unloaded — list class loaders via jcmd VM.classloader_stats and look for many small loaders","Rewrite the service","Increase the heap","Remove dependencies","Restart the pod"],"ans":0,"fb":"VM.classloader_stats lists class loaders and how many classes each holds. Many small or duplicate loaders indicate dynamic generation without unloading — the fingerprint of a leak. This step proves the hypothesis before pursuing fixes."},
    {"level":4,"diff":3,"scenario":4,"q":"A service uses Groovy scripts that are recompiled at runtime to evaluate user-supplied rules. Metaspace grows by 200MB per day. What is the architectural fix, not just a tuning fix?","opts":["Increase MaxMetaspaceSize","Increase heap","Cache compiled scripts by content hash so identical scripts are not recompiled, ensuring class definitions are stable and unloadable","Disable Groovy","Reboot daily"],"ans":2,"fb":"Recompiling identical content creates a new class loader and class definition each time. Caching by content hash means a given script compiles once. Without this, no MaxMetaspaceSize is large enough to hide the underlying churn."},
    {"level":4,"diff":1,"scenario":0,"q":"In Eclipse MAT, the 'Leak Suspects' report points to two suspects but the second is 5x larger. Which should you investigate first?","opts":["Neither","The smaller one","The larger one — it represents more retained memory and is more likely the root cause","Random","Both equally"],"ans":2,"fb":"MAT ranks leak suspects by retained heap. The larger retained size is almost always the more impactful leak. Start there; the smaller suspect can be revisited if needed."},
    {"level":4,"diff":2,"scenario":1,"q":"GC logs show concurrent mark cycles starting back-to-back. What does this say about the heap?","opts":["Network is busy","Heap is too large","GC is broken","CPU is too low","The heap is filling fast enough that concurrent marking cannot keep up between cycles — Old Gen is under sustained pressure"],"ans":4,"fb":"Back-to-back concurrent marks indicate G1 cannot reclaim Old Gen as fast as it fills. If this persists, full collections become likely. Either reduce promotion rate or grow the heap."},
    {"level":4,"diff":3,"scenario":2,"q":"A JFR recording shows TLAB allocations dominated by byte[] of size ~1MB created in a JSON parsing path. What is the most surgical fix?","opts":["Switch to XML","Increase heap","Reuse a single, thread-local byte buffer for JSON parsing instead of allocating per-call","Disable JSON","Add more replicas"],"ans":2,"fb":"Parsing-time allocations are easy wins because they are short-lived but high-volume. Reusing a buffer (with a thread-local or a pool) cuts allocation pressure dramatically without changing semantics."},
    {"level":4,"diff":2,"scenario":3,"q":"You want to detect a HikariCP connection leak in production with minimal risk. Which configuration helps?","opts":["Set hikari.leakDetectionThreshold=30000 — HikariCP logs the stack trace of any connection held longer than the threshold","Disable HikariCP","Reduce pool size","Restart pods","Disable logging"],"ans":0,"fb":"leakDetectionThreshold turns suspected leaks into actionable stack traces. Set it just above your normal max connection hold time so it does not produce false positives. It is one of the most useful HikariCP settings for diagnosis."},
    {"level":4,"diff":3,"scenario":4,"q":"A service runs Java 17, has Metaspace stable but Compressed Class Space growing. Which configuration would let you cap CCS specifically?","opts":["-XX:CompressedClassSpaceSize=256m","-XX:+UseG1GC","-Xss","-Xms","-XX:NewRatio"],"ans":0,"fb":"CompressedClassSpaceSize caps the compressed class space sub-region. Setting it explicitly forces failure earlier and prevents native memory from being silently consumed by class metadata growth."},
    {"level":4,"diff":1,"scenario":0,"q":"In MAT, what is the difference between 'shallow heap' and 'retained heap' for an object?","opts":["Retained heap is for primitives","They are the same","Shallow heap includes children","Retained heap excludes references","Shallow heap is the memory of the object itself; retained heap is the total memory that would be freed if the object were collected"],"ans":4,"fb":"Shallow heap is just the object. Retained heap includes everything the object exclusively keeps alive. For finding leaks, retained heap is the more useful metric because it captures impact."},
    {"level":4,"diff":2,"scenario":1,"q":"GC logs show frequent 'Full GC (System.gc())' lines. Which is the most likely cause?","opts":["JVM bug","Kubernetes triggering GC","Healthy GC","Application or library code calling System.gc() — sometimes via java.nio.Bits when Direct buffers are exhausted","Cron job"],"ans":3,"fb":"Explicit Full GCs are unusual. Common culprits are application code calling System.gc() directly, frameworks doing it, or java.nio.Bits invoking it when direct buffer reservations fail. -XX:+DisableExplicitGC suppresses such calls if you confirm none are required."},
    {"level":4,"diff":3,"scenario":2,"q":"You see 'OutOfMemoryError: Direct buffer memory' in production. JFR shows the leak path runs through a third-party HTTP client. What is the most defensible mitigation while a fix is developed?","opts":["Set -XX:MaxDirectMemorySize explicitly and add monitoring/alerts on direct buffer usage; bound the client's connection pool","Restart pods every hour","Remove the HTTP client","Increase -Xmx","Disable monitoring"],"ans":0,"fb":"Bounding direct memory and the connection pool turns an unbounded leak into a controlled failure mode. Monitoring catches it early. These are mitigations, not fixes — the underlying client misuse must still be corrected."},
    {"level":4,"diff":2,"scenario":3,"q":"A thread dump captured under load shows ~5 threads in RUNNABLE state stuck in java.util.regex.Matcher.match. What does this suggest?","opts":["Catastrophic backtracking in a regex — pathological input is causing exponential matching time","Network slowness","GC pause","Disk I/O","Healthy load"],"ans":0,"fb":"Long RUNNABLE time inside Matcher is the signature of regex backtracking. A small number of stuck threads matching a complex regex on adversarial input can saturate the CPU. Mitigation: simpler regex or input validation."},
    {"level":4,"diff":3,"scenario":4,"q":"A service shows Metaspace=400MB and growing slowly. Number of loaded classes is stable at 18,000. Which conclusion is most defensible?","opts":["A class loader leak","CPU starvation","The growth is not new classes — investigate Metaspace fragmentation or NMT 'Class' detail; it may be internal CCS growth, not a leak","Disk full","Normal"],"ans":2,"fb":"If class count is stable, it is not a class loader leak in the simple sense. Metaspace bookkeeping and fragmentation, or growth in native structures backing Metaspace, can still increase committed memory. NMT detail confirms which slice grew."},
    {"level":4,"diff":1,"scenario":0,"q":"What kind of GC roots most frequently anchor leaks involving Spring beans?","opts":["Static fields and ApplicationContext/SingletonBeanRegistry references","Class loaders only","Thread frames","Synchronization monitors","Local variables"],"ans":0,"fb":"Spring singletons live for the JVM's lifetime via the application context. Leaks rooted there will not be reclaimed regardless of GC. Static fields have the same effect. MAT's GC roots view exposes both clearly."},
    {"level":4,"diff":2,"scenario":2,"q":"A JFR recording stored on a busy node has a high overhead. Which configuration reduces overhead while keeping useful data?","opts":["Increase heap","Use the 'profile' template","Switch to async-profiler only","Disable JFR","Use the 'default' template, lower sample frequency, and shorter recording duration"],"ans":4,"fb":"JFR's 'default' template is intentionally low overhead (~1%). Increasing duration and lowering sampling further reduces impact while still capturing aggregate trends. 'profile' is more detailed but higher overhead — keep for short investigations."},
    {"level":4,"diff":3,"scenario":3,"q":"You analyse a thread dump and notice 200 threads named 'pool-7-thread-N' in WAITING on a LinkedBlockingQueue. The application is hanging. What is the typical root cause?","opts":["Disk I/O","Healthy idle workers","Heap full","An ExecutorService whose worker threads are waiting for tasks while the producer is also blocked elsewhere — likely a thread pool deadlock or starvation across pools","CPU starvation"],"ans":3,"fb":"Pools waiting on their queues with the system hung often indicate inter-pool dependency problems: producer pool blocked, consumer pool idle, or shared resource contention. Map the dependency between pools to find the deadlock."},
    {"level":5,"diff":1,"scenario":0,"q":"For a latency-sensitive Spring Boot service with strict P99 < 50ms, which JVM characteristic of ZGC is most relevant compared to G1?","opts":["Smaller footprint","Higher throughput","Less CPU","Sub-millisecond pause times even with very large heaps","Simpler tuning"],"ans":3,"fb":"ZGC and Shenandoah are designed for low pause times. They concurrently relocate objects and produce sub-millisecond stop-the-world pauses regardless of heap size. For tight P99 SLOs, this trait dominates the choice."},
    {"level":5,"diff":2,"scenario":0,"q":"You have JFR data from a service running G1 with 6Gi heap. P99 latency is 90ms; analysis shows 30ms is GC pause. What is the most evidence-based next step?","opts":["Disable JFR","Increase heap","Compare a parallel canary run on ZGC under identical traffic; verify CPU and footprint cost is acceptable","Switch to ZGC immediately","Lower heap"],"ans":2,"fb":"Switching collectors is a tuning decision. Without a controlled comparison you don't know if ZGC's lower pauses outweigh its higher CPU and footprint. A canary with the same JFR setup gives a defensible answer."},
    {"level":5,"diff":3,"scenario":0,"q":"A service migrated from G1 to ZGC. P99 dropped from 90ms to 25ms but container memory rose by ~25%. What explains this trade-off?","opts":["A bug","CPU change only","ZGC's coloured pointers and concurrent relocation require additional metadata and a larger working set; the latency win is paid for in footprint","JIT churn","Network change"],"ans":2,"fb":"ZGC trades footprint for pause time. Coloured pointers, forwarding tables, and concurrent relocation overhead add to the resident set. This is the standard trade-off and why ZGC is not a free upgrade."},
    {"level":5,"diff":1,"scenario":1,"q":"You are designing a memory budget for a service with two thread pools: 100 Tomcat workers and 200 background workers. What is the first sizing question to answer?","opts":["CPU model","Pod IP","Which JIT compiler to use","Image size","Whether the two pools can run at peak concurrency simultaneously"],"ans":4,"fb":"Memory budgets must account for worst-case concurrency, not average. If both pools can be active at the same time, stack memory and per-thread state must be sized for the sum. If they cannot, the overlap can be bounded smaller."},
    {"level":5,"diff":2,"scenario":1,"q":"A service has Tomcat=300, Hikari=50, and a Kafka consumer pool of 20. Each pool can be saturated independently. Total threads ≈ 370. Stack budget?","opts":["~37MB","Cannot estimate","Negligible","~370MB","~3.7GB"],"ans":3,"fb":"At default 1MB stacks, 370 threads ≈ 370MB. This must be reserved in the container budget on top of -Xmx, Metaspace, JIT, and direct buffers. Capping pool sizes (or moving to virtual threads) directly reduces the stack share."},
    {"level":5,"diff":3,"scenario":1,"q":"You want to bound HikariCP pool size based on database capacity, not application optimism. What is the most defensible upper bound rule?","opts":["Number of HTTP threads","Total connection budget across all pods ≤ database max_connections × safety margin (e.g., 0.7)","Always 200","Number of users","The number of CPU cores"],"ans":1,"fb":"DB capacity is the constraint, not the JVM. Pool size × pod count × replica count must stay safely under database max_connections, with margin for migrations and reconnect storms. This prevents one team from starving the database."},
    {"level":5,"diff":1,"scenario":2,"q":"For services with strict cold-start SLOs, which JVM feature most directly reduces startup time?","opts":["Smaller image","More CPU","Larger heap","Class Data Sharing (AppCDS) or CDS-with-AOT in newer JDKs","G1GC"],"ans":3,"fb":"Class data sharing pre-loads class metadata at startup, eliminating significant repeat work. Combined with AOT in modern JDKs, it can shave seconds off cold start. It is the canonical JVM-level cold-start optimisation."},
    {"level":5,"diff":2,"scenario":2,"q":"A service with a 30-second cold start fails its 20-second readiness probe. What is the cleanest Kubernetes-level fix that does not silently mask warm-up issues?","opts":["Disable liveness","Disable readiness probes","Increase container memory","Increase replicas","Add a startupProbe with a generous failureThreshold so liveness/readiness only become active after the JVM has warmed"],"ans":4,"fb":"startupProbe was added precisely for slow-starting applications. It defers liveness/readiness until the app is up, without weakening those probes' behaviour at steady state. Disabling probes hides real problems."},
    {"level":5,"diff":3,"scenario":2,"q":"After migrating to GraalVM native image, cold start dropped from 25s to 200ms but P99 latency under load increased ~30%. Why?","opts":["Heap increased","Disk slow","Native images lack JIT, so they cannot adaptively optimise hot paths the way HotSpot does at steady state","CPU change only","GraalVM is broken"],"ans":2,"fb":"Native images trade peak throughput for fast startup and small footprint. Without JIT, optimisation is limited to ahead-of-time compilation. For services where steady-state latency matters most, this is a real cost to weigh."},
    {"level":5,"diff":1,"scenario":3,"q":"What is the most useful single document an org can publish to improve memory consistency across services?","opts":["A code generator","A short JVM baseline standard: -XX:MaxRAMPercentage, GC choice, NMT on, dump-on-OOM, recommended Tomcat thread defaults","A 200-page handbook","Banning Spring","No standard"],"ans":1,"fb":"A short, opinionated baseline that everyone can adopt produces faster wins than a long handbook. Standards covering heap percentage, GC, diagnostics flags, and thread defaults remove most of the variance between services."},
    {"level":5,"diff":2,"scenario":3,"q":"You are defining an org-wide Spring Boot JVM baseline. Which set is the most defensible starting point?","opts":["-XX:MaxRAMPercentage=70, -XX:+ExitOnOutOfMemoryError, -XX:+HeapDumpOnOutOfMemoryError, -XX:HeapDumpPath=/dumps, -XX:NativeMemoryTracking=summary","-Xmx=1g for everyone","-Xmx=4g for everyone","Use only -Xmx and nothing else","Disable GC"],"ans":0,"fb":"This baseline gives container-aware sizing, fail-fast on OOM, automatic dump capture, and non-trivial diagnostics — all without committing to a particular GC. It is enough to remove the worst footguns and enable diagnosis when problems arise."},
    {"level":5,"diff":3,"scenario":3,"q":"You roll out a new memory baseline. Two service teams refuse: one has a custom GC config, one runs a legacy JVM. What is the most pragmatic approach?","opts":["Ignore them","Define an exception process: teams can opt out with a documented reason and a sunset plan, while the baseline becomes default for new services and gradually for old ones","Remove their access","Block deploys","Force adoption"],"ans":1,"fb":"Forcing rollout creates resentment and risk. An explicit exception process keeps the baseline credible while acknowledging real constraints. Combined with a default-on for new services, the standard wins by attrition without confrontation."},
    {"level":5,"diff":1,"scenario":4,"q":"A team is evaluating moving from Java 17 to Java 21. Which memory-related feature is the headline change relevant to a Spring Boot service?","opts":["No memory changes","No GC changes","Generational ZGC (preview/stable depending on version) and improvements in virtual threads' interaction with memory","Smaller Metaspace","Removal of heap"],"ans":2,"fb":"Java 21 introduced generational ZGC, which improves throughput at similar pause characteristics. Virtual threads also stabilised. Both are headline memory-related changes worth evaluating, especially for latency-sensitive services."},
    {"level":5,"diff":2,"scenario":4,"q":"You evaluate Java 21 virtual threads for a service that blocks heavily on JDBC. What is the key memory concern to test before adopting?","opts":["Disk usage","Whether HikariCP or any blocking library pins virtual threads (defeats the benefit) and whether ThreadLocals retained per virtual thread bloat the heap","DNS","CPU usage","Image size"],"ans":1,"fb":"Pinning happens when virtual threads are stuck on monitors or native frames, preventing the carrier thread from being released — losing the scalability benefit. ThreadLocal-heavy libraries also retain memory per virtual thread. Both must be tested."},
    {"level":5,"diff":3,"scenario":4,"q":"A team plans to upgrade JVM versions across the fleet. Which signal would you use to define 'safe to roll out' for memory specifically?","opts":["Pod restart count","A canary cohort running for 7+ days under production traffic with monitoring on heap baseline, GC pause distribution, Metaspace, and direct buffer usage compared to the previous version","No signal — just upgrade","CPU only","Disk only"],"ans":1,"fb":"A canary with a long observation window captures slow leaks and tail behaviour the unit tests will not. Comparing memory regions side-by-side with the prior version makes the rollout decision evidence-based."},
    {"level":5,"diff":1,"scenario":0,"q":"A workload is throughput-oriented (batch ETL, no human users). Which collector is generally the simplest reasonable default?","opts":["G1 (default in modern JDK), or even Parallel for pure throughput","Shenandoah","Serial","CMS","ZGC"],"ans":0,"fb":"For batch workloads where pause times don't matter, throughput-oriented collectors win. G1 is a safe modern default; Parallel can squeeze out more throughput at the cost of long pauses. Low-pause collectors trade throughput for latency, which batch doesn't need."},
    {"level":5,"diff":2,"scenario":1,"q":"Two services share a Cloud SQL database. Service A uses Hikari pool=50; Service B uses pool=100. The DB max_connections=200. What is the systemic risk?","opts":["No risk","If both services scale to 3 replicas each, they collectively can demand 50×3 + 100×3 = 450 connections, exceeding 200 — connection storms during deploy","Disk I/O","CPU pressure","Heap pressure"],"ans":1,"fb":"Per-pod pool sizes multiplied by replica count is the real demand on the database. Without a budget across the fleet, a deploy or autoscaling event can cause connection exhaustion at the DB. Centralised budgeting prevents this."},
    {"level":5,"diff":3,"scenario":2,"q":"A team wants to use CRaC (Coordinated Restore at Checkpoint) for cold-start improvements. What is the principal risk to a stateful Spring Boot service?","opts":["Slower startup","Larger image","Less heap","More CPU","Restoring a JVM checkpoint can resurrect open file descriptors, sockets, and connection pools — if those external resources have changed, the restored process is in an inconsistent state"],"ans":4,"fb":"CRaC checkpoints captured live state. After a long pause, sockets, DB connections, and tokens may no longer be valid. Restoration must trigger reconnect and revalidation logic, otherwise the service will fail in subtle ways."},
    {"level":5,"diff":2,"scenario":3,"q":"An org-wide JVM baseline is hard to enforce because configurations live in many repositories. What is the most leveraged way to enforce defaults?","opts":["Email reminders","Manual review","A wiki page","Central base Docker images that already set the JVM flags, plus a Kubernetes admission policy that blocks deploys without container-aware sizing","No enforcement"],"ans":3,"fb":"Defaults set in the base image apply to every service that uses it without changing application code. Admission policies catch outliers. Combined, they make the baseline the path of least resistance, which is the only enforcement that works at scale."},
    {"level":5,"diff":3,"scenario":4,"q":"You are assessing an upgrade from Java 17 to Java 21 for a high-throughput service. Which memory-relevant test would you weight highest?","opts":["Long-running production-shaped load test comparing GC pause distribution, allocation rate, Old Gen growth, and direct buffer use across versions","Compile time","Startup time only","Microbenchmarks","Image size"],"ans":0,"fb":"Memory characteristics emerge over hours of realistic load, not in microbenchmarks. A long, production-shaped run is the only way to compare GC pauses, leaks, and pressure under conditions that match real usage."},
    {"level":5,"diff":1,"scenario":0,"q":"Which production scenario most strongly favours Shenandoah or ZGC over G1?","opts":["Batch ETL","Single-threaded workloads","CPU-only workloads","User-facing services with strict P99 latency goals and large heaps","Tiny services"],"ans":3,"fb":"Shenandoah and ZGC shine when pauses must be small even at large heap sizes. G1 is fine until the heap grows large enough that its pauses become noticeable. P99 SLOs and big heaps are the joint signal."},
    {"level":5,"diff":2,"scenario":2,"q":"You design a 'warm-up endpoint' that the platform calls after pod startup, before traffic. What should it exercise to be effective?","opts":["Realistic representative requests against the actual business endpoints, ideally including DB and Kafka calls, to JIT-compile hot paths and warm pools","/actuator/info","Random URLs","Health checks","DNS lookups"],"ans":0,"fb":"Warm-up only helps if it exercises the same code paths real traffic will use. Realistic synthetic requests cause JIT to compile the right methods, populate caches, and trigger pool establishment."},
    {"level":5,"diff":3,"scenario":3,"q":"Across the fleet, ~30% of services have -Xmx >= container limit. As an expert leading a remediation, what is the smallest, highest-leverage fix to apply first?","opts":["Email teams","Provide a fixed base image and admission policy that enforce -XX:MaxRAMPercentage <= 75 unless explicitly overridden, and migrate the worst offenders manually","Mass migration","Do nothing","Block all deploys"],"ans":1,"fb":"Most teams with bad sizing copied an old config. A central default fixes the majority quietly. Manual migration of the worst cases handles the long tail. This combines automation with targeted human attention."},
    {"level":5,"diff":2,"scenario":4,"q":"A new Java version proposes deprecating a GC you currently use in production. How do you scope the impact assessment?","opts":["Ignore","Migrate everything immediately","Stay on old JVM forever","List every service using that GC, prioritise by SLO sensitivity and heap size, and define a migration test plan per group","Switch random services"],"ans":3,"fb":"A targeted assessment based on SLO sensitivity focuses effort on the services where the GC choice actually matters. Grouped migrations reduce duplicated work. Drift to old JVMs creates bigger problems later."},
    {"level":5,"diff":3,"scenario":1,"q":"In a fleet, you see services with hikari max pool sizes ranging from 10 to 200. What is the principled way to set defaults?","opts":["Pick 100 for everyone","No default","Disable HikariCP","Default per service tier based on connection budget and observed P99 connection wait time, with central guidance and exceptions documented","Allow anything"],"ans":3,"fb":"A single number does not fit all services. Tier-based defaults derived from real connection budgets and observed waits are defensible and scale better than one-size-fits-all."},
    {"level":5,"diff":1,"scenario":2,"q":"Which is the most useful pre-warm signal that your readiness probe should require for memory-sensitive services?","opts":["DNS","CPU < 50%","A first set of representative requests has succeeded with latency under threshold, indicating JIT and pools are warm","Pod start","Heap committed > 50%"],"ans":2,"fb":"Synthetic warm-up traffic that meets a latency target is a strong functional signal that the JVM is ready. It captures JIT, pools, and caches together — real signals that 'ready' is meaningful."},
    {"level":5,"diff":3,"scenario":4,"q":"You assess an upgrade to a JVM version that introduces CDS by default. What should you measure to claim the change is net positive for memory?","opts":["Just heap","Disk usage","Compare both startup time and resident set size at steady state, since CDS changes class metadata residency too","DNS","CPU only"],"ans":2,"fb":"CDS speeds startup but also changes how class metadata is shared. RSS comparison ensures the change is not silently increasing footprint elsewhere. Measuring both is the honest assessment."},
    {"level":5,"diff":2,"scenario":0,"q":"In an A/B canary comparing G1 and ZGC under identical traffic, which metric set best decides the winner for a latency-sensitive service?","opts":["CPU only","Just heap","Pod count","P99 latency, GC CPU overhead, container RSS, throughput at SLO — together","Image size"],"ans":3,"fb":"No single metric captures the trade-off. ZGC may win on P99 but lose on CPU and footprint. The decision is multi-dimensional and should be made with the service's SLOs in mind."},
    {"level":6,"diff":1,"scenario":0,"q":"Which artefact is the most useful to hand to other teams when launching a fleet-wide memory reliability initiative?","opts":["An email","A short, opinionated baseline standard plus a one-page rollout guide and a working reference image","No artefact","A 100-page report","A wiki link"],"ans":1,"fb":"Adoption is driven by friction. A clear baseline, a one-page guide, and a working reference let teams adopt without re-deriving the answer. Long reports rarely change behaviour."},
    {"level":6,"diff":1,"scenario":0,"q":"A fleet-wide initiative aims to reduce OOMKills by 80% in a quarter. Which one input is most predictive of success?","opts":["Number of meetings","Funding","An accurate baseline measurement of OOMKill frequency by service and the ability to tag responsible owners — without this, you cannot prioritise or measure progress","Tool count","Slogan"],"ans":2,"fb":"You cannot improve what you cannot measure. A clean baseline with ownership lets you target the highest-impact services first and prove progress. Without it, the initiative is anecdotal."},
    {"level":6,"diff":2,"scenario":0,"q":"Two services dominate fleet OOMKill counts. Service A has many small leaks; Service B has one big sizing bug. Which do you prioritise?","opts":["Service B — it is a single, high-leverage fix with immediate, measurable impact, which builds momentum for the broader initiative","Neither","Random","Service A","Both equally"],"ans":0,"fb":"Initiatives need early wins. A single sizing fix on Service B can be deployed in days and quoted as a 50% reduction in fleet OOMKills. Service A's many small leaks are real but slower to address; do them after the momentum is established."},
    {"level":6,"diff":3,"scenario":0,"q":"A fleet-wide reliability initiative meets resistance from a high-status team that insists their custom JVM settings are necessary. What is the most effective leadership move?","opts":["Ignore","Co-author the standard with them — incorporate their valid concerns, document the exception explicitly, and use them as a reference adopter","Block their deploys","Force compliance","Escalate to HR"],"ans":1,"fb":"High-status holdouts often have legitimate requirements and can become powerful allies when included. Co-authoring turns resistance into endorsement and produces a better standard. Forcing rarely works at this level."},
    {"level":6,"diff":1,"scenario":1,"q":"Translating GC pauses to SLA budgets: a service has a 50ms P99 SLA and observes 100ms G1 pauses every 30 seconds. Roughly what fraction of requests at peak load could be SLA-violating from GC alone?","opts":["Half","Negligible always","Small but meaningful — proportional to (pause × concurrency) / interval; for typical services this can mean a measurable percentage of requests","100%","0%"],"ans":2,"fb":"Any in-flight request when GC pauses suffers the full pause length. The fraction depends on concurrency and pause frequency, but with 100ms pauses and a 50ms SLO, even modest concurrency produces measurable violations."},
    {"level":6,"diff":2,"scenario":1,"q":"A platform-wide P99 latency budget is 100ms. JIT and GC together account for ~30ms. As an architect, which lever would you propose first?","opts":["Reduce CPU","Increase the SLA","Tighten the GC pause target via collector choice and heap sizing, then re-measure — leaving room in the budget for other latency contributors","Add CPU","Disable GC"],"ans":2,"fb":"GC consumption of 30% of the latency budget is high. Collector choice and heap sizing are the most direct levers. Tightening here frees budget for other unavoidable contributors like network and DB latency."},
    {"level":6,"diff":3,"scenario":1,"q":"You are asked to translate a fleet-wide '99.9% requests under 100ms' SLO into a JVM memory standard. Which standard would best support that SLO?","opts":["Ignore JVM","Mandate pause-time-oriented collector, cap heap size by tier, require dump-on-OOM and NMT on, define maximum acceptable pause distribution per tier","Use any GC","Use any heap size","No standard"],"ans":1,"fb":"A meaningful standard pins down the dimensions that affect tail latency: collector choice, heap caps, observability, and explicit pause distribution targets. This connects an SLA to operational JVM choices."},
    {"level":6,"diff":1,"scenario":2,"q":"What is the principal memory benefit of GraalVM native image for a Spring Boot service?","opts":["Smaller image only","Higher peak throughput","Lower steady-state RSS and very fast startup, at the cost of giving up JIT optimisations","Larger heap","Lower CPU"],"ans":2,"fb":"Native images consume less memory and start in milliseconds, useful for serverless and short-lived workloads. The trade-off is loss of JIT, which can hurt steady-state throughput on long-running services."},
    {"level":6,"diff":2,"scenario":2,"q":"You are asked to evaluate CRaC (Coordinated Restore at Checkpoint) for the fleet. Which service profile would benefit most safely?","opts":["All services","Long-running stateful payment processors","Background workers with file handles","Stateless services with no long-lived external resources at the moment of checkpoint, such as request handlers behind a stateless API gateway","Database services"],"ans":3,"fb":"CRaC restores resurrect held resources. Stateless services with minimal external state are the safest first targets because they have less to revalidate. Long-lived stateful services carry the most risk."},
    {"level":6,"diff":3,"scenario":2,"q":"You produce a structured assessment of virtual threads for the platform. Which dimension is most often missed?","opts":["ThreadLocal retention per virtual thread, which can drive heap up sharply for libraries that use ThreadLocals heavily (security context, MDC, transaction binding)","Image registry","CPU","Image size","Performance"],"ans":0,"fb":"ThreadLocals scale by thread count. With millions of virtual threads, ThreadLocal-heavy libraries can convert thread-cheap concurrency into a heap explosion. This is the most common surprise in evaluations."},
    {"level":6,"diff":1,"scenario":3,"q":"What is the highest-leverage component of fleet-wide memory observability?","opts":["Per-pod CPU","Disk","A consistent set of JVM metrics and labels exported by every service: heap, non-heap by area, GC pauses, allocation rate, threads, classes loaded","Network","DNS"],"ans":2,"fb":"Consistency is what makes fleet observability useful. When every service exposes the same labels, dashboards and alerts can be reused, comparisons become possible, and platform tools can be built once."},
    {"level":6,"diff":2,"scenario":3,"q":"You are designing automated diagnostics that capture context around an OOMKill. Which capture is most useful given that the JVM has already been killed?","opts":["A heap dump after the kill","Just logs","Just exit code","Nothing","Pre-kill snapshots: periodic short JFR recordings, recent jvm.memory.* metric history, and Metaspace + thread count time series, retained on a sidecar volume"],"ans":4,"fb":"OOMKill leaves no live JVM. The only useful diagnostics are the ones captured before the event. Periodic short JFR snapshots and dense metric retention give you the breadcrumbs to triage after the fact."},
    {"level":6,"diff":3,"scenario":3,"q":"A platform tool aims to detect class loader leaks fleet-wide automatically. What is the most reliable algorithmic signal?","opts":["Heap usage","Thread count","CPU usage","A steady, sustained increase in jvm.classes.loaded.classes that is not correlated with deploys, combined with rising Metaspace","Pod restarts"],"ans":3,"fb":"A class loader leak shows up as continuously rising loaded class count without deploys. Pairing it with Metaspace growth filters out incidental class loading. This combination is robust enough for automated detection."},
    {"level":6,"diff":1,"scenario":4,"q":"During a deep production investigation, you suspect a native (non-JVM) memory leak from a JNI library. Which tool gives the clearest first signal?","opts":["Linux pmap/smaps showing growing anonymous regions outside the JVM heap, and /proc/<pid>/status RssAnon","jstack","jmap","MAT","Dashboard"],"ans":0,"fb":"Native leaks live below the JVM. pmap and /proc/<pid>/status show resident segments by mapping. Growing anonymous segments not attributable to JVM regions are the signature of native leaks. NMT may not capture them since they originate outside the JVM."},
    {"level":6,"diff":2,"scenario":4,"q":"A driver-level memory leak is suspected in a third-party library. What is the most defensible mitigation while you negotiate an upstream fix?","opts":["Force the upstream","Restart pods on a schedule based on observed leak rate, monitor via Grafana, document the trade-off, and pin the library version","Disable observability","Switch language","Ignore"],"ans":1,"fb":"Scheduled restarts are an honest, bounded mitigation when the root cause is outside your control. Combined with monitoring and documentation, they keep the system stable without pretending the underlying problem is fixed."},
    {"level":6,"diff":3,"scenario":4,"q":"You lead a deep production investigation that crosses multiple teams: app, database driver, and kernel. Which artefact is most valuable to produce at the end?","opts":["A timeline-based incident write-up with evidence (heap dumps, NMT, pmap, kernel logs), root cause, contributing factors, and concrete preventive actions per team","A blame report","A graph","An email summary","A meeting"],"ans":0,"fb":"Cross-team incidents are forgotten unless documented honestly and constructively. A timeline with evidence and concrete actions per team turns the investigation into organisational learning and prevents recurrence."},
    {"level":6,"diff":1,"scenario":1,"q":"A leadership question: 'What is the cost of our current GC pauses to user experience?' What is the most credible way to answer?","opts":["A measurement: percentage of P99 latency budget consumed by GC pauses, expressed in real ms and as a percentage of SLA","Survey","Benchmarks","Anecdotes","Guess"],"ans":0,"fb":"Leadership wants impact in their language. Translating GC into SLA budget consumption is concrete and decision-useful. It also bridges the gap between engineering metrics and business outcomes."},
    {"level":6,"diff":2,"scenario":2,"q":"A platform team proposes adopting Project Loom virtual threads org-wide. What is the most responsible scoping move?","opts":["Mandate it","Ban it","Roll out everywhere","Pilot in a small set of services with mixed workloads (I/O bound, CPU bound, ThreadLocal heavy), measure heap and pinning, then expand based on evidence","Email everyone"],"ans":3,"fb":"Loom's benefits depend on workload. A small pilot covering different patterns surfaces pinning, ThreadLocal, and library compatibility issues before they affect the fleet. Evidence-based expansion is the responsible path."},
    {"level":6,"diff":3,"scenario":0,"q":"Six months into a fleet memory initiative, OOMKills are down 70%, but two services regressed and now drive most remaining OOMKills. What is the most strategic response?","opts":["Kick them out","Treat the regression as a signal that the standard does not yet cover those services' workload — extend the standard or define a new tier, and add the regression cases to the rollout playbook","Declare victory","Blame the teams","Stop the initiative"],"ans":1,"fb":"Regressions in late-adopters are usually evidence that the standard's assumptions don't fit them. Adapting the standard rather than blaming the teams expands its coverage and shows the initiative is willing to learn — both required for durable adoption."},
    {"level":6,"diff":2,"scenario":1,"q":"A new SLA requires P99 < 30ms. The current GC config produces 50ms pauses. As authority, what cross-team conversation must you initiate first?","opts":["Disable GC","Add servers","Order a GC change","Switch language","Reset the SLA discussion: explain the JVM constraints to product, present the cost of meeting the new target (footprint, CPU), and decide jointly which service tiers actually need it"],"ans":4,"fb":"SLAs that ignore JVM physics are unrealistic. The authority's job is to bring engineering reality into the SLA conversation so the business can choose with full information. Sometimes the answer is 'only the user-facing tier needs that SLA'."},
    {"level":6,"diff":3,"scenario":2,"q":"You evaluate CRaC for a fleet that handles regulated financial transactions. What is the strongest reason to delay adoption?","opts":["Performance","Image size","State validation: a restored process must re-validate every external resource, token, and connection to avoid acting on stale state — for regulated workloads this is a non-trivial correctness risk","CPU","Cost"],"ans":2,"fb":"Regulated workloads cannot afford acting on stale state. CRaC's restore semantics make this a real risk. The mitigation cost (validating everything on restore) often outweighs the cold-start benefit at the current maturity."},
    {"level":6,"diff":2,"scenario":3,"q":"A fleet-wide observability project wants to capture continuous JFR profiles. What is the most realistic constraint?","opts":["JFR is too slow","JFR cannot be enabled","Storage and indexing cost: hundreds of services × always-on JFR can be expensive — limit to short rotating recordings and centralised analysis on demand","JFR is broken","JFR is unsupported"],"ans":2,"fb":"JFR runtime overhead is small with the default template, but storing and indexing recordings at fleet scale is significant. Rotating short recordings retained centrally for on-demand analysis is the usual realistic balance."},
    {"level":6,"diff":3,"scenario":4,"q":"A driver-level memory issue is escalated to you after an outage. Two engineers disagree about root cause. What approach gives the clearest answer?","opts":["Vote","Email vendor","Define a controlled reproduction with NMT on, pmap snapshots, and a representative load — if reproducible, capture before/after to attribute the leak; if not, the disagreement is unresolved by data and that fact must be stated","Restart and forget","Pick a side"],"ans":2,"fb":"The honest authority acknowledges when data does not yet decide a question. A controlled reproduction is the only way to resolve technical disagreements about root cause. If reproduction fails, that failure is itself a data point."},
    {"level":6,"diff":1,"scenario":3,"q":"What is the simplest, most universal automated diagnostic to add to every service in the fleet?","opts":["Restart on OOM","-XX:+HeapDumpOnOutOfMemoryError with HeapDumpPath pointing to a persistent location, plus a sidecar that uploads the dump to object storage","No diagnostics","Heap dump on startup","Just logs"],"ans":1,"fb":"This single combination captures a heap dump at the moment of OOM and gets it off the dying pod before restart. It is cheap, universal, and produces the artefact that makes post-mortem analysis tractable."},
    {"level":6,"diff":2,"scenario":4,"q":"You are leading a deep investigation where the JVM is exonerated and the issue lies in a Linux kernel/cgroup interaction. As authority, what is your reporting responsibility?","opts":["Hide the result","Blame Java","Stay quiet","Move on","Document the finding clearly, share with the platform/SRE team, raise with the OS vendor, and update internal knowledge so the next team does not repeat the investigation"],"ans":4,"fb":"Authority means circulating learnings. Many investigations stop at exoneration because the team is exhausted, but the next team will repeat the work without a documented finding. Sharing closes the loop and prevents recurrence."},
    {"level":6,"diff":3,"scenario":1,"q":"A new business line proposes a 99.99% / 20ms SLA. What is the most defensible technical position regarding JVM memory management?","opts":["Maybe","No","Sure, anything is possible","A 20ms tail SLA at 99.99% is feasible only with low-pause collectors, capped heap sizes, dedicated nodes, and engineered backpressure — and even then carries cost; commit only to what current measurements support","Yes always"],"ans":3,"fb":"At those numbers, GC pauses, scheduling jitter, and noisy neighbours all matter. A defensible position quantifies the engineering required and refuses unrealistic commitments. Setting expectations now prevents broken promises later."},
    {"level":6,"diff":2,"scenario":0,"q":"You publish a quarterly fleet memory report. Which single chart most clearly motivates further investment?","opts":["OOMKills per service per week, ranked, with the top 5 highlighted — it makes pain concrete and shows where the next quarter's effort should go","Pod count","CPU","Heap usage","Disk"],"ans":0,"fb":"OOMKills are the single most visible failure mode. Ranking them turns abstract reliability into concrete service-level pain that leadership can prioritise. Combined with trend lines, it justifies investment without rhetoric."},
    {"level":1,"diff":3,"scenario":3,"q":"A team enables Spring Boot's lazy initialization (-Dspring.main.lazy-initialization=true) to speed up startup. What is the main memory trade-off?","opts":["Memory is saved permanently","Spring fails to start","CPU increases at startup","First requests to each bean pay the initialization cost, creating unpredictable latency spikes and memory allocation bursts after deployment","No trade-off"],"ans":3,"fb":"Lazy init defers bean creation until first use. This shifts startup time into the first request path per bean, producing latency spikes and bursty memory allocation at unpredictable times. It improves startup but shifts the cost, not eliminates it."},
    {"level":1,"diff":3,"scenario":4,"q":"A colleague says 'calling System.gc() will prevent OOM errors'. Why is this misleading?","opts":["System.gc() always works","The JVM treats System.gc() as a suggestion, not a command — it may or may not run GC, and if there is a real leak, calling it repeatedly just wastes CPU without freeing retained objects","It causes OOMKill","It helps in containers","System.gc() is efficient"],"ans":1,"fb":"System.gc() is advisory. Even if GC runs, it cannot reclaim objects that are still reachable. Calling it in a loop to 'prevent OOM' masks the real problem and adds CPU overhead. Fix the leak instead."},
    {"level":2,"diff":1,"scenario":2,"q":"A pod's container memory metric reads 1.8Gi out of 2Gi limit after running for a week, but the JVM heap shows only 500MB used. Is this normal?","opts":["Kubernetes bug","The JVM committed heap plus non-heap regions can legitimately consume that much; check whether non-heap is stable or growing before deciding","Always normal","Dashboard error","Always a leak"],"ans":1,"fb":"Container RSS includes committed heap (which may be larger than used), Metaspace, stacks, buffers, and JIT cache. If non-heap is stable, the high RSS may be expected. If it is growing, investigate."},
    {"level":2,"diff":3,"scenario":1,"q":"A service has -Xmx=700m in a 1Gi container. It runs fine in dev but OOMKills in prod. Dev has 20 threads; prod has 200. What explains the difference?","opts":["The image is different","The JVM version is different","Cloud SQL is different","200 prod threads consume ~200MB in stacks vs ~20MB in dev — the extra 180MB pushes total past the 1Gi limit in production","Dev is broken"],"ans":3,"fb":"Thread count is the most common variable between dev and prod. At 1MB per stack, 200 threads vs 20 is 180MB difference — enough to tip a tight budget over the limit."},
    {"level":2,"diff":3,"scenario":4,"q":"A team sets resources.requests.memory: 256Mi and limits: 2Gi. The scheduler places 8 pods on a node with 4Gi. What happens when they all use 1Gi each?","opts":["Fine — they all fit","Kubernetes resizes the node","Pods migrate automatically","Nothing","The node becomes overcommitted (8Gi demand > 4Gi available), triggering kernel OOM-killing of pods based on QoS class and usage"],"ans":4,"fb":"Low requests enable overcommitment. The scheduler counts requests for placement but limits for enforcement. When actual usage exceeds node capacity, the kernel intervenes. Burstable pods with high gap between requests and limits are most at risk."},
    {"level":2,"diff":1,"scenario":4,"q":"Which JVM flag, on by default since Java 11, prevents the JVM from assuming it has access to the full host memory inside a container?","opts":["-Xmx","-XX:MaxMetaspaceSize","-XX:+UseContainerSupport","-XX:+HeapDumpOnOutOfMemoryError","-XX:+PrintGC"],"ans":2,"fb":"UseContainerSupport makes the JVM read cgroup limits instead of /proc/meminfo. It is on by default in modern JDKs. Without it, the JVM sizes itself for the host node, not the container."},
    {"level":2,"diff":2,"scenario":3,"q":"After an OOMKill, you find that the JVM produced a heap dump file (heap.hprof). What does this tell you about the kill mechanism?","opts":["The sidecar wrote it","The JVM detected OutOfMemoryError first and wrote the dump via -XX:+HeapDumpOnOutOfMemoryError before the kernel killed the container","OOMKill always produces dumps","Kubernetes wrote the dump","The kernel produced the dump"],"ans":1,"fb":"A heap dump exists only if the JVM had time to write it. OOMKill by itself is instant SIGKILL. The dump indicates the JVM detected a heap-level OOM first, wrote the dump, and then the process was killed. This means the heap was full."},
    {"level":2,"diff":2,"scenario":2,"q":"Which Kubernetes annotation or metric most directly tells you how much memory a pod is actually using at runtime?","opts":["Pod labels","Node allocatable","Pod annotations","Service mesh metrics","container_memory_working_set_bytes from cAdvisor / kubelet metrics"],"ans":4,"fb":"container_memory_working_set_bytes reports actual RSS minus reclaimable cache, which is what the kernel enforces against the cgroup limit. It is the most accurate runtime metric for container memory usage."},
    {"level":3,"diff":1,"scenario":2,"q":"What is the key difference between 'OutOfMemoryError: Java heap space' and 'OutOfMemoryError: GC overhead limit exceeded'?","opts":["Heap space is worse","Heap space means the heap is literally full; GC overhead means the JVM spent >98% of time in GC while reclaiming <2% — the heap is effectively full but technically has bytes left","One is fatal, the other is not","One is for containers, the other for VMs","They are identical"],"ans":1,"fb":"GC overhead limit exceeded is a throughput safeguard. The JVM decides the application is making no useful progress and throws rather than letting the app crawl. Both indicate heap pressure but from slightly different angles."},
    {"level":3,"diff":3,"scenario":4,"q":"A team asks you to estimate memory for a new service: 150 threads, Metaspace ~100MB, 3 Kafka consumers with 32MB fetch buffer each, Caffeine cache target 500MB, JIT ~80MB. What container limit would you propose?","opts":["1Gi","About 1.8–2Gi: heap must hold 500MB cache + working set (say 700MB heap), plus ~150MB stacks + 100MB Metaspace + 96MB Kafka + 80MB JIT + overhead ≈ 1.8Gi","4Gi","About 1.5Gi","Cannot estimate"],"ans":1,"fb":"Sum the slices: 700MB heap + 150MB stacks + 100MB Meta + 96MB Kafka buffers + 80MB JIT + ~100MB native ≈ 1.23Gi. Add safety margin → ~1.5-1.8Gi. 2Gi gives comfortable headroom. The cache lives inside the heap."},
    {"level":3,"diff":2,"scenario":0,"q":"A service's Spring Boot @Scheduled job runs every 5 minutes and processes a batch of 10,000 records. After each run, heap climbs by 50MB and only partially recovers. What should you check first?","opts":["Network","Disk","Pod count","CPU","Whether the batch method holds references to processed records beyond its scope — leaked references in a static collection, a growing list field, or an unbounded result set"],"ans":4,"fb":"Heap that climbs and only partially recovers after a scheduled job suggests references are surviving GC. Common culprits: results accumulated in a field, static collections, or logging contexts retaining data across runs."},
    {"level":3,"diff":1,"scenario":3,"q":"During code review, you see a Spring service injecting a prototype-scoped bean into a singleton. What is the memory concern?","opts":["Disk issue","No concern","The prototype scope creates a new instance per injection point, but the singleton holds one reference forever — not a leak per se, but the developer likely expected a new instance on each use","CPU issue","Thread safety only"],"ans":2,"fb":"Injecting a prototype into a singleton gives you one instance, defeating the intent of prototype scope. It is not a leak but a design mistake. If the developer wanted per-request instances, they need a Provider<T> or ObjectFactory."},
    {"level":3,"diff":3,"scenario":1,"q":"Actuator metrics show jvm.buffer.memory.used{id='direct'} growing by 10MB/hour but jvm.buffer.count{id='direct'} is flat. What does this combination suggest?","opts":["Class loader leak","Thread leak","Normal","Heap leak","Existing direct buffers are being resized larger, not new buffers being created — likely a library or NIO channel enlarging its allocation"],"ans":4,"fb":"Flat count + growing memory means existing buffers are growing in size. This can happen with NIO channels or libraries that dynamically resize their internal direct buffers. Identifying which library owns those buffers is the next step."},
    {"level":3,"diff":2,"scenario":2,"q":"A service hits 'OutOfMemoryError: Metaspace' but loaded class count is only 12,000 — modest for a Spring Boot app. What else could be filling Metaspace?","opts":["Disk","Too many threads","Large method bytecode, debug info retained at runtime, or heavy use of reflection/dynamic proxies inflating per-class metadata size","Heap pressure","CPU"],"ans":2,"fb":"Metaspace stores more than class counts suggest: method bytecode, constant pools, annotations, and debug symbols. A modest class count with large per-class metadata can still exhaust Metaspace, especially with MaxMetaspaceSize capped low."},
    {"level":4,"diff":1,"scenario":1,"q":"In GC logs, what does 'pause (young)' vs 'pause (mixed)' mean in G1 terminology?","opts":["Mixed collects only old gen","They are the same","Young is a full GC","Young collects only young-gen regions; mixed collects young plus some old-gen regions selected by concurrent marking","Mixed is a full GC"],"ans":3,"fb":"G1 young pauses evacuate only young regions. Mixed pauses additionally evacuate old regions identified as mostly garbage during concurrent marking. Understanding which type is pausing helps diagnose whether the issue is allocation rate (young) or promotion/fragmentation (mixed)."},
    {"level":4,"diff":1,"scenario":2,"q":"Which JFR event type is most useful for identifying which code paths create the most garbage?","opts":["jdk.CPULoad","jdk.SocketRead","jdk.ObjectAllocationInNewTLAB and jdk.ObjectAllocationOutsideTLAB","Thread sleep","jdk.FileRead"],"ans":2,"fb":"TLAB allocation events capture every significant heap allocation with the allocating stack trace. Sorting by total allocated bytes pinpoints exactly where GC pressure originates — the most actionable JFR data for memory."},
    {"level":4,"diff":2,"scenario":4,"q":"A Metaspace OOM occurs every 3 days. You set -XX:MaxMetaspaceSize=512m to catch it earlier and enable -verbose:class. The log shows repeated loading of classes like 'GeneratedMethodAccessor'. What is the likely root cause?","opts":["Normal reflection","Network","Java reflection inflates accessors after a threshold — repeated inflation with different class loaders can leak Metaspace if the inflation count is unbounded","JIT issue","Heap issue"],"ans":2,"fb":"After ~15 reflective calls, the JVM generates a bytecode accessor class. If something causes fresh generation repeatedly (new class loaders, framework reloads), these accumulate. -Dsun.reflect.inflationThreshold can control this."},
    {"level":4,"diff":3,"scenario":0,"q":"A heap dump shows 40% retained by java.lang.ref.Finalizer references. What does this indicate?","opts":["Normal","JVM bug","Heap too small","GC broken","Objects with finalize() methods are queuing faster than the Finalizer thread can process them, retaining memory until finalisation completes"],"ans":4,"fb":"Objects overriding finalize() are moved to a finalisation queue after becoming unreachable. If the queue backs up, these objects and everything they reference stays retained. Modern code should use Cleaner or try-with-resources instead."},
    {"level":4,"diff":3,"scenario":3,"q":"A thread dump shows a thread in TIMED_WAITING inside sun.misc.Unsafe.park, owned by a ForkJoinPool. 500 other tasks are queued. What is the architectural problem?","opts":["CPU starvation","The ForkJoinPool is undersized for the workload — blocking tasks are monopolising worker threads while hundreds of tasks wait in the queue","Heap full","Network slow","Normal pool behaviour"],"ans":1,"fb":"ForkJoinPool works best with non-blocking tasks. Blocking operations (I/O, locks) pin worker threads and starve the queue. The fix is either a dedicated thread pool for blocking work or using ManagedBlocker to expand the pool."},
    {"level":5,"diff":1,"scenario":3,"q":"What is the single most impactful JVM flag to standardise across the fleet for post-mortem diagnosis?","opts":["-XX:+TraceClassLoading","-XX:+PrintCompilation","-XX:+HeapDumpOnOutOfMemoryError — it captures a heap dump automatically at the moment of failure, enabling root-cause analysis after the fact","-verbose:class","-XX:+PrintGCDetails"],"ans":2,"fb":"A heap dump at OOM is the single most valuable diagnostic artefact. Without it, post-mortems rely on inference. With it, tools like MAT can identify the leak in minutes. Every service should have this flag."},
    {"level":5,"diff":1,"scenario":4,"q":"Java 21 stabilised virtual threads. What is the key memory implication for a Spring Boot service?","opts":["More Metaspace","No change","Virtual threads use heap-allocated continuations instead of OS thread stacks, dramatically reducing per-thread memory but increasing heap usage for high concurrency","Less heap","Less CPU"],"ans":2,"fb":"Virtual threads replace ~1MB OS stacks with small heap continuations. For services with thousands of concurrent tasks, this can save hundreds of MB of stack memory while shifting pressure onto the heap."},
    {"level":5,"diff":3,"scenario":0,"q":"A team runs G1 with -XX:MaxGCPauseMillis=50 but observes P99 pauses of 120ms. Which tuning direction is most defensible?","opts":["Reduce heap to shrink the amount of work per collection, or switch to ZGC if the workload cannot tolerate G1's pause variance at that heap size","Disable G1","Add CPU","Increase pause target","Increase heap"],"ans":0,"fb":"G1's pause target is a best-effort goal. Large heaps and high allocation rates can exceed it. Reducing heap or moving to ZGC are the two most effective paths to tighter pauses."},
    {"level":5,"diff":2,"scenario":3,"q":"You are writing an org-wide JVM configuration standard. Which two flags together provide the best safety net?","opts":["-XX:+HeapDumpOnOutOfMemoryError and -XX:+ExitOnOutOfMemoryError — the first captures diagnosis data, the second ensures the pod fails fast and restarts cleanly","-Xmx and -Xms","-verbose:gc and -verbose:class","-Xss and -XX:MaxMetaspaceSize","-XX:+PrintGC and -XX:+PrintCompilation"],"ans":0,"fb":"HeapDumpOnOOM captures the evidence; ExitOnOOM ensures the JVM does not limp along in a degraded state. Together they turn an OOM from a silent corruption risk into a diagnosed, clean restart."},
    {"level":5,"diff":2,"scenario":4,"q":"You evaluate Java 21's generational ZGC. What is the key improvement over non-generational ZGC?","opts":["Better throughput for workloads with many short-lived objects, because young-gen collections avoid scanning the entire heap","Lower CPU","Faster startup","Less Metaspace","Smaller footprint"],"ans":0,"fb":"Generational ZGC adds a young generation, so short-lived objects are collected cheaply without scanning old objects. This improves throughput while maintaining ZGC's sub-millisecond pauses."},
    {"level":6,"diff":1,"scenario":4,"q":"You lead a deep investigation into a native memory issue that turns out to be a known JDK bug. What is the most valuable output beyond fixing the immediate service?","opts":["A documented write-up shared internally and an upstream bug report, so other teams recognise the symptoms and the fix is tracked to a JDK release","A meeting","An email","Nothing","A Jira ticket only"],"ans":0,"fb":"Investigations that end in known JDK bugs are valuable learning for the whole org. A write-up with symptoms and workarounds prevents other teams from repeating the investigation. An upstream report ensures the fix is tracked."},
    {"level":6,"diff":3,"scenario":3,"q":"You are designing a fleet-wide automated memory anomaly detector. Which signal has the best precision-recall trade-off for detecting slow leaks?","opts":["Peak heap","GC pause count","A 7-day linear regression on jvm.memory.used{area='heap'} minimum (trough) values — a positive slope indicates retained object growth independent of traffic","CPU usage","Pod restart count"],"ans":2,"fb":"Trough-over-trough growth is the canonical leak signal. Regressing on minimum values filters out allocation noise and traffic variation. A 7-day window provides enough data to distinguish leaks from seasonal patterns."},
    {"level":6,"diff":2,"scenario":3,"q":"A fleet-wide observability rollout needs buy-in from 15 service teams. What is the most effective adoption strategy?","opts":["Hire consultants","Wait for teams to ask","Force all teams simultaneously","Ship a Grafana dashboard template that works out-of-the-box with the standard metrics, and fix the first production incident using it publicly — teams adopt tools that prove their value","Mandate by email"],"ans":3,"fb":"Adoption follows demonstrated value. A ready-made dashboard lowering the effort plus a visible incident win provides both the easy path and the proof. Mandates without value create compliance theater."},
    {"level":6,"diff":3,"scenario":1,"q":"An SLA review reveals that 3 services violate P99 targets during GC. Leadership asks for a fix timeline. What is the most responsible answer?","opts":["2 weeks","Cannot be fixed","Present a tiered plan: quick wins (GC tuning, heap resizing) in 2 weeks for the worst offender, collector migration for the other two in 6-8 weeks with canary validation — and state explicitly which improvements are certain vs experimental","Tomorrow","6 months"],"ans":2,"fb":"Responsible answers decompose the problem: what can be tuned quickly, what requires a collector change, and what is uncertain. Committing to timelines for each tier, with explicit risk, is what leadership needs to plan around."},
    {"level":6,"diff":1,"scenario":2,"q":"What is the primary technical difference between CRaC and GraalVM native image for startup improvement?","opts":["Native image uses JIT","Neither helps startup","CRaC is for native code","They are the same","CRaC checkpoints a running HotSpot JVM (including JIT-compiled code) and restores it; native image compiles ahead-of-time to a standalone binary with no JIT"],"ans":4,"fb":"CRaC preserves the JIT-warmed state by checkpointing the process. Native image pre-compiles everything at build time. CRaC gets both fast start and peak performance; native image trades peak performance for smaller footprint."},
    {"level":6,"diff":3,"scenario":4,"q":"You are investigating a production issue where RSS grows but NMT shows no growth in any JVM category. What is the most likely explanation?","opts":["Metaspace is leaking","Thread stacks are growing","Heap is leaking","The growth is in native memory allocated outside the JVM — a JNI library, a native driver, or glibc malloc fragmentation","NMT is broken"],"ans":3,"fb":"NMT only tracks allocations the JVM itself makes. Native libraries using malloc directly, glibc arena fragmentation, and JNI allocations are invisible to NMT. pmap and /proc/<pid>/smaps are needed to identify the source."},
    {"level":4,"diff":2,"scenario":0,"q":"In Eclipse MAT, the histogram shows 50,000 instances of byte[] totalling 800MB. What is the most useful next step?","opts":["Delete the dump","Sort by retained heap and inspect the largest byte[] instances via their GC root paths to identify which objects are holding them","Increase heap","Restart the pod","Check CPU"],"ans":1,"fb":"Large byte[] arrays are common leak payloads (serialised data, buffers, images). Tracing their GC roots tells you which data structure retains them — often a cache, queue, or static map."}
]
};
